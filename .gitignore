# --- general ---
.DS_Store
.env
.env.*
*.log

# --- legacy (do not push) ---
legacy/

# --- separate project, lives here only for local convenience ---
BioApp/

# --- frontend ---
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.vite/

# --- backend (python common ignores) ---
backend/.venv/
backend/.venv-eval/
backend/venv/
backend/__pycache__/
backend/**/*.pyc
backend/.pytest_cache/
backend/.mypy_cache/
backend/coverage.json
# Nested junk from an earlier cwd path bug (real eval data lives in
# backend/eval-results/, not backend/backend/).
backend/backend/

# --- misc (catch-all) ---
node_modules/
dist/
build/

# --- playwright ---
test-results/
playwright-report/
backend/.coverage

# --- AI scaffolding (never commit) ---
.claude/
.gsd/
.continue-here.md

# --- local scratch ---
screenshots/
.eval-checkpoint/
Untitled
Untitled.*
# Sample CSVs in repo root (use backend/seeds/ or backend/tests/fixtures/ instead)
/*.csv
