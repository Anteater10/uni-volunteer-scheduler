# --- general ---
.DS_Store
.env
.env.*
# but DO track the production env TEMPLATE (placeholders only, no real secrets)
!backend/.env.production.example
*.log

# --- legacy (do not push) ---
legacy/

# --- frontend ---
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.vite/

# --- backend (python common ignores) ---
backend/.venv/
backend/venv/
backend/__pycache__/
backend/**/*.pyc
backend/.pytest_cache/
backend/.mypy_cache/

# --- misc (catch-all) ---
node_modules/
dist/
build/

# --- playwright ---
test-results/
playwright-report/
backend/.coverage

# --- AI scaffolding (never commit) ---
.claude/
.gsd/
.continue-here.md

# --- local scratch ---
screenshots/
.eval-checkpoint/
Untitled
Untitled.*
# Sample CSVs in repo root (use backend/seeds/ or backend/tests/fixtures/ instead)
/*.csv

# demo-day golden database dump (local artifact, not source)
/uvs-demo-golden.sql
.superpowers/
