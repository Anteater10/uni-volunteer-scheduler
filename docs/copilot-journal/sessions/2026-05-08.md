# 2026-05-08 — v1.3 wrap-up + stabilization for v1.4 kickoff

**Phase:** 1.x (v1.3 wrap-up)
**Branch:** feature/google-calendar-link
**Started:** ~14:00
**Ended:** (in progress)

## Goal for this session

Close all v1.3 loose ends, get `main` to a known-stable state, and
clear the runway so Phase 30 of v1.4 can start cleanly.

## What we did

- Audited v1.3 status: 8 of 9 phases shipped, Phase 27 (SMS) deferred,
  Phase 28 (QR) shipped with a deviation, STATE.md was stale.
- Wrote `28-PLAN.md` + `28-SUMMARY.md` retroactively documenting the
  Phase 28 deviation (organizer-displayed event-QR + volunteer
  self-check-in by email, replacing the original
  per-signup-QR-in-email plan).
- Refreshed `.planning/STATE.md` to mark v1.3 code-complete (8/9
  phases) and Phase 27 deferred.
- Locked in the two-folder rule (lecture + publication) and the
  session-start protocol (recommend phase, ask first; superpowers
  before GSD) into memory.
- Updated journal workflow rule: session journal writes append in
  parallel as work happens, not at end-of-session.
- Audited `feature/google-calendar-link` branch: it's mislabeled —
  contains all 100+ v1.3 commits, no actual Google Calendar code.
  `auth.py` had a stray cosmetic regression (login error string
  downgraded to lowercase) — discarded.
- Ran full backend + frontend test suites. Backend: 344/344 clean.
  Frontend: 14 regressions vs Phase 29 baseline (6 known) — all
  test-suite drift, no product bugs.
- Repaired all 14 frontend test regressions:
  - OrganizerRosterPage ×4 — wrap in AuthContext.Provider mock
  - EventDetailPage ×2 — `getAllByText` for responsive duplicates
  - EventsBrowsePage ×3 — match new button copy ("Jump to this week",
    "View next week →")
  - ManageSignupsPage ×1 — header copy "Hi {first}" not "Signups
    for {first} {last}"
  - FormFieldsDrawer ×2 — drawer uses `<li>` not `<table>`; trigger
    is "+ Add question"
  - EventsSection ×2 — switch scope filter to "all" so past-dated
    fixture event becomes visible
  - TemplatesSection ×1 — drop assertion for removed
    "number of sessions" field
  - ImportsSection ×1 — click row to expand collapsed detail panel
- Verified green: 344 backend + 206 frontend = 550/550 pass.
- Pushed branch + updated PR #7 with proper title, body, test
  status, and merge guidance (squash-merge to bury misleading
  branch name).

## Decisions made

- **Phase 27 (SMS) deferred indefinitely** — TCPA + flag-gated, not a
  v1.4 blocker. Revisit post-paper.
- **Branch consolidation strategy:** PR `feature/google-calendar-link`
  → `main` as-is, squash on merge so the misleading name dies in
  history. Lowest-churn path.
- **Two atomic commits today:** (a) v1.3 docs cleanup, (b) v1.4
  scaffold (journal folders + memory rules).

## Concepts introduced

- (none yet — concept docs start with Phase 30)

## Experiments run

- Backend pytest baseline (see Files/results below).
- Frontend vitest baseline.

## Failures hit

- (logged separately in `failures/` if encountered during test runs)

## Files touched

- `.planning/phases/28-qr-check-in/28-PLAN.md` (new, retroactive)
- `.planning/phases/28-qr-check-in/28-SUMMARY.md` (new, retroactive)
- `.planning/STATE.md` (refreshed: v1.3 → complete, 8/9 phases)
- `docs/copilot-journal/sessions/2026-05-08.md` (this file)

## Late-session: v1.3 → v1.4 transition

- v1.3 PR #7 squash-merged to `main` after CI green.
  - Backend coverage gates raised from 0.90 → **1.00** for
    `signup_service.py`, `routers/signups.py`, `celery_app.py`.
  - Redundant pre-Plan-07 `backend-ci` job removed (lacked redis;
    superseded by `phase0-backend-tests`).
  - 44 new tests added across `test_signups_router_full.py` +
    `test_celery_app_full.py` to hit 100% on the three target files.
  - 388 backend tests passing total.
- Created branch `feature/v1.4-phase-30-streaming-chat` from updated
  `main`.
- v1.4 Stage 0 scaffold landed:
  - `.planning/REQUIREMENTS-v1.4.md` (new) — north star + 9-phase table
    + hard requirements + locked open decisions.
  - `.planning/STATE.md` updated: milestone v1.3 complete → v1.4
    scaffolding, Phase 30 planning.
  - `.planning/phases/30-streaming-chat-mvp/30-PLAN.md` (new) — full
    Phase 30 plan with scope, risks, definition-of-done.
  - `docs/learning/30-streaming-chat-mvp/` (new) — 4 lecture stubs
    (sse-streaming, openrouter-integration, telemetry-schema,
    role-aware-system-prompts).
  - `docs/documentation/30-streaming-chat-mvp/` (new) — 4 publication
    stubs matching the lecture list.

## Open threads / TODO for next session

- Lock primary + fallback OpenRouter free-tier model IDs.
- Draft the v0 system prompt (admin + organizer variants) and hash it.
- Begin Phase 30 backend: Alembic migration `0042_copilot_*` first,
  then `app/copilot/llm.py`.

## Notes for the paper

- The Phase 28 deviation is a clean micro-case-study of "the plan
  said X, the simpler thing was Y" — worth referencing in the
  failure-taxonomy or design-decisions section as an example of
  scope inversion driven by infrastructure cost.
