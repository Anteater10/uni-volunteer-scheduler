# Every step from here to W7 done — 2026-08-20

Ordered. `∥` = runs in parallel with the step above it, no waiting.
**Step 0 is decided: SendGrid** (UCSB SSO issues no IAM user credentials, so SES
SMTP is unavailable).

| # | Step | Owner | Est | Depends on | Done when |
|---|---|---|---|---|---|
| **A — today, no decisions needed** | | | | | |
| 1 | **Email UCSB IT for the 3 SendGrid DNS CNAMEs.** Do this first — 24–48h propagation *after* they agree, and they may refuse. | Andy | 15 min | — | Request sent |
| 2 | ∥ Push `569c3ff` (K31). Branch is 1 ahead. | Andy | 1 min | — | `main` has it |
| 3 | ∥ **F6** — startup assertion: `email_mode=sendgrid` with a missing key or from-address must refuse to boot. | Claude | 1 h | — | Boot fails loudly; test asserts it |
| 4 | ∥ **PR #78 / K26** — close the `app/celery_app.py: 0.971 < 1.0` gate. Same file as F6; batch the test run. | Claude | 1–2 h | 3 | CI green, #78 merged |
| 5 | ∥ Fix `config.py:53` — comment still says `smtp` = "prod: AWS SES SMTP". | Claude | 2 min | — | Comment matches reality |
| 6 | ∥ **Dependabot + Semgrep CE** in CI. The permanent regression net. | Claude | 30 min | — | Both run on PRs |
| **B — your decisions (blocks work, isn't work)** | | | | | |
| 7 | **F1** — httpOnly cookies, or accept `localStorage` **in writing**? | Andy | 20 min | — | Decision recorded in a doc |
| 8 | **F2** — approve security headers in `Caddyfile`. | Andy | 5 min | — | Go-ahead given |
| 9 | **K21** — does a late cancellation / no-show carry any consequence? | Andy | 20 min | — | Written into the KB doc |
| 10 | **PR #50** (bulk event builder) — rebase and land, or close with a salvage note? Changes W6.2's scope. | Andy | 20 min | — | Merged or closed |
| 11 | **`/admin/imports`** — delete the dormant endpoints or keep them? | Andy | 10 min | — | Decided |
| **C — apply the approved fixes** | | | | | |
| 12 | **F2** — CSP, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`. Compounds F1: no `connect-src` = nothing stops token exfiltration. | Claude | 1 h | 8 | Headers verified on a response |
| 13 | ∥ **F3** — bump `react-router-dom` off 7.14.0. | Claude | 15 min | — | `npm audit` clean-ish |
| 14 | ∥ **F4** — add `USER` to `frontend/Dockerfile`. | Claude | 15 min | — | Container runs non-root |
| 15 | **F1** — implement whatever step 7 decided. | Claude | 0–1 d | 7 | Accepted-in-writing, or cookies shipped |
| **D — SendGrid live** | | | | | |
| 16 | Create SendGrid account; API key scoped **Mail Send only**, not Full Access. | Andy | 20 min | — | Key exists |
| 17 | DNS CNAMEs land → domain authenticated. **Fallback if refused:** single-sender verification (no DNS; mail shows "via sendgrid.net", deliverability drops — launch-acceptable, not permanent). | Andy + UCSB IT | 1–2 d wait | 1 | Verified in SendGrid UI |
| 18 | Set on AWS: `EMAIL_MODE=sendgrid`, `SENDGRID_API_KEY`, `EMAIL_FROM_ADDRESS` (must match what 17 verified or every send 403s). | Rafael | 15 min | 16, 17 | Vars set |
| 19 | **W4.4 rotate 4 secrets + seed admin.** Non-optional — they were readable in a published image layer. | Andy | 1 h | — | New values in AWS only |
| 20 | **W4.6** — confirm `celery_worker` + `celery_beat` run as **separate ECS tasks**. Email lives here, so this is on the email critical path, not a side item. Five beat schedules die silently otherwise. | Rafael + Andy | 1 h | — | Both tasks running; a beat job observed firing |
| 21 | **S-01** — restate `--proxy-headers` in the ECS task definition. The fix lives in `start_render.sh`, which Rafael never runs. Without it `rate_limit` collapses every caller into one bucket per path and simultaneous QR check-ins 429 each other mid-event. | Rafael + Andy | 30 min | — | Real client IPs in logs |
| 22 | **Send one real email end-to-end and confirm receipt.** | Andy | 30 min | 3, 18, 20 | Mail arrives in a real inbox |
| **E — finish W5** | | | | | |
| 23 | **W5.4** — JWT expiry assertions. (Magic-link single-use already verified: `magic_link_service.py:273` updates under `consumed_at IS NULL`, so it holds under concurrency.) | Claude | 1 h | — | Tests assert expiry |
| 24 | ∥ **W5.6** — write down why broadcasts bypass the unsubscribe link (operational, not promotional). Before it's a CAN-SPAM question, not after. | Claude | 30 min | — | Reasoning documented |
| 25 | ∥ **W5.7** — two inert adversarial cases (`token_budget_exhaustion`, `indirect_injection`): assert, or mark untested. | Claude | 1 h | — | Runner asserts or doc says untested |
| 26 | ∥ **S-03** — collapse six spellings of "staff". The mechanism by which K33 hid. | Claude | 2 h | — | One spelling |
| 27 | ∥ **T3** — cross-check `App.jsx`'s admin-only block against the backend's 43 `require_admin` endpoints. A page gated `organizer\|admin` calling an admin-only endpoint is a broken screen nobody has looked for. | Claude | 1 h | — | Map matches, or mismatches filed |
| **F — W6 runtime verification · the long pole · 3–4 days** | | | | | |
| 28 | **W6.1** — every route × every role × desktop + real phone viewport. 46 routes. | Andy | 1 d | 18 | All walked |
| 29 | **W6.2** — every side-effecting flow with Mailpit open: signup→confirm→reminder→manage; orientation block → credit persisting; waitlist→promote→confirm; admin create→slots→roster→export; organizer on a phone. **Including shifts**, which postdates the last audit. | Andy | 1 d | 10, 28 | All flows complete |
| 30 | **W6.3** — all six templates in Gmail web, Gmail iOS, Apple Mail, Outlook. **Not Mailpit's preview.** Email *is* the app here. | Andy | 0.5 d | 22 | Seen in 4 real clients |
| 31 | **W6.4** — adversarial input: double-submit, back button mid-flow, two tabs, expired token, slot filling between render and submit, network drop, clock skew, non-ASCII + very long names, `+`-addressed email. | Andy | 0.5 d | 28 | Each attempted |
| 32 | **W6.5** — every P0 found in 28–31 becomes a regression test; delete the mocks that hid them (`EventsSection.test.jsx:489` is the reason K1 shipped). | Claude | 0.5 d | 28–31 | Test per P0 |
| 33 | **T2** — one OWASP ZAP pass against the live URL. Would have caught F2. | Andy | 1 h | 18 | Report triaged |
| 34 | **W6.6** — real conditions, if time: a full quarter of data in unbounded tables (K36), 30 concurrent signups against one event (does `slot.current_count` hold?), Safari date parsing, a real iPhone. | Andy | 0.5 d | 28 | Attempted or explicitly skipped |
| **G — re-audit 3 · required, routinely skipped** | | | | | |
| 35 | Targeted re-audit of the **W6 fix batch only** — written under time pressure against a live deploy, reviewed by nobody. Every prior pass found defects in code believed finished. | Claude | 0.5 d | 32 | Findings fixed or accepted |
| **H — W7 handoff · ~0.5 day** | | | | | |
| 36 | **Runbook**, platform-neutral. Consolidate `docs/deployment.md`; don't rewrite. Must include: memory budget (~900MB/worker, 4GB for two), proxy headers (21), Celery as separate services (20), and **"never roll back a deploy that applied a migration"** — forward-only migration + code rollback leaves the old image unable to find the revision the DB is stamped with, which can permanently brick the service. | Claude | 2 h | 20, 21 | Runbook reviewed |
| 37 | ∥ Env-var reference, including `ENVIRONMENT` (defaults to `production`) and the SendGrid trio. | Claude | 30 min | 18 | Complete |
| 38 | ∥ **K39** — fix `PRODUCT-BRIEF.md:31,342`: orientation is a **hard 422 block**, not a soft warning; the CSV import is gone. **And `git add` it** — the file is untracked, so it teaches nobody and is one `rm` from gone. | Claude | 1 h | — | Correct and committed |
| 39 | **Live walkthrough — Rafael deploys from a clean clone while you watch.** This is the exit criterion, not the document. | Andy + Rafael | 2 h | 36, 37 | Clean-clone deploy succeeds |
| **I — re-audit 4 · last gate** | | | | | |
| 40 | Exhaustive re-audit scoped to **the diff since re-audit 2** — the tree has been swept, the diff has not. | Claude | 0.5 d | 39 | W7 done |

---

## Totals

| Block | Days |
|---|---|
| A + B + C (immediate + decisions + fixes) | ~1 |
| D SendGrid live (excl. DNS wait) | ~0.5 |
| E finish W5 | ~0.5 |
| **F W6 runtime verification** | **3–4** |
| G re-audit 3 | 0.5 |
| H W7 handoff | 0.5 |
| I re-audit 4 | 0.5 |
| **Total** | **6–7.5 working days** |

DNS propagation (17) overlaps blocks A–C, so it costs nothing if step 1 goes out
today. If it slips, it pushes step 22 and therefore **W6.3** — the only hard
dependency the email decision creates.

## Hard dependencies worth remembering

- **3 before 22** — until F6 is fixed, a silent no-send and a working config are
  indistinguishable, so the delivery test proves nothing.
- **20 before 22** — no `celery_worker` means no mail at all, regardless of keys.
- **10 before 29** — PR #50 changes what W6.2 has to cover.
- **1 → 17 → 22 → 30** — the email chain, and the only place the SendGrid
  decision can still cost you calendar time.
