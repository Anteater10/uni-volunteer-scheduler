---
phase: 12-retirement-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/RegisterPage.jsx
  - frontend/src/pages/MySignupsPage.jsx
  - frontend/src/pages/EventsPage.jsx
  - frontend/src/pages/SignupConfirmedPage.jsx
  - frontend/src/pages/SignupConfirmFailedPage.jsx
  - frontend/src/pages/SignupConfirmPendingPage.jsx
  - frontend/src/pages/admin/OverridesSection.jsx
  - frontend/src/pages/AdminTemplatesPage.jsx
  - frontend/src/components/PrereqWarningModal.jsx
  - frontend/src/components/ModuleTimeline.jsx
  - frontend/src/components/__tests__/PrereqWarningModal.test.jsx
  - frontend/src/components/__tests__/ModuleTimeline.test.jsx
  - frontend/src/App.jsx
  - frontend/src/components/Layout.jsx
  - frontend/src/lib/api.js
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/admin/TemplatesSection.jsx
autonomous: true
requirements:
  - R12-PAGE-DELETE
  - R12-NAV-UPDATE
  - R12-PREREQ-STRIP
  - R12-LOGIN-EDIT
  - R12-ROLE-GUARD

must_haves:
  truths:
    - "10 dead page/component files are deleted from disk"
    - "2 dead test files (PrereqWarningModal.test, ModuleTimeline.test) are deleted"
    - "App.jsx has no imports or routes for deleted pages"
    - "Layout.jsx has no studentNavItems and no participant case"
    - "Layout.jsx header has no Register link"
    - "LoginPage.jsx has no Register link/button"
    - "api.js has no createSignup, cancelSignup (student), listMySignups, getModuleTimeline, or admin.overrides functions"
    - "TemplatesSection.jsx has no prereq_slugs references"
    - "NotificationsPage and ProfilePage routes have role guards (organizer/admin only)"
    - "npm run build passes with 0 errors"
    - "npm run test passes"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Clean routing with no dead imports, role-guarded notifications/profile"
    - path: "frontend/src/components/Layout.jsx"
      provides: "No student nav, no Register link"
    - path: "frontend/src/lib/api.js"
      provides: "No dead API functions"
    - path: "frontend/src/pages/admin/TemplatesSection.jsx"
      provides: "No prereq_slugs fields"
  key_links:
    - from: "App.jsx routes"
      to: "surviving page components"
      via: "React Router"
      pattern: "Route.*element"
    - from: "Layout.jsx navItemsForRole"
      to: "organizer/admin nav arrays"
      via: "role switch"
      pattern: "navItemsForRole"
---

<objective>
Delete 10 dead frontend page/component files, remove their routes/imports/nav entries, strip prereq_slugs from TemplatesSection, clean up api.js dead functions, edit LoginPage to remove Register link, and add role guards to NotificationsPage + ProfilePage.

Purpose: Collapse the frontend to a single mental model — public pages for volunteers, authenticated pages for organizer/admin only. No dead imports, no dead routes, no dead nav items.

Output: Clean frontend that builds without errors and has no references to retired surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-retirement-pass/12-RESEARCH.md

@frontend/src/App.jsx
@frontend/src/components/Layout.jsx
@frontend/src/lib/api.js
@frontend/src/pages/LoginPage.jsx
@frontend/src/pages/admin/TemplatesSection.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 10 dead page/component files + 2 dead test files, clean App.jsx routes and imports</name>
  <files>
    frontend/src/pages/RegisterPage.jsx
    frontend/src/pages/MySignupsPage.jsx
    frontend/src/pages/EventsPage.jsx
    frontend/src/pages/SignupConfirmedPage.jsx
    frontend/src/pages/SignupConfirmFailedPage.jsx
    frontend/src/pages/SignupConfirmPendingPage.jsx
    frontend/src/pages/admin/OverridesSection.jsx
    frontend/src/pages/AdminTemplatesPage.jsx
    frontend/src/components/PrereqWarningModal.jsx
    frontend/src/components/ModuleTimeline.jsx
    frontend/src/components/__tests__/PrereqWarningModal.test.jsx
    frontend/src/components/__tests__/ModuleTimeline.test.jsx
    frontend/src/App.jsx
  </files>
  <action>
**Step 1: Delete 12 files from disk.**

```bash
rm frontend/src/pages/RegisterPage.jsx
rm frontend/src/pages/MySignupsPage.jsx
rm frontend/src/pages/EventsPage.jsx
rm frontend/src/pages/SignupConfirmedPage.jsx
rm frontend/src/pages/SignupConfirmFailedPage.jsx
rm frontend/src/pages/SignupConfirmPendingPage.jsx
rm frontend/src/pages/admin/OverridesSection.jsx
rm frontend/src/pages/AdminTemplatesPage.jsx
rm frontend/src/components/PrereqWarningModal.jsx
rm frontend/src/components/ModuleTimeline.jsx
rm frontend/src/components/__tests__/PrereqWarningModal.test.jsx
rm frontend/src/components/__tests__/ModuleTimeline.test.jsx
```

**Step 2: Edit `frontend/src/App.jsx`.**

Remove all imports for deleted pages:
- `RegisterPage`
- `MySignupsPage`
- `EventsPage` (the old one — NOT `EventsBrowsePage` which is the v1.1 replacement)
- `SignupConfirmedPage`, `SignupConfirmFailedPage`, `SignupConfirmPendingPage`
- `OverridesSection`
- `AdminTemplatesPage` (if imported — check; it may not have a route but could still be imported)

Remove all `<Route>` elements for deleted pages:
- `/register` route
- `/my-signups` route (inside `<ProtectedRoute>` block)
- `/signup/confirmed`, `/signup/confirm-failed`, `/signup/confirm-pending` routes
- `/admin/overrides` route

**Keep these routes:**
- `/login` -> `<LoginPage />`
- `/events` -> `<EventsBrowsePage />`
- `/signup/confirm` -> `<ConfirmSignupPage />`
- `/signup/manage` -> `<ManageSignupsPage />`
- All `/admin/*` routes except `/admin/overrides`
- All `/organizer/*` routes

**Add role guards to NotificationsPage and ProfilePage routes:**
- These are currently inside a `<ProtectedRoute>` block with no role restriction.
- Change to `<ProtectedRoute roles={["organizer", "admin"]}>` (or however the ProtectedRoute component accepts role props — check its implementation first).
- If ProtectedRoute doesn't support a `roles` prop, add the prop. It should check `user.role` against the allowed roles array and redirect to `/` if not authorized.

**Verify build after edits:**
```bash
cd frontend && npm run build
```

**Commit 1:** Delete 12 files + clean App.jsx imports/routes + add role guards.
  </action>
  <verify>
    <automated>cd /Users/andysubramanian/uni-volunteer-scheduler/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 12 files deleted from disk
    - App.jsx has no imports or routes for any deleted page
    - NotificationsPage and ProfilePage routes have organizer/admin role guards
    - `npm run build` passes with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean Layout.jsx nav, LoginPage Register link, api.js dead functions, TemplatesSection prereq_slugs</name>
  <files>
    frontend/src/components/Layout.jsx
    frontend/src/pages/LoginPage.jsx
    frontend/src/lib/api.js
    frontend/src/pages/admin/TemplatesSection.jsx
  </files>
  <action>
**1. Edit `frontend/src/components/Layout.jsx`:**
- Delete the `studentNavItems` array (the 3 items: `/events`, `/my-signups`, `/profile`).
- Delete the `"participant"` case from `navItemsForRole()` function.
- In the header area, remove the `<Link to="/register">Register</Link>` element (non-authed users should only see Login, not Register).
- Keep `organizerNavItems`, `adminNavItems`, Login link, and all other nav logic.

**2. Edit `frontend/src/pages/LoginPage.jsx`:**
- Remove any "Register" link, button, or "Don't have an account? Register" text.
- If there's a link to `/register`, delete it.
- Optionally update header text to "Organizer / Admin Login" if it currently says something generic like "Sign In" (per locked decision D-01).
- Keep the login form itself untouched — it calls `POST /auth/token` which is surviving.

**3. Edit `frontend/src/lib/api.js`:**

Delete these functions and their exports:
- `createSignup()` (calls retired `POST /signups/`)
- `cancelSignup()` (student-facing cancel — `POST /signups/{id}/cancel` as logged-in student; admin cancel at `api.admin.signups.cancel` SURVIVES)
- `listMySignups()` (calls retired `GET /signups/my`)
- `getModuleTimeline()` (calls deleted `GET /users/me/module-timeline`)

Delete from the exports object:
- `cancelSignup`, `listMySignups`, `moduleTimeline` (or whatever the export names are)

Delete nested aliases:
- `api.signups.create`, `api.signups.cancel`, `api.signups.my`
- `api.admin.overrides.list`, `api.admin.overrides.create`, `api.admin.overrides.revoke`

**Keep:**
- `api.admin.signups.cancel` (admin cancel path — different from student cancel)
- All public API functions (`api.public.*`)
- All other admin, organizer, and auth functions

**4. Edit `frontend/src/pages/admin/TemplatesSection.jsx`:**

Remove all `prereq_slugs` handling:
- Remove `prereq_slugs` from form state initialization (where the form fields are set up).
- Remove `prereq_slugs` from the form JSX (input field, label, any CSV-split display logic).
- Remove `prereq_slugs` from the data display columns (table or card rendering).
- Remove any CSV-split logic that parses `prereq_slugs` as comma-separated values.
- The `prereq_slugs` column was dropped from the DB schema in migration 0009 and removed from the backend schemas in Phase 08. The frontend field references dead data.

**Verify:**
```bash
cd frontend && npm run build && npm run test -- --run
```

**Commit 1:** Layout.jsx nav cleanup + LoginPage Register link removal
**Commit 2:** api.js dead function removal
**Commit 3:** TemplatesSection prereq_slugs strip
  </action>
  <verify>
    <automated>cd /Users/andysubramanian/uni-volunteer-scheduler/frontend && npm run build 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Layout.jsx has no `studentNavItems`, no `"participant"` case, no Register link in header
    - LoginPage.jsx has no Register link/button
    - api.js has no `createSignup`, no student `cancelSignup`, no `listMySignups`, no `getModuleTimeline`, no `admin.overrides.*`
    - TemplatesSection.jsx has zero references to `prereq_slugs`
    - `npm run build` passes
    - `npm run test -- --run` passes (document test count — expected ~73 after deleting 2 test files with ~5 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Deletion-only on frontend. Role guards on NotificationsPage/ProfilePage tighten existing access.

| Boundary | Description |
|----------|-------------|
| browser -> ProtectedRoute | Role guard added to notifications/profile routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Elevation of Privilege | NotificationsPage/ProfilePage | mitigate | Add roles prop to ProtectedRoute; only organizer/admin can access |
| T-12-04 | Information Disclosure | Dead API functions in api.js | mitigate | Delete dead functions so they cannot be called; endpoints are already removed on backend |

Deletion reduces attack surface: 10 dead pages removed, dead API client functions removed.
</threat_model>

<verification>
After both tasks:
1. `npm run build` passes with 0 errors
2. `npm run test -- --run` passes
3. `grep -rn "RegisterPage\|MySignupsPage\|EventsPage\|SignupConfirmed\|OverridesSection\|PrereqWarningModal\|ModuleTimeline\|AdminTemplatesPage" frontend/src/ --include="*.jsx" --include="*.js"` returns 0 matches
4. `grep -rn "prereq_slugs" frontend/src/ --include="*.jsx" --include="*.js"` returns 0 matches
5. `grep -rn "createSignup\|listMySignups\|getModuleTimeline\|admin\.overrides" frontend/src/lib/api.js` returns 0 matches
</verification>

<success_criteria>
- 12 dead files removed from disk
- App.jsx clean: no dead imports, no dead routes, role guards on notifications/profile
- Layout.jsx clean: no student nav, no Register link
- LoginPage.jsx clean: no Register link
- api.js clean: no dead functions
- TemplatesSection.jsx clean: no prereq_slugs
- Frontend builds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-retirement-pass/12-02-SUMMARY.md`
</output>
