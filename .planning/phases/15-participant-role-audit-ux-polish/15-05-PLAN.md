---
phase: 15-participant-role-audit-ux-polish
plan: 05
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - frontend/src/pages/public/ConfirmSignupPage.jsx
  - frontend/src/pages/public/ManageSignupsPage.jsx
  - frontend/src/components/SignupSuccessCard.jsx
autonomous: true
requirements_addressed: [PART-02, PART-07, PART-08, PART-10, PART-11, PART-12, PART-13]
requirements: [PART-02, PART-07, PART-08, PART-10, PART-11, PART-12, PART-13]

must_haves:
  truths:
    - "ConfirmSignupPage replaces its raw page-level spinner with Skeleton block wrapped in aria-busy region"
    - "ConfirmSignupPage error branch renders ErrorState with UI-SPEC magic-link-expired copy"
    - "SignupSuccessCard exposes an 'Add to calendar' PRIMARY button wired to downloadIcs (PART-13 surface B)"
    - "ManageSignupsPage local ErrorCard deleted — all error branches use shared ErrorState primitive"
    - "ManageSignupsPage empty state uses UI-SPEC copy: 'You haven''t signed up for anything yet' + body + 'View events' primary action"
    - "Cancel + cancel-all Modal copy matches UI-SPEC: title 'Cancel this signup?' / body / 'Yes, cancel' (danger) / 'Keep signup' (secondary)"
    - "Cancel-all Modal copy matches UI-SPEC: 'Cancel all signups?' / body / 'Yes, cancel all' / 'Keep my signups'"
    - "Toast on cancel success reads exactly 'Signup canceled.' (UI-SPEC American spelling)"
    - "Status chips on signup rows carry text label + lucide icon (CheckCircle for Confirmed, Clock for Pending)"
  artifacts:
    - path: "frontend/src/pages/public/ConfirmSignupPage.jsx"
      provides: "Polished post-magic-link page with skeleton + ErrorState + wired SignupSuccessCard"
      contains: "ErrorState"
    - path: "frontend/src/pages/public/ManageSignupsPage.jsx"
      provides: "Polished manage page with UI-SPEC copy + shared ErrorState"
      contains: "ErrorState"
    - path: "frontend/src/components/SignupSuccessCard.jsx"
      provides: "Success card with Add-to-Calendar primary button"
      contains: "downloadIcs"
  key_links:
    - from: "frontend/src/components/SignupSuccessCard.jsx"
      to: "frontend/src/lib/calendar.js"
      via: "import { downloadIcs }"
      pattern: "from ['\"]\\.\\./lib/calendar['\"]"
    - from: "frontend/src/pages/public/ConfirmSignupPage.jsx"
      to: "frontend/src/components/SignupSuccessCard.jsx"
      via: "passes event + slot data as props so the card can build .ics"
      pattern: "SignupSuccessCard"
    - from: "frontend/src/pages/public/ManageSignupsPage.jsx"
      to: "frontend/src/components/ui/ErrorState.jsx"
      via: "barrel import (replaces local ErrorCard)"
      pattern: "import \\{[^}]*ErrorState[^}]*\\}"
---

<objective>
Polish the three surfaces of the post-signup flow — ConfirmSignupPage (`/signup/confirm`), SignupSuccessCard (shared component), and ManageSignupsPage (`/signup/manage`) — to UI-SPEC standard. Replace page-level spinners with skeletons, swap local ErrorCards to the shared ErrorState primitive, align destructive-confirm copy exactly, fix toast spelling, add icons to status chips, and wire the Add-to-Calendar PRIMARY button inside SignupSuccessCard (PART-13 surface B).

Purpose: PART-02 (fix stubbed flows), PART-07 (magic-link resilient across browsers — copy + retry affordance), PART-08 (manage with per-row + cancel-all), PART-10 (axe AA), PART-11 (375px), PART-12 (loading/empty/error on every fetch site), PART-13 (Add-to-Calendar surface B — inside SignupSuccessCard).
Output: Three files polished, .ics download callable from both event detail (Plan 04) and confirm page, Manage page aligned to UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-participant-role-audit-ux-polish/15-CONTEXT.md
@.planning/phases/15-participant-role-audit-ux-polish/15-RESEARCH.md
@.planning/phases/15-participant-role-audit-ux-polish/15-UI-SPEC.md
@.planning/phases/15-participant-role-audit-ux-polish/15-PATTERNS.md
@.planning/phases/15-participant-role-audit-ux-polish/15-01-SUMMARY.md
@.planning/phases/15-participant-role-audit-ux-polish/15-02-SUMMARY.md
@frontend/src/pages/public/ConfirmSignupPage.jsx
@frontend/src/pages/public/ManageSignupsPage.jsx
@frontend/src/components/SignupSuccessCard.jsx

<interfaces>
<!-- downloadIcs + ErrorState already shipped by prior waves -->

<!-- UI-SPEC §Destructive confirmations (exact strings MUST match) -->
| Action | Modal title | Modal body | Confirm button | Cancel button |
| Cancel single signup | "Cancel this signup?" | "You'll lose your spot. If the event fills up, you may not get it back." | "Yes, cancel" (danger) | "Keep signup" (secondary) |
| Cancel ALL signups | "Cancel all signups?" | "You'll lose every spot you've reserved for this event. This can't be undone." | "Yes, cancel all" (danger) | "Keep my signups" (secondary) |

<!-- UI-SPEC §Success copy row 2 -->
After cancel → Toast: "Signup canceled." (neutral, 3s) — note American spelling "canceled"

<!-- UI-SPEC §Success copy row 3 -->
After .ics download → Toast: "Calendar file saved. Open it to add to your calendar."

<!-- UI-SPEC §Error states magic-link rows -->
| Condition | Heading | Body | Action |
| Magic-link expired | "This link has expired" | "Magic links are good for 24 hours. Open the event again and re-submit your signup to get a new one." | "Back to events" (primary) |
| Magic-link invalid | "This link isn't valid" | "The link might be incomplete — copy and paste the full URL from your email." | "Back to events" (secondary) |

<!-- UI-SPEC §Empty states /signup/manage row -->
| Title | Body | Action |
| "You haven't signed up for anything yet" | "Browse this week's volunteer events to get started." | "View events" (primary) |

<!-- UI-SPEC §Add-to-Calendar row for ConfirmSignupPage -->
| Surface | Button label | Variant | Placement |
| ConfirmSignupPage (post-confirm) | "Add to calendar" | primary | inside SignupSuccessCard |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignupSuccessCard — accept event/slot props + wire Add-to-Calendar primary button</name>
  <files>
    frontend/src/components/SignupSuccessCard.jsx
  </files>
  <read_first>
    - frontend/src/components/SignupSuccessCard.jsx (full file — understand current props and render)
    - frontend/src/lib/calendar.js (Plan 02 output — downloadIcs signature)
    - frontend/src/state/toast.js (toast API)
    - .planning/phases/15-participant-role-audit-ux-polish/15-UI-SPEC.md (§Add-to-Calendar row 1, §Success copy rows 2-3)
    - .planning/phases/15-participant-role-audit-ux-polish/15-PATTERNS.md (ConfirmSignupPage section on Add-to-Calendar wiring)
  </read_first>
  <action>
    Edit `frontend/src/components/SignupSuccessCard.jsx` in place.

    **1) Add optional props `event` and `slot`** (plus anything else the component already accepts). The component must render gracefully if either is undefined (back-compat for callers that don't pass them yet).

    **2) Add imports** at the top:
    ```javascript
    import { downloadIcs } from "../lib/calendar";
    import { toast } from "../state/toast";
    import { Button } from "./ui";
    ```
    (If `Button` or `toast` is already imported, don't double-import.)

    **3) Add the Add-to-Calendar PRIMARY button** inside the card's CTA area (below success messaging, alongside or above any existing "Manage my signups" button). Rendering gated on both `event` and `slot` being present:

    ```jsx
    {event && slot ? (
      <Button
        variant="primary"
        onClick={() => {
          const dateStr =
            event.start_date ||
            (slot.start_time
              ? new Date(slot.start_time).toISOString().slice(0, 10)
              : "event");
          const slugPart = event.slug || event.id;
          const filename = `scitrek-${slugPart}-${dateStr}.ics`;
          downloadIcs({ event, slot, filename });
          toast.success("Calendar file saved. Open it to add to your calendar.");
        }}
      >
        Add to calendar
      </Button>
    ) : null}
    ```

    **4) Preserve all existing functionality.** If the card currently renders a check icon, confirmation text, a "Manage my signups" secondary button, etc., keep them. This plan ADDS the calendar button — it does not remove or relabel the existing CTA.

    **5) Primary vs secondary clarity.** Per UI-SPEC §Add-to-Calendar row 2, on ConfirmSignupPage the Add-to-Calendar button is PRIMARY (brand). If the card also has a "Manage my signups" button, that one should be SECONDARY after this change. Adjust variant props accordingly.

    **Do NOT:**
    - Touch `frontend/src/lib/api.js`
    - Remove existing props or change the component's default export
    - Add a hard dependency on `event`/`slot` — the button only renders when both are present
  </action>
  <verify>
    <automated>grep -q "import { downloadIcs }" frontend/src/components/SignupSuccessCard.jsx && grep -q "Add to calendar" frontend/src/components/SignupSuccessCard.jsx && grep -q "Calendar file saved. Open it to add to your calendar" frontend/src/components/SignupSuccessCard.jsx && grep -q 'variant="primary"' frontend/src/components/SignupSuccessCard.jsx && cd frontend && npm run test -- SignupSuccessCard --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { downloadIcs }" frontend/src/components/SignupSuccessCard.jsx` returns 1
    - `grep -c "Add to calendar" frontend/src/components/SignupSuccessCard.jsx` returns at least 1
    - `grep -c "Calendar file saved. Open it to add to your calendar" frontend/src/components/SignupSuccessCard.jsx` returns 1
    - `grep -c 'variant="primary"' frontend/src/components/SignupSuccessCard.jsx` returns at least 1
    - `grep -c "scitrek-" frontend/src/components/SignupSuccessCard.jsx` returns 1
    - Button render is gated by `event && slot` (optional props) — file contains a guard check
    - Component still has a default export (`export default SignupSuccessCard` or similar)
    - `cd frontend && npm run test -- SignupSuccessCard --run` exits 0 (update existing tests if they snapshot the component; add assertion for new button if test file exists)
    - `git diff frontend/src/lib/api.js` is empty
  </acceptance_criteria>
  <done>
    SignupSuccessCard hosts the primary Add-to-Calendar button. Backward-compatible: callers that do not pass event/slot render the card as before.
  </done>
</task>

<task type="auto">
  <name>Task 2: ConfirmSignupPage — skeleton loading + ErrorState + pass event/slot to SignupSuccessCard</name>
  <files>
    frontend/src/pages/public/ConfirmSignupPage.jsx
  </files>
  <read_first>
    - frontend/src/pages/public/ConfirmSignupPage.jsx (full file — 67 lines; focus lines 10 imports, 30-37 confirming branch, 39-50 error branch, 54-66 success branch)
    - frontend/src/components/SignupSuccessCard.jsx (post-Task-1 — new props)
    - frontend/src/components/ui/ErrorState.jsx
    - .planning/phases/15-participant-role-audit-ux-polish/15-UI-SPEC.md (§Error states magic-link rows, §Loading copy)
    - .planning/phases/15-participant-role-audit-ux-polish/15-PATTERNS.md (`ConfirmSignupPage.jsx` section lines 371-412)
    - .planning/phases/15-participant-role-audit-ux-polish/15-RESEARCH.md (§Anti-Patterns — `animate-spin` page-level spinner banned)
  </read_first>
  <action>
    Edit `frontend/src/pages/public/ConfirmSignupPage.jsx` in place.

    **1) Update barrel import (line 10).** Add `Skeleton` and `ErrorState` (and `Button` if not already) to the existing destructured import:
    ```javascript
    import { Button, Card, Skeleton, ErrorState } from "../../components/ui";
    ```

    **2) Replace the "confirming" branch (lines 30-37).** The current raw `animate-spin` page-level spinner violates UI-SPEC §Loading copy. Replace with a Skeleton stack wrapped in aria-busy:

    Before:
    ```jsx
    if (state === "confirming") {
      return (
        <div className="flex flex-col items-center justify-center mt-20 gap-4">
          <div className="animate-spin h-8 w-8 border-4 border-blue-500 border-t-transparent rounded-full" />
          <p className="text-gray-600">Confirming your signup...</p>
        </div>
      );
    }
    ```

    After:
    ```jsx
    if (state === "confirming") {
      return (
        <div aria-busy="true" aria-live="polite" className="max-w-md mx-auto mt-12 space-y-3 px-4">
          <Skeleton className="h-8 rounded-xl" />
          <Skeleton className="h-16 rounded-xl" />
          <Skeleton className="h-10 rounded-xl" />
        </div>
      );
    }
    ```

    **3) Replace the error branch (lines 39-50).** Distinguish expired vs invalid if the state machine differentiates them; otherwise default to the "expired" copy (most common case). If `state === "error"` is the only error flag, use the expired copy:

    ```jsx
    if (state === "error") {
      return (
        <ErrorState
          title="This link has expired"
          body="Magic links are good for 24 hours. Open the event again and re-submit your signup to get a new one."
          action={
            <Button variant="primary" onClick={() => navigate("/events")}>
              Back to events
            </Button>
          }
        />
      );
    }
    ```

    If the state machine exposes an `errorKind` (e.g. `"expired"` vs `"invalid"`), branch the title/body on it using the UI-SPEC table — invalid uses `"This link isn't valid"` + `"The link might be incomplete — copy and paste the full URL from your email."` + secondary Back button.

    **4) Wire event/slot props into the success-branch SignupSuccessCard** (lines ~54-66). The current success branch likely renders `<ManageSignupsPage tokenOverride={token} />` inline. If the page receives the confirmed signup's event + slot in the confirm response, pass them to a `SignupSuccessCard`:

    ```jsx
    if (state === "confirmed") {
      return (
        <>
          <SignupSuccessCard event={confirmedEvent} slot={confirmedSlot} />
          <ManageSignupsPage tokenOverride={token} />
        </>
      );
    }
    ```

    If the confirm response does NOT include event + slot today (backend-bounded data), pass `undefined` for `event`/`slot` — SignupSuccessCard's new prop-gate will skip the Add-to-Calendar button cleanly. Document this in the Plan 05 SUMMARY so Wave 2 knows the surface B button only renders when server returns the required fields; if it doesn't, file a follow-up ticket for a future phase (D-14: api.js is read-only; don't fix backend here).

    **5) Reuse or add `useNavigate`** for the Back button handler. `react-router-dom` is already in this page's imports.
  </action>
  <verify>
    <automated>grep -q "ErrorState" frontend/src/pages/public/ConfirmSignupPage.jsx && grep -q "This link has expired" frontend/src/pages/public/ConfirmSignupPage.jsx && grep -q "Magic links are good for 24 hours" frontend/src/pages/public/ConfirmSignupPage.jsx && grep -q "Skeleton" frontend/src/pages/public/ConfirmSignupPage.jsx && grep -q 'aria-busy="true"' frontend/src/pages/public/ConfirmSignupPage.jsx && ! grep -q "animate-spin" frontend/src/pages/public/ConfirmSignupPage.jsx && cd frontend && npm run test -- ConfirmSignupPage --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ErrorState" frontend/src/pages/public/ConfirmSignupPage.jsx` returns at least 2
    - `grep -c "This link has expired" frontend/src/pages/public/ConfirmSignupPage.jsx` returns 1
    - `grep -c "Magic links are good for 24 hours" frontend/src/pages/public/ConfirmSignupPage.jsx` returns 1
    - `grep -c "Back to events" frontend/src/pages/public/ConfirmSignupPage.jsx` returns at least 1
    - `grep -c "Skeleton" frontend/src/pages/public/ConfirmSignupPage.jsx` returns at least 2 (import + usage)
    - `grep -c 'aria-busy="true"' frontend/src/pages/public/ConfirmSignupPage.jsx` returns at least 1
    - `grep -c 'aria-live="polite"' frontend/src/pages/public/ConfirmSignupPage.jsx` returns at least 1
    - `grep -c "animate-spin" frontend/src/pages/public/ConfirmSignupPage.jsx` returns 0 (page-level spinner removed)
    - `grep -c "Confirming your signup" frontend/src/pages/public/ConfirmSignupPage.jsx` returns 0 (old bare-text loading gone)
    - `cd frontend && npm run test -- ConfirmSignupPage --run` exits 0
    - `git diff frontend/src/lib/api.js` is empty
  </acceptance_criteria>
  <done>
    ConfirmSignupPage uses Skeleton for loading, ErrorState for magic-link errors with UI-SPEC copy, and passes event/slot to SignupSuccessCard when available. Surface B of PART-13 renders when server data permits.
  </done>
</task>

<task type="auto">
  <name>Task 3: ManageSignupsPage — delete local ErrorCard + UI-SPEC copy + modal labels + toast spelling + status chip icons</name>
  <files>
    frontend/src/pages/public/ManageSignupsPage.jsx
  </files>
  <read_first>
    - frontend/src/pages/public/ManageSignupsPage.jsx (full file — focus lines 12 imports, 30-42 local ErrorCard, 104 cancel toast, 146 empty copy, 157 h1, 187-196 status chip, 223-276 cancel modals)
    - frontend/src/components/ui/ErrorState.jsx
    - .planning/phases/15-participant-role-audit-ux-polish/15-UI-SPEC.md (§Destructive confirmations table, §Success copy rows, §Empty states /signup/manage row, §Per-page primary CTA row 4)
    - .planning/phases/15-participant-role-audit-ux-polish/15-PATTERNS.md (`ManageSignupsPage.jsx` section lines 415-468)
  </read_first>
  <action>
    Edit `frontend/src/pages/public/ManageSignupsPage.jsx` in place. Six concrete changes:

    **1) Update barrel import (line ~12).** Add `ErrorState` to the destructured list:
    ```javascript
    import { Button, Card, Chip, Skeleton, EmptyState, ErrorState, Modal, PageHeader } from "../../components/ui";
    ```

    **2) Delete the local `ErrorCard` component (lines ~30-42).** Remove the entire `function ErrorCard() { ... }` block. Every call site must now render `<ErrorState />` directly.

    **3) Replace all `<ErrorCard />` usages with ErrorState** using UI-SPEC network-error copy. The page likely uses ErrorCard for "no token" and "fetch failed" branches. Both become:
    ```jsx
    <ErrorState
      title="We couldn't load this page"
      body="Check your connection and try again. If the problem continues, email scitrek@ucsb.edu."
      action={
        <Button variant="secondary" onClick={() => q.refetch()}>
          Try again
        </Button>
      }
    />
    ```
    For the no-token case (where refetch is irrelevant), swap the action to a primary "Back to events" button navigating to `/events`.

    **4) Update empty state copy (line ~146).** Replace the current `"No upcoming signups"` EmptyState with:
    ```jsx
    <EmptyState
      title="You haven't signed up for anything yet"
      body="Browse this week's volunteer events to get started."
      action={
        <Button variant="primary" onClick={() => navigate('/events')}>
          View events
        </Button>
      }
    />
    ```
    (Import `useNavigate` if needed.)

    **5) Update cancel-single and cancel-all Modal copy (lines ~223-276).** Copy must match UI-SPEC §Destructive confirmations EXACTLY:

    Cancel-single Modal:
    - title: `"Cancel this signup?"`
    - body: `"You'll lose your spot. If the event fills up, you may not get it back."`
    - danger confirm label: `"Yes, cancel"` (gerund during pending: `"Canceling…"`)
    - secondary cancel label: `"Keep signup"` (was `"Never mind"`)

    Cancel-all Modal:
    - title: `"Cancel all signups?"`
    - body: `"You'll lose every spot you've reserved for this event. This can't be undone."`
    - danger confirm label: `"Yes, cancel all"` (pending: `"Canceling all…"`)
    - secondary cancel label: `"Keep my signups"`

    Keep the existing Modal structure (role="dialog", focus trap, ESC close) — only the label strings change.

    **6) Fix toast spelling (line ~104).** UI-SPEC uses American spelling `"canceled"` (one L). Change:
    ```javascript
    toast.success("Signup cancelled.");
    ```
    to:
    ```javascript
    toast.success("Signup canceled.");
    ```
    Apply the same one-L spelling consistently to any other toast / button pending label (`Canceling…` not `Cancelling…`).

    **7) Add lucide-react icons to status chips (lines ~187-196).** Currently color-only. Add:
    ```javascript
    import { CheckCircle, Clock } from 'lucide-react'
    // ...
    <Chip tone={signup.status === "confirmed" ? "success" : "warning"}>
      {signup.status === "confirmed" ? (
        <CheckCircle size={12} aria-hidden="true" />
      ) : (
        <Clock size={12} aria-hidden="true" />
      )}
      {signup.status === "confirmed" ? "Confirmed" : "Pending"}
    </Chip>
    ```
    Use the Chip primitive's actual `tone`/`variant` prop names. If Chip only supports one variant, render the icon inline next to the label text so color is not the sole signal.

    **8) Align page heading to UI-SPEC §Typography display.** Line ~157 uses `text-xl`; UI-SPEC Display is `text-2xl md:text-3xl font-semibold`. EITHER update the class OR replace the raw `h1` with `<PageHeader title="Your signups" />` — prefer PageHeader for consistency with other public pages. The primitive already handles the typography tokens correctly.

    **Do NOT:**
    - Touch `frontend/src/lib/api.js` (D-14)
    - Change the cancel mutation, refetch logic, or token handling
    - Remove the per-row + cancel-all controls (PART-08 requires both)
  </action>
  <verify>
    <automated>grep -q "ErrorState" frontend/src/pages/public/ManageSignupsPage.jsx && ! grep -q "function ErrorCard" frontend/src/pages/public/ManageSignupsPage.jsx && grep -q "You haven't signed up for anything yet" frontend/src/pages/public/ManageSignupsPage.jsx && grep -q "Keep signup" frontend/src/pages/public/ManageSignupsPage.jsx && grep -q "Keep my signups" frontend/src/pages/public/ManageSignupsPage.jsx && grep -q "Yes, cancel all" frontend/src/pages/public/ManageSignupsPage.jsx && grep -q "Signup canceled." frontend/src/pages/public/ManageSignupsPage.jsx && ! grep -q "Signup cancelled" frontend/src/pages/public/ManageSignupsPage.jsx && cd frontend && npm run test -- ManageSignupsPage --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ErrorState" frontend/src/pages/public/ManageSignupsPage.jsx` returns at least 2 (import + usage)
    - `grep -c "function ErrorCard" frontend/src/pages/public/ManageSignupsPage.jsx` returns 0 (local component deleted)
    - `grep -c "You haven't signed up for anything yet" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Browse this week's volunteer events" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "View events" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Cancel this signup?" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "You'll lose your spot" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Yes, cancel" frontend/src/pages/public/ManageSignupsPage.jsx` returns at least 1
    - `grep -c "Keep signup" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Cancel all signups?" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Yes, cancel all" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Keep my signups" frontend/src/pages/public/ManageSignupsPage.jsx` returns 1
    - `grep -c "Signup canceled\." frontend/src/pages/public/ManageSignupsPage.jsx` returns 1 (one L)
    - `grep -c "Signup cancelled" frontend/src/pages/public/ManageSignupsPage.jsx` returns 0 (two-L spelling gone)
    - `grep -c "CheckCircle\|Clock" frontend/src/pages/public/ManageSignupsPage.jsx` returns at least 2 (lucide icon usage)
    - `cd frontend && npm run test -- ManageSignupsPage --run` exits 0
    - `git diff frontend/src/lib/api.js` is empty
  </acceptance_criteria>
  <done>
    ManageSignupsPage uses shared ErrorState, UI-SPEC copy everywhere, one-L "canceled" spelling, icons on status chips, and `PageHeader`-style heading. Per-row + cancel-all controls preserved (PART-08).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Magic-link token (URL query) → backend → page render | Token is user-capability; server validates and burns single-use |
| Cancel Modal confirm click → DELETE /public/signups/:id | Destructive action; server-authoritative |
| downloadIcs filename via SignupSuccessCard | Constructed from event.slug/id + start_date |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | Cancel Modal confirm action | mitigate | Modal uses existing ManageSignupsPage mutation wrapper — goes through api.public.* (read-only per D-14). No new wiring. Destructive confirm requires explicit click; color + copy both signal danger. |
| T-15-05-02 | Information Disclosure | Replacement of local ErrorCard | mitigate | Old ErrorCard may have leaked error messages via `q.error?.message`. New ErrorState uses hardcoded UI-SPEC copy — no raw backend text surfaces to user. |
| T-15-05-03 | Tampering | SignupSuccessCard Add-to-Calendar flow | mitigate | Same escape discipline as Plan 04 — Plan 02's `escapeText()` is the single RFC 5545 escape path. Filename sanitized by browser download UI. |
| T-15-05-04 | Spoofing | Magic-link token in URL bar | accept | Token is single-use server-side; this phase does not modify token lifecycle. Standard capability-URL pattern, unchanged. |
| T-15-05-05 | Clickjacking | /signup/manage in iframe | accept | NOTE-only per security_threat_model. Backend concern (X-Frame-Options / CSP frame-ancestors). File follow-up if PART-AUDIT surfaces it in Wave 2; do not fix in this worktree (D-14). |
| T-15-05-06 | Denial of Service | Cancel-all spam | accept | react-query mutation dedupes; Modal pending-state disables confirm button. Server rate-limits. Low risk. |
| T-15-05-07 | Tampering | Status chip icon rendering | mitigate | lucide icons imported as React components (not URLs, not strings). JSX renders directly — no injection surface. |
</threat_model>

<verification>
After all three tasks:
1. Dev server boots; `/signup/confirm?token=...` with a valid token renders SignupSuccessCard with Add-to-Calendar primary button when server returns event+slot data
2. `/signup/confirm?token=invalid` renders ErrorState with UI-SPEC magic-link-expired copy + "Back to events" primary
3. `/signup/manage?token=...` with signups: each row's status chip carries lucide icon + text label; per-row Cancel and page-level Cancel all present
4. Clicking per-row Cancel opens Modal with UI-SPEC title/body/"Yes, cancel"/"Keep signup"
5. Clicking Cancel all opens Modal with UI-SPEC title/body/"Yes, cancel all"/"Keep my signups"
6. Toast after cancel reads "Signup canceled." (one L)
7. Empty `/signup/manage` shows "You haven't signed up for anything yet" + "View events" primary
8. `cd frontend && npm run test -- ConfirmSignupPage ManageSignupsPage SignupSuccessCard --run` all exit 0
9. `git diff frontend/src/lib/api.js` empty; `git diff frontend/src/App.jsx` empty
</verification>

<success_criteria>
1. SignupSuccessCard accepts optional event/slot props and renders primary Add-to-Calendar button when both present
2. ConfirmSignupPage uses Skeleton for loading (no animate-spin page spinner), ErrorState for error, wires event/slot to SignupSuccessCard
3. ManageSignupsPage uses shared ErrorState (local ErrorCard deleted) — every error branch consistent
4. All destructive-confirm Modal copy matches UI-SPEC §Destructive confirmations exactly
5. Toast message uses American spelling "canceled"
6. Status chips on signup rows carry icon + label
7. Empty state on Manage uses UI-SPEC copy + "View events" primary
8. PR-only files, api.js, App.jsx, and admin/organizer pillars remain untouched
9. Vitest suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/15-participant-role-audit-ux-polish/15-05-SUMMARY.md` documenting:
- Props added to SignupSuccessCard (event, slot — both optional)
- Whether the confirm API currently returns event+slot data; if NO, note that surface B Add-to-Calendar only renders when server payload is extended (follow-up, out of scope per D-14)
- Exact Modal copy strings shipped (both Cancel Modals)
- Toast spelling fix audit (all "cancelled" → "canceled")
- Confirmation api.js + App.jsx + PR-only files untouched
- PART-07, PART-08, PART-13 surface B status for PART-AUDIT.md
</output>
