---
phase: 01-mobile-first-frontend-pass-tailwind-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.js
  - frontend/src/index.css
  - frontend/src/App.jsx
  - frontend/src/App.css
  - frontend/src/components/Layout.jsx
  - frontend/index.html
autonomous: true
requirements:
  - Tailwind migration
must_haves:
  truths:
    - "Tailwind v4 utilities work in dev and production builds"
    - "App.css boilerplate is fully removed"
    - "Layout shell renders header (h-14) and reserves room for bottom nav (pb-20 md:pb-0)"
    - "Theme tokens are declared in @theme block in src/index.css"
  artifacts:
    - path: frontend/src/index.css
      provides: "Tailwind v4 entry + @theme tokens"
      contains: "@import \"tailwindcss\""
    - path: frontend/vite.config.js
      provides: "Tailwind Vite plugin wired"
      contains: "@tailwindcss/vite"
    - path: frontend/src/components/Layout.jsx
      provides: "Mobile-first layout shell"
  key_links:
    - from: frontend/vite.config.js
      to: frontend/src/index.css
      via: "Tailwind v4 plugin processes index.css"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Install Tailwind v4 via the @tailwindcss/vite plugin with CSS-first config, declare the @theme tokens from UI-SPEC, delete legacy App.css boilerplate, and rebuild Layout.jsx as a mobile-first shell with sticky header and bottom-nav reservation.

Purpose: Foundation that every other plan in this phase depends on. Ships the design tokens, the layout structure, and proves the build pipeline works.
Output: Working Tailwind v4 build, theme tokens, clean layout shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-CONTEXT.md
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-RESEARCH.md
@frontend/vite.config.js
@frontend/src/App.jsx
@frontend/src/App.css
@frontend/src/index.css
@frontend/src/components/Layout.jsx
@frontend/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 + Vite plugin</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>frontend/package.json</read_first>
  <action>Run `npm install -D tailwindcss@^4 @tailwindcss/vite@^4` from `frontend/`. Do NOT install postcss, autoprefixer, or `tailwindcss-cli` — v4 with the Vite plugin needs none of them. Do NOT create `tailwind.config.js` or `postcss.config.js`. Verify both packages are added to devDependencies in package.json with major version `^4`.</action>
  <verify>cd frontend && node -e "const p=require('./package.json'); if(!p.devDependencies['tailwindcss']||!p.devDependencies['@tailwindcss/vite']) process.exit(1)"</verify>
  <acceptance_criteria>
    - frontend/package.json devDependencies contains "tailwindcss" with version starting "^4"
    - frontend/package.json devDependencies contains "@tailwindcss/vite" with version starting "^4"
    - frontend/tailwind.config.js does NOT exist
    - frontend/postcss.config.js does NOT exist
  </acceptance_criteria>
  <done>Tailwind v4 + Vite plugin installed, no extra configs created.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind plugin into vite.config.js</name>
  <files>frontend/vite.config.js</files>
  <read_first>frontend/vite.config.js</read_first>
  <action>Add `import tailwindcss from '@tailwindcss/vite'` at the top of vite.config.js. Add `tailwindcss()` to the `plugins` array AFTER `react()`. Do not change any other Vite settings (server, test, build).</action>
  <verify>cd frontend && grep -q "@tailwindcss/vite" vite.config.js && grep -q "tailwindcss()" vite.config.js</verify>
  <acceptance_criteria>
    - frontend/vite.config.js contains `from '@tailwindcss/vite'`
    - frontend/vite.config.js plugins array contains `tailwindcss()`
    - frontend/vite.config.js plugins array still contains `react()`
  </acceptance_criteria>
  <done>Vite config has Tailwind plugin alongside React plugin.</done>
</task>

<task type="auto">
  <name>Task 3: Replace src/index.css with Tailwind v4 entry + @theme tokens</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css, .planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md, .planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-RESEARCH.md</read_first>
  <action>Replace the entire contents of frontend/src/index.css with:

```css
@import "tailwindcss";

@theme {
  --color-bg: #ffffff;          /* TODO(brand) */
  --color-surface: #f8fafc;     /* TODO(brand) */
  --color-fg: #0f172a;          /* TODO(brand) */
  --color-fg-muted: #475569;    /* TODO(brand) */
  --color-border: #e2e8f0;      /* TODO(brand) */
  --color-brand: #0284c7;       /* TODO(brand) */
  --color-brand-fg: #ffffff;    /* TODO(brand) */
  --color-danger: #dc2626;      /* TODO(brand) */
  --color-success: #16a34a;     /* TODO(brand) */

  --font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;

  --header-h: 56px;
}

html, body, #root { height: 100%; }
body {
  font-family: var(--font-sans);
  background: var(--color-bg);
  color: var(--color-fg);
  -webkit-font-smoothing: antialiased;
  font-size: 16px;
}
```

Do NOT keep any prior CSS. Do NOT add a `tailwind.config.js`.</action>
  <verify>cd frontend && grep -q '@import "tailwindcss"' src/index.css && grep -q "@theme" src/index.css && grep -q "TODO(brand)" src/index.css</verify>
  <acceptance_criteria>
    - frontend/src/index.css starts with `@import "tailwindcss";`
    - frontend/src/index.css contains `@theme {`
    - frontend/src/index.css contains all 9 color tokens (bg, surface, fg, fg-muted, border, brand, brand-fg, danger, success)
    - frontend/src/index.css contains `--header-h: 56px`
    - frontend/src/index.css contains at least 9 `TODO(brand)` comments
    - frontend/src/index.css contains `font-size: 16px` in body rule
  </acceptance_criteria>
  <done>index.css has Tailwind v4 entry + theme tokens + 16px body floor.</done>
</task>

<task type="auto">
  <name>Task 4: Delete App.css and remove its import</name>
  <files>frontend/src/App.css, frontend/src/App.jsx</files>
  <read_first>frontend/src/App.css, frontend/src/App.jsx</read_first>
  <action>Delete frontend/src/App.css entirely (`rm frontend/src/App.css`). Open frontend/src/App.jsx and remove the line `import './App.css'` if present. Do not change any other imports or component logic.</action>
  <verify>cd frontend && test ! -f src/App.css && ! grep -q "App.css" src/App.jsx</verify>
  <acceptance_criteria>
    - frontend/src/App.css does NOT exist
    - frontend/src/App.jsx does NOT contain `App.css`
  </acceptance_criteria>
  <done>App.css removed, no dangling import.</done>
</task>

<task type="auto">
  <name>Task 5: Add &lt;html lang="en"&gt; and meta viewport in index.html</name>
  <files>frontend/index.html</files>
  <read_first>frontend/index.html</read_first>
  <action>Open frontend/index.html. Ensure the `<html>` tag has `lang="en"`. Ensure the `<head>` contains `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` (the `viewport-fit=cover` is required for `safe-area-inset-bottom`). Add `<meta name="robots" content="index,follow">` if missing. Do not change `<title>` or other tags.</action>
  <verify>grep -q 'lang="en"' frontend/index.html && grep -q 'viewport-fit=cover' frontend/index.html && grep -q 'name="robots"' frontend/index.html</verify>
  <acceptance_criteria>
    - frontend/index.html contains `<html lang="en">`
    - frontend/index.html contains `viewport-fit=cover`
    - frontend/index.html contains `name="robots"` with `index,follow`
  </acceptance_criteria>
  <done>HTML root has lang, mobile viewport with safe-area, and robots meta.</done>
</task>

<task type="auto">
  <name>Task 6: Rebuild Layout.jsx as mobile-first shell</name>
  <files>frontend/src/components/Layout.jsx</files>
  <read_first>frontend/src/components/Layout.jsx, frontend/src/App.jsx, .planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md</read_first>
  <action>Rewrite frontend/src/components/Layout.jsx as a mobile-first shell. Requirements (concrete):

- Outer wrapper: `<div className="min-h-screen flex flex-col bg-[var(--color-bg)] text-[var(--color-fg)]">`.
- Sticky header: `<header className="sticky top-0 z-30 h-14 border-b border-[var(--color-border)] bg-[var(--color-bg)]/95 backdrop-blur"><div className="mx-auto flex h-full max-w-screen-md items-center justify-between px-4"><span className="font-semibold">{/* TODO(brand): logo/wordmark */}Volunteer Scheduler</span></div></header>`.
- Main: `<main className="flex-1 mx-auto w-full max-w-screen-md px-4 pb-20 md:pb-8"><Outlet /></main>` (use the existing React Router `<Outlet />` import).
- Footer slot at the bottom: leave a placeholder div `<div id="bottom-nav-slot" />` directly after `</main>` — Plan 03 mounts BottomNav into Layout via this slot. For now this plan can render an empty `<></>` placeholder; the slot id is sufficient.
- Preserve all existing logic for auth/state from the prior Layout.jsx (reading user state, redirects). Do NOT delete behavior — only swap markup/classNames.
- Remove any old CSS class names that referenced removed App.css.

Add a `TODO(brand)` comment near the brand wordmark and a `TODO(copy)` comment near any user-visible string.</action>
  <verify>cd frontend && grep -q "max-w-screen-md" src/components/Layout.jsx && grep -q "h-14" src/components/Layout.jsx && grep -q "pb-20 md:pb-8" src/components/Layout.jsx && grep -q "TODO(brand)" src/components/Layout.jsx</verify>
  <acceptance_criteria>
    - Layout.jsx contains `sticky top-0` for header
    - Layout.jsx contains `h-14` for header
    - Layout.jsx contains `pb-20 md:pb-8` on main
    - Layout.jsx contains `max-w-screen-md`
    - Layout.jsx contains `TODO(brand)` at least once
    - Layout.jsx still imports and renders `<Outlet />`
    - Layout.jsx does NOT reference any class from the deleted App.css
  </acceptance_criteria>
  <done>Layout shell is mobile-first with sticky header and reserved bottom-nav space.</done>
</task>

<task type="auto">
  <name>Task 7: Verify dev build and production build succeed</name>
  <files></files>
  <read_first>frontend/package.json</read_first>
  <action>From `frontend/`, run `npm run build`. The build must complete with no errors. If there are warnings about unused CSS or missing tokens, investigate and fix. Do not commit broken builds.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `npm run build` exits 0 from frontend/
    - frontend/dist/index.html exists after build
    - frontend/dist contains a CSS file with `--color-brand` defined (tokens propagated)
  </acceptance_criteria>
  <done>Production build succeeds with Tailwind v4 active.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds in frontend/
- [ ] `grep -r "App.css" frontend/src` returns zero results
- [ ] `grep -c "TODO(brand)" frontend/src/index.css` returns ≥ 9
- [ ] Dev server `npm run dev` boots without console errors (manual / e2e in later plan)
</verification>

<success_criteria>
- Tailwind v4 installed and processing src/index.css via the Vite plugin
- @theme tokens declared per UI-SPEC
- App.css and its boilerplate fully removed
- Layout.jsx rebuilt as mobile-first shell with sticky header
- index.html has lang, viewport-fit, robots meta
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-01-SUMMARY.md`
</output>
