---
phase: 01-mobile-first-frontend-pass-tailwind-migration
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/pages/EventsPage.jsx
  - frontend/src/pages/EventDetailPage.jsx
  - frontend/src/pages/MySignupsPage.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/RegisterPage.jsx
  - frontend/src/pages/PortalPage.jsx
  - frontend/src/pages/NotFoundPage.jsx
  - frontend/src/pages/ProfilePage.jsx
  - frontend/src/App.jsx
  - frontend/src/components/Layout.jsx
autonomous: true
requirements:
  - 375px redesign on all pages
  - card-based event list
  - sticky filter chips
  - skeleton loaders
  - bottom-tab nav
  - one-tap signup flow
must_haves:
  truths:
    - "Events list shows cards with sticky filter chips at <md"
    - "Event detail signup flow completes in 3 taps with no navigation between taps"
    - "MySignups page lists upcoming and past with cancel action"
    - "Profile stub renders display name + email + logout"
    - "Authenticated student users see BottomNav at <md (Events / My Signups / Profile)"
    - "All forms use Input/Label/FieldError primitives with min-h-11"
  artifacts:
    - path: frontend/src/pages/EventsPage.jsx
      provides: "Card-based event list with sticky chip filter"
      contains: "Chip"
    - path: frontend/src/pages/EventDetailPage.jsx
      provides: "Event detail with one-tap signup modal flow"
      contains: "Modal"
    - path: frontend/src/pages/ProfilePage.jsx
      provides: "Profile stub for bottom nav target"
  key_links:
    - from: frontend/src/pages/EventDetailPage.jsx
      to: frontend/src/components/ui/Modal.jsx
      via: "Signup confirm modal"
      pattern: "Modal"
    - from: frontend/src/components/Layout.jsx
      to: frontend/src/components/ui/BottomNav.jsx
      via: "Layout renders BottomNav for authenticated students"
      pattern: "BottomNav"
---

<objective>
Redesign all student-facing pages mobile-first using the primitives from Plan 02. Build the new ProfilePage stub. Mount BottomNav in Layout for authenticated students. Implement the strict 3-tap signup flow on EventDetailPage.

Purpose: Delivers the user-visible mobile experience for the volunteer/student persona — the 375px-first redesign across the public + student-authenticated pages.
Output: 7 redesigned existing pages + 1 new ProfilePage + Layout BottomNav integration + route added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-CONTEXT.md
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md
@frontend/src/App.jsx
@frontend/src/components/Layout.jsx
@frontend/src/components/ui/index.js
@frontend/src/pages/EventsPage.jsx
@frontend/src/pages/EventDetailPage.jsx
@frontend/src/pages/MySignupsPage.jsx
@frontend/src/pages/LoginPage.jsx
@frontend/src/pages/RegisterPage.jsx
@frontend/src/pages/PortalPage.jsx
@frontend/src/pages/NotFoundPage.jsx
@frontend/src/state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign EventsPage with cards + sticky filter chips + skeletons</name>
  <files>frontend/src/pages/EventsPage.jsx</files>
  <read_first>frontend/src/pages/EventsPage.jsx, frontend/src/components/ui/index.js, .planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md</read_first>
  <action>Rewrite EventsPage. Concrete requirements:

- Use existing data hook (React Query call to `/events`). Preserve existing query keys and error handling — only swap markup.
- Render `<PageHeader title="Events" />` with `// TODO(copy)` next to the title string.
- Below header, sticky filter chip bar: `<div className="sticky top-[calc(var(--header-h))] z-10 -mx-4 px-4 py-2 bg-[var(--color-bg)]/90 backdrop-blur border-b border-[var(--color-border)]"><div className="flex gap-2 overflow-x-auto">{chips}</div></div>`.
- Three Chip components: filter values `'upcoming'` (default), `'this-week'`, `'mine'`. Labels are `// TODO(copy)` placeholders: "Upcoming", "This week", "My signups". Local state via `useState('upcoming')`.
- Filter the events array client-side based on the active chip:
  - upcoming: `event.startsAt >= now`
  - this-week: `event.startsAt >= now && event.startsAt <= now + 7 days`
  - mine: filter to events the current user has a signup for (use existing my-signups query OR pass `mine` flag — read existing code to find the right approach; if not available, hide chip behind a TODO).
- Loading state (`query.isPending`): render 5 `<Skeleton className="h-28 w-full rounded-xl" />` stacked.
- Error state: `<EmptyState title="..." body="..." action={<Button onClick={refetch}>Retry</Button>}>` with `// TODO(copy)` markers.
- Empty state (filtered list empty): `<EmptyState title="No events yet" ...>` with `// TODO(copy)` markers.
- Each event renders as a `<Card>` containing: event title (`<h2 className="text-lg font-semibold">`), date/time row (caption style), short blurb, and a `<Button as={Link} to={`/events/${event.id}`}>` View slots CTA. Card is wrapped in `Link` if simpler.
- Stack: `<div className="space-y-3">` (vertical card spacing).
- Use Link from react-router-dom.

Preserve any existing TanStack Query config exactly (queryKey, retry, etc.).</action>
  <verify>cd frontend && grep -q "Chip" src/pages/EventsPage.jsx && grep -q "Skeleton" src/pages/EventsPage.jsx && grep -q "PageHeader" src/pages/EventsPage.jsx && grep -q "sticky" src/pages/EventsPage.jsx && grep -q "TODO(copy)" src/pages/EventsPage.jsx</verify>
  <acceptance_criteria>
    - EventsPage.jsx imports Chip, Card, Skeleton, EmptyState, PageHeader, Button from components/ui
    - Contains `sticky top-[calc(var(--header-h))]`
    - Contains 3 Chip elements with values upcoming/this-week/mine
    - Renders `<Skeleton ... />` on isPending
    - Renders `<EmptyState ... />` on empty results
    - Contains at least 3 `TODO(copy)` markers
  </acceptance_criteria>
  <done>EventsPage is mobile-first card list with sticky chips and skeletons.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign EventDetailPage with strict 3-tap signup flow</name>
  <files>frontend/src/pages/EventDetailPage.jsx</files>
  <read_first>frontend/src/pages/EventDetailPage.jsx, frontend/src/components/ui/Modal.jsx, frontend/src/state/toast.js</read_first>
  <action>Rewrite EventDetailPage. Concrete requirements:

- Preserve existing data fetching (event detail query, slots, mutations) — only change markup.
- Header block: `<PageHeader title={event.title} subtitle={formattedDateRange} />`. `event.title` is dynamic (no copy TODO), but format/labels are.
- Event description in `<Card>`.
- Slot list as `<ul className="space-y-2">`. Each slot is a `<li>` containing slot time and a `<Button onClick={() => openSignupModal(slot)}>` with label `// TODO(copy)` "Sign up". If user already signed up for this slot, show `<Button variant="secondary" onClick={() => openCancelModal(slot)}>` with label "Cancel". The button is the entire interactive area; whole row min-h-14.
- State: `const [pendingSignup, setPendingSignup] = useState(null)` and `const [pendingCancel, setPendingCancel] = useState(null)`.
- Signup `<Modal open={!!pendingSignup} onClose={() => setPendingSignup(null)} title="Confirm signup">`:
  - Body: `<p>// TODO(copy): Confirm signup for {pendingSignup.timeLabel}?</p>`.
  - Footer flex with `<Button variant="ghost" onClick={...}>Not now</Button>` and `<Button onClick={confirmSignup}>Confirm signup</Button>`.
  - `confirmSignup`: calls existing signup mutation; on success: `setPendingSignup(null)`; `toast.success('TODO(copy): You're in. See you there.')`; refetch slots query.
  - On error: keeps modal open, shows inline `<FieldError>` with mutation error message; do NOT toast on error inside modal.
- Cancel `<Modal>` similar pattern using DELETE/cancel mutation, danger Button.
- **The 3-tap rule:** Tap 1 is the slot Button (no nav), Tap 2 is the modal Confirm Button, Tap 3 ... wait, re-read: tap slot → modal opens (tap 1), tap confirm in modal (tap 2), modal closes + toast (no tap 3 needed). The plan must achieve "≤ 3 taps". Verify with: ≤ 2 taps to confirmed state from EventDetailPage. Add a comment in the file: `// 3-tap rule: slot button (1) → confirm in modal (2) → done. No navigations between taps.`.

- Loading state: 3 `<Skeleton>` blocks (header, description, slot list).
- Error: `<EmptyState>` with retry.

Preserve all data hooks and mutation logic; only swap markup and add the modal flow.</action>
  <verify>cd frontend && grep -q "Modal" src/pages/EventDetailPage.jsx && grep -q "pendingSignup" src/pages/EventDetailPage.jsx && grep -q "toast.success" src/pages/EventDetailPage.jsx && grep -q "3-tap rule" src/pages/EventDetailPage.jsx</verify>
  <acceptance_criteria>
    - EventDetailPage.jsx imports Modal, Button, Card, FieldError, PageHeader from components/ui
    - Imports `toast` from `../state/toast`
    - Contains `Modal` with `open` prop tied to local state
    - Contains comment `// 3-tap rule:`
    - Contains `toast.success` call on successful signup
    - Contains at least 4 `TODO(copy)` markers
  </acceptance_criteria>
  <done>EventDetailPage implements the locked 3-tap signup flow.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign MySignupsPage with grouped list + cancel action</name>
  <files>frontend/src/pages/MySignupsPage.jsx</files>
  <read_first>frontend/src/pages/MySignupsPage.jsx, frontend/src/components/ui/index.js</read_first>
  <action>Rewrite MySignupsPage. Requirements:

- Preserve existing data hooks.
- `<PageHeader title="My Signups" />` (TODO(copy) marker).
- Group signups: `upcoming` (startsAt >= now) and `past` (startsAt < now). Render two sections with `<h2 className="text-sm font-medium text-[var(--color-fg-muted)] mt-6 mb-2 uppercase tracking-wide">Upcoming</h2>` etc. (TODO(copy) marker on the heading text).
- Each signup is a `<Card>` with: event title (Link to `/events/${id}`), slot time, and (for upcoming only) a `<Button variant="ghost" size="md" onClick={() => setPendingCancel(signup)}>` "Cancel".
- Cancel confirmation `<Modal>` with danger button — same pattern as EventDetailPage cancel.
- On cancel success: `toast.success('TODO(copy): Signup canceled.')` + refetch.
- Loading: skeletons. Empty: `<EmptyState title="You haven't signed up for anything" body="..." action={<Button as={Link} to="/events">Browse events</Button>}>`.</action>
  <verify>cd frontend && grep -q "PageHeader" src/pages/MySignupsPage.jsx && grep -q "Modal" src/pages/MySignupsPage.jsx && grep -q "EmptyState" src/pages/MySignupsPage.jsx</verify>
  <acceptance_criteria>
    - MySignupsPage.jsx imports PageHeader, Card, Modal, Button, EmptyState, Skeleton
    - Contains grouping by upcoming/past
    - Contains `toast.success` call on cancel
    - Contains at least 3 `TODO(copy)` markers
  </acceptance_criteria>
  <done>MySignupsPage redesigned with cancel modal and empty state.</done>
</task>

<task type="auto">
  <name>Task 4: Redesign LoginPage and RegisterPage forms</name>
  <files>frontend/src/pages/LoginPage.jsx, frontend/src/pages/RegisterPage.jsx</files>
  <read_first>frontend/src/pages/LoginPage.jsx, frontend/src/pages/RegisterPage.jsx, frontend/src/components/ui/index.js</read_first>
  <action>Rewrite both forms. Requirements (apply to each):

- Single column, centered: `<div className="mx-auto max-w-sm w-full pt-8">`.
- `<PageHeader title="Sign in" />` / `"Create account"` with TODO(copy).
- `<form className="space-y-4">` containing Label/Input pairs and FieldError per field. Every Input is full-width, min-h-11 (already from primitive).
- Submit button: `<Button type="submit" className="w-full" size="lg">` with TODO(copy) label.
- Below the form, secondary link (Login → Register and vice versa) using `<Button variant="ghost" as={Link}>` with TODO(copy).
- Preserve existing form validation, mutation logic, and error display — only swap markup and class names.
- On successful login/register, preserve the existing redirect logic.</action>
  <verify>cd frontend && grep -q "Label" src/pages/LoginPage.jsx && grep -q "Input" src/pages/LoginPage.jsx && grep -q "Label" src/pages/RegisterPage.jsx && grep -q "Input" src/pages/RegisterPage.jsx</verify>
  <acceptance_criteria>
    - LoginPage.jsx imports Label, Input, Button, FieldError, PageHeader
    - RegisterPage.jsx imports Label, Input, Button, FieldError, PageHeader
    - Both contain `<form` element
    - Both contain `TODO(copy)` markers
    - Both submit Button has `size="lg"` and `w-full`
  </acceptance_criteria>
  <done>Login and Register forms redesigned mobile-first with proper labels.</done>
</task>

<task type="auto">
  <name>Task 5: Create ProfilePage stub</name>
  <files>frontend/src/pages/ProfilePage.jsx, frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, frontend/src/state, frontend/src/components/ui/index.js</read_first>
  <action>Create frontend/src/pages/ProfilePage.jsx as a minimal stub:

- `<PageHeader title="Profile" />` with TODO(copy).
- `<Card>` showing display name + email read from existing user/auth state (find the existing state slice used by Layout.jsx — read it first).
- A row labeled "Display name" / "Email" using Label primitive on the labels and plain `<p>` for values.
- `<Button variant="secondary" as={Link} to="/profile/change-password">` with TODO(copy) "Change password" — link target is `/profile/change-password` which does NOT exist; add a `// TODO(copy): change-password route lands in a future phase` comment.
- `<Button variant="danger" onClick={logout}>` with TODO(copy) "Log out" — wire to existing logout flow used in Layout.jsx.

Then in frontend/src/App.jsx, add a route for `/profile` that renders `<ProfilePage />` inside the Layout (protected — students must be logged in). Match the style of existing protected routes; do NOT change router structure.</action>
  <verify>cd frontend && test -f src/pages/ProfilePage.jsx && grep -q "ProfilePage" src/App.jsx && grep -q '/profile' src/App.jsx</verify>
  <acceptance_criteria>
    - frontend/src/pages/ProfilePage.jsx exists
    - ProfilePage.jsx renders user display name and email
    - ProfilePage.jsx has logout Button
    - frontend/src/App.jsx contains a route for `/profile`
    - frontend/src/App.jsx imports `ProfilePage`
    - Profile page contains at least 3 `TODO(copy)` markers
  </acceptance_criteria>
  <done>Profile stub exists and is routed.</done>
</task>

<task type="auto">
  <name>Task 6: Redesign PortalPage and NotFoundPage</name>
  <files>frontend/src/pages/PortalPage.jsx, frontend/src/pages/NotFoundPage.jsx</files>
  <read_first>frontend/src/pages/PortalPage.jsx, frontend/src/pages/NotFoundPage.jsx</read_first>
  <action>PortalPage: Use `<PageHeader title="..." subtitle="...">` with TODO(copy), `<Card>` with hero copy + primary `<Button as={Link} to="/events" size="lg">` "Browse events".

NotFoundPage: Use `<EmptyState title="Page not found" body="..." action={<Button as={Link} to="/">Go home</Button>}>` with TODO(copy) markers.</action>
  <verify>cd frontend && grep -q "PageHeader\|EmptyState" src/pages/PortalPage.jsx && grep -q "EmptyState" src/pages/NotFoundPage.jsx</verify>
  <acceptance_criteria>
    - PortalPage.jsx uses PageHeader or EmptyState + Button primitives
    - NotFoundPage.jsx uses EmptyState primitive
    - Both contain TODO(copy) markers
  </acceptance_criteria>
  <done>Landing and 404 pages redesigned.</done>
</task>

<task type="auto">
  <name>Task 7: Mount BottomNav in Layout for authenticated students</name>
  <files>frontend/src/components/Layout.jsx</files>
  <read_first>frontend/src/components/Layout.jsx, frontend/src/components/ui/BottomNav.jsx, frontend/src/state</read_first>
  <action>Update Layout.jsx to render `<BottomNav>` only when:
1. There is an authenticated user (read existing user state).
2. The user role is `student` (NOT organizer / admin — those get a different nav in Plan 04).

Pass items array (with TODO(copy) labels):
```js
const studentNavItems = [
  { to: '/events', label: 'Events', icon: <EventsIcon className="h-5 w-5" /> },        // TODO(copy)
  { to: '/my-signups', label: 'My Signups', icon: <SignupsIcon className="h-5 w-5" /> }, // TODO(copy)
  { to: '/profile', label: 'Profile', icon: <ProfileIcon className="h-5 w-5" /> },      // TODO(copy)
]
```

Use `lucide-react` icons (Calendar, ListChecks, User). If `lucide-react` is not installed, install it: `cd frontend && npm install lucide-react`.

Render `<BottomNav items={studentNavItems} />` directly above `<ToastHost />`. Ensure it's only rendered when the conditions above are met. Wrap in fragment if needed.</action>
  <verify>cd frontend && grep -q "BottomNav" src/components/Layout.jsx && grep -q "lucide-react" src/components/Layout.jsx && node -e "const p=require('./package.json'); if(!p.dependencies['lucide-react']) process.exit(1)"</verify>
  <acceptance_criteria>
    - frontend/package.json dependencies contains `lucide-react`
    - Layout.jsx imports `BottomNav`
    - Layout.jsx imports icons from `lucide-react`
    - Layout.jsx renders `<BottomNav>` conditionally on authenticated student role
    - Layout.jsx contains at least 3 `TODO(copy)` markers near nav labels
  </acceptance_criteria>
  <done>Students see bottom nav at <md.</done>
</task>

<task type="auto">
  <name>Task 8: Verify build</name>
  <files></files>
  <action>Run `npm run build` in frontend/. Fix import errors. Ensure no orphan imports of removed CSS classes.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All redesigned pages compile.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `grep -rn "TODO(copy)" frontend/src/pages | wc -l` returns ≥ 15
- [ ] `grep -rn "Modal" frontend/src/pages/EventDetailPage.jsx` shows modal usage
- [ ] EventDetailPage has the `3-tap rule` comment
</verification>

<success_criteria>
- All 7 student-facing pages redesigned with primitives
- ProfilePage stub created and routed
- BottomNav mounted for authenticated students
- 3-tap signup flow in place via Modal on EventDetailPage
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-03-SUMMARY.md`
</output>
