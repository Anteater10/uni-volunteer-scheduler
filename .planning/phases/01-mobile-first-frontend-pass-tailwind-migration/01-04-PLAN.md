---
phase: 01-mobile-first-frontend-pass-tailwind-migration
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/pages/OrganizerDashboardPage.jsx
  - frontend/src/pages/OrganizerEventPage.jsx
  - frontend/src/pages/AdminDashboardPage.jsx
  - frontend/src/pages/AdminEventPage.jsx
  - frontend/src/pages/UsersAdminPage.jsx
  - frontend/src/pages/PortalsAdminPage.jsx
  - frontend/src/pages/AuditLogsPage.jsx
  - frontend/src/pages/NotificationsPage.jsx
  - frontend/src/components/Layout.jsx
autonomous: true
requirements:
  - 375px redesign on all pages
  - bottom-tab nav
must_haves:
  truths:
    - "Organizer roster page rows are one-tap (whole row min-h-14, single tap toggles check-in)"
    - "All organizer + admin tables collapse to stacked Card lists at <md"
    - "Authenticated organizer/admin users see BottomNav at <md (Events / Roster / Admin)"
    - "All admin CRUD actions use Modal primitive for confirmations"
  artifacts:
    - path: frontend/src/pages/OrganizerEventPage.jsx
      provides: "Roster with one-tap check-in rows"
      contains: "min-h-14"
    - path: frontend/src/pages/AdminDashboardPage.jsx
      provides: "Mobile-first admin dashboard"
  key_links:
    - from: frontend/src/pages/OrganizerEventPage.jsx
      to: frontend/src/state/toast.js
      via: "Toast on check-in success"
      pattern: "toast\\."
---

<objective>
Redesign all organizer + admin pages mobile-first using the primitives. Convert tables to stacked Card lists at <md. Make organizer roster rows one-tap to check in. Switch BottomNav for organizer/admin roles.

Purpose: Mirror the student redesign for the staff personas. Without this plan, organizers and admins cannot use the app on mobile.
Output: 8 redesigned organizer/admin pages + Layout role-based BottomNav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-CONTEXT.md
@.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md
@frontend/src/components/Layout.jsx
@frontend/src/components/ui/index.js
@frontend/src/pages/OrganizerDashboardPage.jsx
@frontend/src/pages/OrganizerEventPage.jsx
@frontend/src/pages/AdminDashboardPage.jsx
@frontend/src/pages/AdminEventPage.jsx
@frontend/src/pages/UsersAdminPage.jsx
@frontend/src/pages/PortalsAdminPage.jsx
@frontend/src/pages/AuditLogsPage.jsx
@frontend/src/pages/NotificationsPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign OrganizerEventPage roster with one-tap check-in rows</name>
  <files>frontend/src/pages/OrganizerEventPage.jsx</files>
  <read_first>frontend/src/pages/OrganizerEventPage.jsx, .planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-UI-SPEC.md, frontend/src/components/ui/index.js</read_first>
  <action>Rewrite OrganizerEventPage. Concrete requirements:

- `<PageHeader title={event.title} subtitle="Roster" />` with TODO(copy) on subtitle.
- Roster as `<ul className="divide-y divide-[var(--color-border)]">`.
- Each row is a `<li><button type="button" onClick={() => toggleCheckIn(signup)} className="flex w-full min-h-14 items-center justify-between gap-3 px-2 py-3 text-left hover:bg-[var(--color-surface)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-brand)]"><div><p className="font-medium">{signup.user.name}</p><p className="text-sm text-[var(--color-fg-muted)]">{slot.timeLabel}</p></div><CheckInPill checkedIn={signup.checkedIn} /></button></li>`.
- The `<button>` is the entire interactive area — the whole row is the tap target. NO separate checkbox; tapping anywhere in the row toggles check-in.
- `CheckInPill` is a local component: `function CheckInPill({checkedIn}) { return <span className={cn('inline-flex min-h-6 items-center rounded-full px-3 text-xs font-medium', checkedIn ? 'bg-[var(--color-success)] text-white' : 'bg-[var(--color-surface)] text-[var(--color-fg-muted)] border border-[var(--color-border)]')}>{checkedIn ? 'Checked in' : 'Not yet'}</span> }` — TODO(copy) on both labels.
- `toggleCheckIn` calls existing PATCH mutation (read existing code for the right hook). Use optimistic update via React Query mutation `onMutate` / `onError` rollback. On success: `toast.success('TODO(copy): Checked in.')` (or "Undid check-in" on toggle off).
- Loading skeleton: 6 row skeletons. Empty: `<EmptyState>` "No signups yet" with TODO(copy).
- Preserve any existing event-detail header data (capacity, time, location).</action>
  <verify>cd frontend && grep -q "min-h-14" src/pages/OrganizerEventPage.jsx && grep -q "toggleCheckIn\|onClick" src/pages/OrganizerEventPage.jsx && grep -q "PageHeader" src/pages/OrganizerEventPage.jsx && grep -q "toast" src/pages/OrganizerEventPage.jsx</verify>
  <acceptance_criteria>
    - OrganizerEventPage.jsx contains `min-h-14` on the row button
    - The row is a `<button>` element (whole row is interactive)
    - No `<input type="checkbox">` for check-in (it's a button toggle)
    - Imports `toast` from `../state/toast`
    - Contains at least 3 `TODO(copy)` markers
    - Imports PageHeader, EmptyState, Skeleton primitives
  </acceptance_criteria>
  <done>Roster page is one-tap-per-row check-in.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign OrganizerDashboardPage as card list</name>
  <files>frontend/src/pages/OrganizerDashboardPage.jsx</files>
  <read_first>frontend/src/pages/OrganizerDashboardPage.jsx, frontend/src/components/ui/index.js</read_first>
  <action>Rewrite. `<PageHeader title="My Events" />` (TODO(copy)). Each event renders as `<Card>` with title, date, signup count, and a `<Button as={Link} to={`/organizer/events/${event.id}`} variant="secondary">` "Open roster" (TODO(copy)). Skeleton list on loading. EmptyState with TODO(copy) when none.</action>
  <verify>cd frontend && grep -q "Card" src/pages/OrganizerDashboardPage.jsx && grep -q "PageHeader" src/pages/OrganizerDashboardPage.jsx && grep -q "TODO(copy)" src/pages/OrganizerDashboardPage.jsx</verify>
  <acceptance_criteria>
    - Imports Card, PageHeader, Button, Skeleton, EmptyState
    - Contains at least 2 `TODO(copy)` markers
  </acceptance_criteria>
  <done>Organizer dashboard is mobile-first.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign AdminDashboardPage and AdminEventPage</name>
  <files>frontend/src/pages/AdminDashboardPage.jsx, frontend/src/pages/AdminEventPage.jsx</files>
  <read_first>frontend/src/pages/AdminDashboardPage.jsx, frontend/src/pages/AdminEventPage.jsx</read_first>
  <action>Both: PageHeader + Card-based layouts. AdminDashboardPage shows links/cards to user mgmt, portal mgmt, audit logs, etc. AdminEventPage shows event CRUD form (Label/Input/FieldError + Button submit, mobile-first). All destructive actions (delete event, etc.) go through `<Modal>` confirmation with danger Button. Preserve existing data hooks. Add TODO(copy) markers on every user-visible string.</action>
  <verify>cd frontend && grep -q "PageHeader" src/pages/AdminDashboardPage.jsx && grep -q "PageHeader" src/pages/AdminEventPage.jsx && grep -q "Modal" src/pages/AdminEventPage.jsx</verify>
  <acceptance_criteria>
    - AdminDashboardPage.jsx imports PageHeader, Card, Button
    - AdminEventPage.jsx imports PageHeader, Label, Input, FieldError, Button, Modal
    - Both contain `TODO(copy)` markers
  </acceptance_criteria>
  <done>Admin dashboard + event editor mobile-first with modal confirmations.</done>
</task>

<task type="auto">
  <name>Task 4: Redesign UsersAdminPage and PortalsAdminPage as stacked card lists</name>
  <files>frontend/src/pages/UsersAdminPage.jsx, frontend/src/pages/PortalsAdminPage.jsx</files>
  <read_first>frontend/src/pages/UsersAdminPage.jsx, frontend/src/pages/PortalsAdminPage.jsx</read_first>
  <action>Both currently render tables. At `<md`, replace `<table>` with a stacked `<ul>` of `<Card>` items. At `md+`, you may keep a `<table>` (`hidden md:table`) — but the easy/safe path is "Cards everywhere" since they read fine on desktop too. **Pick: cards everywhere.** Each card has: identifying field (name/email or portal name) as title, role/status as caption, and action buttons (edit/disable/delete). Destructive actions through `<Modal>` confirmation. Add TODO(copy) on labels/buttons.</action>
  <verify>cd frontend && grep -q "Card" src/pages/UsersAdminPage.jsx && grep -q "Card" src/pages/PortalsAdminPage.jsx && grep -q "Modal" src/pages/UsersAdminPage.jsx</verify>
  <acceptance_criteria>
    - UsersAdminPage.jsx imports Card, Modal, Button, PageHeader
    - PortalsAdminPage.jsx imports Card, Modal, Button, PageHeader
    - Neither file contains `<table>` tags (cards everywhere)
    - Both contain TODO(copy) markers
  </acceptance_criteria>
  <done>User and portal admin pages mobile-first with confirmations.</done>
</task>

<task type="auto">
  <name>Task 5: Redesign AuditLogsPage and NotificationsPage</name>
  <files>frontend/src/pages/AuditLogsPage.jsx, frontend/src/pages/NotificationsPage.jsx</files>
  <read_first>frontend/src/pages/AuditLogsPage.jsx, frontend/src/pages/NotificationsPage.jsx</read_first>
  <action>Both: PageHeader + scrollable list of `<Card>` items (or simple `<li>` rows in a `<ul className="divide-y">`). Audit log entries are read-only — show action, actor, timestamp, target. Notifications page lists notifications with read/unread state. Preserve existing data hooks. TODO(copy) on labels.</action>
  <verify>cd frontend && grep -q "PageHeader" src/pages/AuditLogsPage.jsx && grep -q "PageHeader" src/pages/NotificationsPage.jsx</verify>
  <acceptance_criteria>
    - Both files import PageHeader and at least one of (Card, EmptyState, Skeleton)
    - Both contain TODO(copy) markers
    - Neither file contains `<table>` tags
  </acceptance_criteria>
  <done>Audit logs and notifications mobile-first.</done>
</task>

<task type="auto">
  <name>Task 6: Update Layout to switch BottomNav by role</name>
  <files>frontend/src/components/Layout.jsx</files>
  <read_first>frontend/src/components/Layout.jsx</read_first>
  <action>Update Layout.jsx so the BottomNav items array is selected by role:

- student → Events / My Signups / Profile (already from Plan 03).
- organizer → Events (`/organizer`) / Roster (current event roster — link to `/organizer` if no current event) / Admin if also admin else Profile.
- admin → Events (`/events`) / Users (`/admin/users`) / Admin (`/admin`).

Define each items array as a const at top of file. Pick the one to render based on user role. Use `lucide-react` icons (Calendar, Users, Shield, ListChecks, User). Add TODO(copy) markers on every label.

Guests / logged-out: no BottomNav (already from Plan 01/03 conditional).</action>
  <verify>cd frontend && grep -q "organizerNavItems\|adminNavItems\|role" src/components/Layout.jsx && grep -c "TODO(copy)" src/components/Layout.jsx</verify>
  <acceptance_criteria>
    - Layout.jsx defines distinct nav items arrays per role
    - Layout.jsx selects nav items based on user role
    - Layout.jsx contains at least 6 `TODO(copy)` markers (3 student + 3 organizer/admin labels)
  </acceptance_criteria>
  <done>BottomNav switches by role.</done>
</task>

<task type="auto">
  <name>Task 7: Verify build</name>
  <files></files>
  <action>Run `npm run build` in frontend/. Fix import errors.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All organizer + admin pages compile.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `grep -rn '<table' frontend/src/pages` returns zero
- [ ] OrganizerEventPage has `min-h-14` on the row button
- [ ] Layout.jsx has role-based nav items
</verification>

<success_criteria>
- Organizer + admin pages all redesigned mobile-first
- Roster is one-tap per row
- All tables converted to card lists
- BottomNav switches based on role
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-first-frontend-pass-tailwind-migration/01-04-SUMMARY.md`
</output>
