---
phase: 14-collaboration-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COLLABORATION.md
autonomous: true
requirements:
  - COLLAB-01
  - COLLAB-02
  - COLLAB-03
  - COLLAB-04
  - COLLAB-07
must_haves:
  truths:
    - "docs/COLLABORATION.md exists at repo root and is the single source of truth for v1.2-prod parallel work between Andy and Hung"
    - "The doc describes the actual implementation (single checkout per dev, branch switching) and explicitly reconciles it with the 'git-worktree' wording in REQUIREMENTS COLLAB-01 (D-06, D-07, D-09)"
    - "The doc lists Andy as owner of admin pillar (Phases 16, 17, 18) AND organizer pillar (Phase 19), and Hung as owner of participant pillar (Phase 15) (D-02)"
    - "The doc names the three long-lived role branches verbatim: feature/v1.2-participant, feature/v1.2-admin, feature/v1.2-organizer"
    - "The doc encodes the PR-only file list from D-12 with at least the canonical entries (api.js, App.jsx, models.py, alembic/versions, STATE.md, ROADMAP.md, CLAUDE.md, COLLABORATION.md, docker-compose.yml, .github/workflows)"
    - "The doc states the daily 3-hour pair/sync cadence (D-14) and the merge-after-each-phase-ships-green rule (D-15)"
    - "The doc states the tie-breaker rule: pillar-most-affected wins, Andy holds casting vote (D-16)"
    - "The doc explains 'Hung's frontend patterns ship first, Andy refactors them into admin/organizer' so downstream readers don't assume both pillars race independently (D-03, D-04)"
    - "The doc preserves the docker-network test pattern reference (still non-negotiable for both devs)"
  artifacts:
    - path: "docs/COLLABORATION.md"
      provides: "v1.2-prod collaboration contract for Andy + Hung parallel work"
      contains: "feature/v1.2-participant"
  key_links:
    - from: "docs/COLLABORATION.md"
      to: ".planning/REQUIREMENTS-v1.2-prod.md"
      via: "explicit reference by filename (NOT REQUIREMENTS-v1.2.md)"
      pattern: "REQUIREMENTS-v1.2-prod\\.md"
    - from: "docs/COLLABORATION.md"
      to: "CLAUDE.md"
      via: "cross-reference to docker-network test pattern + Alembic single-writer rule"
      pattern: "CLAUDE\\.md"
---

<objective>
Wholesale rewrite of `docs/COLLABORATION.md` for the v1.2-prod milestone. Replace the existing stale file (which uses Andy=backend / Hung=frontend split, references `REQUIREMENTS-v1.2.md`, mentions waitlist/iCal features, and treats Phase 14 as UCSB infra work) with a new file that encodes all 18 locked decisions from `14-CONTEXT.md`.

Purpose: This file is the joint contract between Andy and Hung for running Claude Code + GSD on the same repo in parallel without colliding. It is also read by both Claude Code sessions, so it must be unambiguous about file ownership, role pillars, and the merge cadence.

Output: One commit replacing the entire file. No archive copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v1.2-prod.md
@.planning/phases/14-collaboration-setup/14-CONTEXT.md
@docs/COLLABORATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale rewrite of docs/COLLABORATION.md</name>
  <files>docs/COLLABORATION.md</files>
  <read_first>
    - docs/COLLABORATION.md (the stale file being REPLACED — read it so you can adapt the section skeleton but discard all content; do not preserve any references to Andy=backend/Hung=frontend split, waitlist/iCal features, REQUIREMENTS-v1.2.md, Hetzner Phase 14 infra, or v1.2-prod-deploy milestone)
    - .planning/phases/14-collaboration-setup/14-CONTEXT.md (the source of truth for D-01..D-18 — every decision must be encoded somewhere in the rewritten file)
    - .planning/REQUIREMENTS-v1.2-prod.md (note the EXACT filename — `-v1.2-prod.md`, not `-v1.2.md`. The rewritten doc must reference this filename, not the old one)
    - .planning/ROADMAP.md (Phase 14 success criteria + the 7 phases 14-20 layout)
    - CLAUDE.md (preserve cross-references to docker-network test pattern and Alembic conventions)
  </read_first>
  <action>
Replace the entire contents of `docs/COLLABORATION.md` with a new document that encodes all 18 locked decisions from CONTEXT.md (D-01 through D-18). Use the existing file's section skeleton as a structural starting point — Roles / Hard ownership / GSD multi-dev workflow / PR & merge / Collision prevention / Cadence / Risks / Critical files reference — but discard ALL existing content.

The new file MUST include the following sections and content (table-vs-list layout is at your discretion as long as every required string below appears verbatim somewhere in the file):

1. **Header / status block** — Title `# Collaboration Contract`, one-line summary that the doc is for v1.2-prod (production-ready by role), explicit note that this is a wholesale rewrite from a prior pass and reflects the v1.2-prod role-pillar split (NOT the older backend/frontend split). Reference `.planning/REQUIREMENTS-v1.2-prod.md` by exact filename.

2. **Role assignment (D-02, D-03, D-04, D-05)** — State plainly:
   - Andy owns the **admin pillar** (Phases 16, 17, 18) AND the **organizer pillar** (Phase 19).
   - Hung owns the **participant pillar** (Phase 15).
   - Integration (Phase 20) is shared.
   - Hung is "frontend-leaning"; his participant patterns ship first and Andy refactors them into admin/organizer with role-appropriate deviations. Phases 15 and 16 START in parallel but Andy's UI polish for admin intentionally lags Hung's participant polish.
   - Use the exact strings `participant`, `admin`, `organizer` for the three pillar names so future readers (and Claude) can grep for them.

3. **Machine model and worktree explanation (D-01, D-06, D-07, D-08, D-09)** — Explain that:
   - Hung is a real second human on his own machine with his own clone of the repo. Coordination is via push/pull/PR against shared `main`. There is NO shared filesystem.
   - Each dev uses **single checkout, branch switching** on their own machine — NOT literal `git worktree add`. The phrase "single checkout" or "branch switching" must appear verbatim in this section.
   - The REQUIREMENTS COLLAB-01 wording uses "git-worktree" loosely. The *spirit* of the requirement is "role-owned long-lived branches enabling parallel work"; the *implementation* is one clone per dev with `git checkout` to switch pillars. Reconcile the two explicitly so downstream readers don't go looking for fake `git worktree add` instructions.
   - One docker stack at a time per machine. Container names and ports (5173, 8000) are hardcoded in `docker-compose.yml`, and DB/Redis are not exposed to localhost — both devs run pytest via the docker-network pattern documented in CLAUDE.md.

4. **The three long-lived role branches** — Name them verbatim:
   - `feature/v1.2-participant` (Hung)
   - `feature/v1.2-admin` (Andy)
   - `feature/v1.2-organizer` (Andy)
   Explain that they are created from `main` once, live for the entire v1.2-prod milestone, and merge back to `main` only after each phase ships green (D-15).

5. **File ownership rules (D-10, D-11, D-12, D-13)** — Two clearly distinct lists:
   - **PR-only files (cross-cutting / operationally risky — both devs must approve)** — must include AT LEAST these entries verbatim:
     - `frontend/src/lib/api.js`
     - `frontend/src/lib/api.public.js`
     - `frontend/src/App.jsx`
     - `frontend/src/components/ui/*`
     - `backend/app/models.py`
     - `backend/alembic/versions/*`
     - `.planning/STATE.md`
     - `.planning/ROADMAP.md`
     - `.planning/REQUIREMENTS-v1.2-prod.md`
     - `CLAUDE.md`
     - `docs/COLLABORATION.md`
     - `docker-compose.yml`, `backend/Dockerfile`, `frontend/Dockerfile`
     - `.github/workflows/*`
   - **Pillar-direct files (each dev edits freely on their own role branch)** — describe by domain rather than enumerating every path. List Hung's participant domain (`frontend/src/pages/public/*`, public-facing routers like `portals.py`, `events.py`, `signups.py` for read paths) and Andy's admin/organizer domain (`frontend/src/pages/admin/*`, `frontend/src/pages/organizer/*`, `backend/app/routers/admin.py`, `backend/app/routers/organizer.py`, etc.).
   - State explicitly: pillar-domain ownership is **high trust** — each dev edits any file they need on their own role branch. Resist formalizing this into a 30-row matrix; the PR-only list above is the only enforced wall.
   - **Hard rule that overrides high trust:** Andy is the single Alembic writer. `backend/alembic/versions/*` is Andy-only regardless of D-11; multi-head migrations are operationally too painful to recover from.

6. **Sync cadence (D-14)** — State the **daily 3-hour pair/sync session** between Andy and Hung as the planned cadence. Use the exact phrase "daily 3-hour" or "3 hour sync" so it is greppable. Note that this is unusual but explicitly chosen, and that "we expect this to relax to lighter cadence once the milestone is in flight" so future-Andy doesn't feel locked in.

7. **Merge cadence (D-15)** — Role branches merge to `main` after each phase ships green. The gates are: phase success criteria pass + Playwright suite green + code review done. NO mid-phase merges to main.

8. **GSD harness command flow** — Reference the GSD commands each dev uses per phase: `/gsd-progress` (daily start), `/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-verify-work`, `/gsd-code-review`, `/gsd-pr-branch`, `/gsd-ship`, `/gsd-pause-work`, `/gsd-resume-work`. Note that these are documented in the GSD harness — this file references them, doesn't redefine them. State that `.planning/STATE.md` is PR-only (D-12) and changes flow through Andy via `/gsd-progress` rather than direct edits from either dev's role branch.

9. **Conflict-resolution playbook + tie-breaker (D-16) (COLLAB-07)** — Cover:
   - **Tie-breaker rule (D-16):** the dev whose pillar is being most affected by a shared-file change wins the call. If a `frontend/src/lib/api.js` change is for participant work, Hung decides; for admin or organizer work, Andy decides. Tied still tied? Andy holds the casting vote as project owner. The phrase "casting vote" or equivalent must appear.
   - **Rebase-on-main rule:** when shared files change on main, role branches rebase rather than merge to keep history clean. Don't let a role branch drift more than ~24h behind main during the parallel window.
   - **Daily-sync use:** disagreements that aren't tie-breaker calls go to the daily 3-hour sync, not to async commit-watching.

10. **Critical files reference table** — A short table of the most important files for both devs to know about, including this file, CLAUDE.md, ROADMAP.md, STATE.md, REQUIREMENTS-v1.2-prod.md, the three role-branch names, and `docker-compose.yml`. Reference CLAUDE.md by exact path so the cross-link is greppable.

The file should be **short and scannable**. Aim for roughly 200-400 lines of markdown. Do NOT include:
- Anything about Hetzner / autonomous remote box (drop the existing file's section entirely).
- Anything about waitlist / iCal / bulk signup / organizer analytics (those are old v1.2-prod-deploy scope, not v1.2-prod).
- A "Joint scoping call" section with v1.2 feature picking — that decision is already locked in PROJECT.md and REQUIREMENTS-v1.2-prod.md.
- Any reference to the filename `REQUIREMENTS-v1.2.md` (only `REQUIREMENTS-v1.2-prod.md`).
- Any sentence that frames Andy as backend-only or Hung as frontend-only.
- A `docs/weeknotes.md` requirement (the deferred ideas note says it's optional, not a Phase 14 deliverable — mention it at most as "optional, append-only if anyone wants to start one").
- Per-worktree docker stacks / `COMPOSE_PROJECT_NAME` overrides / multi-port setups (deferred).

Write the file using the `Write` tool to overwrite the existing path entirely.
  </action>
  <verify>
    <automated>test -f docs/COLLABORATION.md && wc -l docs/COLLABORATION.md | awk '{ if ($1 < 80 || $1 > 600) { print "LINE_COUNT_OUT_OF_RANGE " $1; exit 1 } else { print "OK " $1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/COLLABORATION.md` returns 0 (file exists)
    - `wc -l docs/COLLABORATION.md` shows between 80 and 600 lines (catches "didn't write enough" and "wrote too much")
    - `grep -q "feature/v1.2-participant" docs/COLLABORATION.md` returns 0
    - `grep -q "feature/v1.2-admin" docs/COLLABORATION.md` returns 0
    - `grep -q "feature/v1.2-organizer" docs/COLLABORATION.md` returns 0
    - `grep -q "Hung" docs/COLLABORATION.md` returns 0
    - `grep -q "Andy" docs/COLLABORATION.md` returns 0
    - `grep -q "participant" docs/COLLABORATION.md` returns 0
    - `grep -q "admin" docs/COLLABORATION.md` returns 0
    - `grep -q "organizer" docs/COLLABORATION.md` returns 0
    - `grep -E -q "single checkout|branch switching" docs/COLLABORATION.md` returns 0 (D-06 implementation reconciliation)
    - `grep -E -q "daily 3-hour|3 hour sync|3-hour" docs/COLLABORATION.md` returns 0 (D-14)
    - `grep -q "REQUIREMENTS-v1.2-prod.md" docs/COLLABORATION.md` returns 0 (correct filename)
    - `! grep -q "REQUIREMENTS-v1.2\.md" docs/COLLABORATION.md` (the wrong filename does NOT appear; only the `-prod.md` variant) — note the regex escapes the dot
    - `grep -q "frontend/src/lib/api.js" docs/COLLABORATION.md` returns 0 (PR-only file list, D-12)
    - `grep -q "backend/alembic/versions" docs/COLLABORATION.md` returns 0 (Andy single-writer rule)
    - `grep -q "backend/app/models.py" docs/COLLABORATION.md` returns 0 (D-12)
    - `grep -q "casting vote" docs/COLLABORATION.md` returns 0 (D-16 tie-breaker)
    - `grep -q "ships green" docs/COLLABORATION.md || grep -q "phase ships green" docs/COLLABORATION.md` returns 0 (D-15 merge cadence)
    - `! grep -q "Hetzner" docs/COLLABORATION.md` (Hetzner section dropped per scope)
    - `! grep -q "waitlist" docs/COLLABORATION.md` (old v1.2 scope dropped)
    - `! grep -q "iCal" docs/COLLABORATION.md` (old v1.2 scope dropped)
    - `! grep -q "v1.2-prod-deploy" docs/COLLABORATION.md` (old milestone name dropped)
    - `! grep -i -q "Andy.*backend.*Hung.*frontend" docs/COLLABORATION.md` (do not preserve the wrong role split)
    - The file is committed to git as part of the execute-phase commit cadence
  </acceptance_criteria>
  <done>docs/COLLABORATION.md is fully replaced with a v1.2-prod-correct collaboration contract that encodes D-01..D-18, names the three role branches, lists PR-only files, states the daily 3-hour sync and tie-breaker rules, and contains no stale references to the prior file's content.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-decision verification of D-01..D-18 coverage</name>
  <files>docs/COLLABORATION.md</files>
  <read_first>
    - docs/COLLABORATION.md (the file just rewritten in Task 1)
    - .planning/phases/14-collaboration-setup/14-CONTEXT.md (the D-01..D-18 source list)
  </read_first>
  <action>
Run a structured cross-check that every locked decision from CONTEXT.md is observably encoded in the rewritten `docs/COLLABORATION.md`. For each decision below, run the indicated grep and confirm it returns content (or runs cleanly for negative checks). If ANY check fails, edit `docs/COLLABORATION.md` to add the missing content, then re-run all checks. Do not declare done until every check passes.

Decision-by-decision verification:

- **D-01** (Hung is a real second human on his own machine): `grep -E -i "real (second )?human|own machine|his own clone" docs/COLLABORATION.md`
- **D-02** (Andy = admin + organizer; Hung = participant): `grep -i "Andy.*admin.*organizer" docs/COLLABORATION.md` AND `grep -i "Hung.*participant" docs/COLLABORATION.md`
- **D-03** (Hung is frontend-leaning, patterns ship first): `grep -E -i "frontend.lean|patterns ship first|refactor" docs/COLLABORATION.md`
- **D-04** (Phases 15 and 16 start in parallel; Andy's UI polish lags): `grep -E -i "parallel|lag|starts? in parallel" docs/COLLABORATION.md`
- **D-05** (admin↔organizer is intra-Andy not inter-dev): documented in role assignment section — confirm the doc says Andy owns BOTH pillars; reuse D-02 grep
- **D-06** (single checkout + branch switching, NOT literal git worktree add): `grep -E -i "single checkout|branch switching" docs/COLLABORATION.md` AND `! grep -q "git worktree add" docs/COLLABORATION.md`
- **D-07** (Hung uses same model on his machine): covered by D-01/D-06; confirm there is no instruction to use `git worktree add`
- **D-08** (one docker stack at a time, hardcoded ports 5173/8000): `grep -E "5173|8000|one docker stack|one stack at a time" docs/COLLABORATION.md`
- **D-09** (reconcile loose 'git-worktree' wording in REQUIREMENTS COLLAB-01): `grep -E -i "REQUIREMENTS|COLLAB-01|spirit of" docs/COLLABORATION.md`
- **D-10** (high-trust pillar-domain ownership): `grep -E -i "high.trust|high trust|pillar.domain" docs/COLLABORATION.md`
- **D-11** (Hung allowed to touch anything on his role branch, PR-only list still applies): covered by D-10 + D-12 grep
- **D-12** (PR-only file list — exact filenames from CONTEXT.md):
  - `grep -q "frontend/src/lib/api.js" docs/COLLABORATION.md`
  - `grep -q "frontend/src/lib/api.public.js" docs/COLLABORATION.md`
  - `grep -q "frontend/src/App.jsx" docs/COLLABORATION.md`
  - `grep -q "frontend/src/components/ui" docs/COLLABORATION.md`
  - `grep -q "backend/app/models.py" docs/COLLABORATION.md`
  - `grep -q "backend/alembic/versions" docs/COLLABORATION.md`
  - `grep -q ".planning/STATE.md" docs/COLLABORATION.md`
  - `grep -q ".planning/ROADMAP.md" docs/COLLABORATION.md`
  - `grep -q "REQUIREMENTS-v1.2-prod.md" docs/COLLABORATION.md`
  - `grep -q "CLAUDE.md" docs/COLLABORATION.md`
  - `grep -q "docs/COLLABORATION.md" docs/COLLABORATION.md` (file references itself in critical-files table)
  - `grep -q "docker-compose.yml" docs/COLLABORATION.md`
  - `grep -E -q "Dockerfile" docs/COLLABORATION.md`
  - `grep -E -q ".github/workflows" docs/COLLABORATION.md`
- **D-13** (pillar-direct domain examples): `grep -q "frontend/src/pages/public" docs/COLLABORATION.md` AND `grep -q "frontend/src/pages/admin" docs/COLLABORATION.md` AND `grep -q "frontend/src/pages/organizer" docs/COLLABORATION.md`
- **D-14** (daily 3-hour sync): `grep -E -i "daily 3-hour|3 hour sync|3-hour" docs/COLLABORATION.md`
- **D-15** (merge after phase ships green, no mid-phase merges): `grep -E -i "ships green|after each phase|no mid-phase" docs/COLLABORATION.md`
- **D-16** (tie-breaker = pillar most affected wins, Andy casting vote): `grep -E -i "tie-breaker|most affected|casting vote" docs/COLLABORATION.md`
- **D-17** (wholesale rewrite — the file's existence in this state IS the verification; cross-check that no stale terms remain): `! grep -q "Hetzner" docs/COLLABORATION.md` AND `! grep -q "waitlist" docs/COLLABORATION.md` AND `! grep -q "iCal" docs/COLLABORATION.md` AND `! grep -q "v1.2-prod-deploy" docs/COLLABORATION.md`
- **D-18** (CLAUDE.md update guidance) — out of scope for THIS plan; verified in Plan 02

Additionally, run a sanity check that the docker-network test pattern is referenced (carry-over from CLAUDE.md): `grep -E -i "docker.network|uni-volunteer-scheduler_default|docker-network" docs/COLLABORATION.md`

If any check fails, fix it in `docs/COLLABORATION.md` and re-run the check. Repeat until clean.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=docs/COLLABORATION.md; grep -i "Andy.*admin.*organizer" $F >/dev/null; grep -i "Hung.*participant" $F >/dev/null; grep -E -i "single checkout|branch switching" $F >/dev/null; ! grep -q "git worktree add" $F; grep -E -i "high.trust|pillar.domain" $F >/dev/null; grep -E -i "casting vote" $F >/dev/null; grep -E -i "5173|8000|one docker stack|one stack at a time" $F >/dev/null; grep -q "frontend/src/lib/api.public.js" $F; grep -q "frontend/src/components/ui" $F; grep -q "REQUIREMENTS-v1.2-prod.md" $F; ! grep -q "Hetzner" $F; ! grep -q "waitlist" $F; ! grep -q "v1.2-prod-deploy" $F; echo "ALL_CHECKS_PASS"'</automated>
  </verify>
  <acceptance_criteria>
    - The bash one-liner in `<verify>` exits 0 and prints `ALL_CHECKS_PASS`
    - Every D-01..D-17 check listed in the action passes (D-18 is verified in Plan 02)
    - No stale terms remain: Hetzner, waitlist, iCal, v1.2-prod-deploy, REQUIREMENTS-v1.2.md (without `-prod`)
    - The doc references docker-network test pattern so Hung knows to follow it
    - All grep checks above pass on the current `docs/COLLABORATION.md`
  </acceptance_criteria>
  <done>Every locked decision from CONTEXT.md (D-01 through D-17) is observably encoded in `docs/COLLABORATION.md` with a passing grep check. D-18 is deferred to Plan 02.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the combined verification block from Task 2's `<verify>` section. Plan is done when:
- `docs/COLLABORATION.md` is a wholesale rewrite (no stale content from the prior pass)
- All grep checks for D-01..D-17 pass
- The file is committed to git via execute-phase's commit cadence
</verification>

<success_criteria>
- `docs/COLLABORATION.md` is the v1.2-prod collaboration contract reflecting D-01..D-17
- The file names the three long-lived role branches verbatim
- The PR-only file list contains every entry from D-12
- The daily 3-hour sync, merge-after-phase-ships-green, and pillar-most-affected tie-breaker rules are all present
- The "git-worktree" loose wording from REQUIREMENTS COLLAB-01 is reconciled with the actual single-checkout implementation
</success_criteria>

<output>
After completion, create `.planning/phases/14-collaboration-setup/14-01-SUMMARY.md` capturing what was rewritten, key D-XX decisions encoded, and a diff size note (lines added/removed).
</output>
