---
phase: 14-collaboration-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .gitignore
autonomous: true
requirements:
  - COLLAB-06
must_haves:
  truths:
    - "CLAUDE.md no longer says 'Sole developer: Andy' — it acknowledges two devs (Andy + Hung) on the v1.2-prod milestone"
    - "CLAUDE.md lists the role pillars and their owners (Hung = participant, Andy = admin + organizer)"
    - "CLAUDE.md instructs future Claude sessions to run `git branch --show-current` before working and only edit files in the pillar that owns the current branch (plus the PR-only list with explicit user permission)"
    - "CLAUDE.md preserves the existing `## Running tests`, `## Alembic conventions`, `## CSV import cadence`, and `## Teaching style` sections unchanged"
    - "`.gitignore` includes `.continue-here.md` so `/gsd-pause-work` artifacts from either dev's role branch don't accidentally commit and create cross-branch noise"
  artifacts:
    - path: "CLAUDE.md"
      provides: "v1.2-prod-aware project notes for both Claude Code sessions"
      contains: "git branch --show-current"
    - path: ".gitignore"
      provides: "ignore for /gsd-pause-work continuation artifacts"
      contains: ".continue-here.md"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/COLLABORATION.md"
      via: "cross-reference for full collaboration contract"
      pattern: "COLLABORATION\\.md"
---

<objective>
Two small, independent housekeeping changes that both prepare the repo for parallel role-branch work:

1. Update `CLAUDE.md` per D-18 to acknowledge two devs, list role pillars + owners, and add a branch-awareness instruction for future Claude sessions. Preserve all existing technical convention sections (docker-network test pattern, Alembic, CSV cadence, teaching style) unchanged.
2. Add `.continue-here.md` to `.gitignore` so `/gsd-pause-work` artifacts from either dev's role branch don't accidentally commit.

Purpose: Both changes prepare the repo for the parallel role-branch model. The CLAUDE.md update tells future Claude sessions which branch they should be on for any given pillar (COLLAB-06). The `.gitignore` entry is a small preventive measure to keep per-dev pause artifacts from creating cross-branch conflicts.

Output: Two files modified in one phase commit cadence. No app code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v1.2-prod.md
@.planning/phases/14-collaboration-setup/14-CONTEXT.md
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md per D-18</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the file being edited — read it so you see the current content of every section that must be PRESERVED unchanged)
    - .planning/phases/14-collaboration-setup/14-CONTEXT.md (D-18 is the canonical spec for this edit)
  </read_first>
  <action>
Edit `CLAUDE.md` (use the `Edit` tool, not `Write`, to keep the unchanged sections intact). Make exactly the following changes:

**Change 1: Replace the opening header block.**

Find this current opening (the first 4 lines of the file):

```
# uni-volunteer-scheduler — project notes for Claude

UCSB SciTrek volunteer scheduling app. Sole developer: Andy. First-time Claude
Code user — prefer plain-language explanations and short replies.
```

Replace it with a new opening block that:
- Keeps the title `# uni-volunteer-scheduler — project notes for Claude`
- States that this is the v1.2-prod milestone (production-ready by role)
- Acknowledges two devs: **Andy** and **Hung** (both running Claude Code + GSD on the same repo from their own machines)
- Lists the role pillars and owners explicitly:
  - **Hung** owns the **participant pillar** (Phase 15)
  - **Andy** owns the **admin pillar** (Phases 16, 17, 18) and the **organizer pillar** (Phase 19)
  - Phase 20 (cross-role integration) is shared
- Cross-references `docs/COLLABORATION.md` as the full collaboration contract

**Change 2: Add a new "Branch awareness" section.**

Add a new top-level section `## Branch awareness` immediately after the opening block (and before the existing `## Stack` section). The section MUST contain:

- An instruction to run `git branch --show-current` at the start of every session
- The mapping of branch → pillar:
  - `feature/v1.2-participant` → participant pillar (Hung's branch)
  - `feature/v1.2-admin` → admin pillar (Andy's branch)
  - `feature/v1.2-organizer` → organizer pillar (Andy's branch)
  - `main` → integration / shared / read-only between phase merges
- The rule: only edit files in the pillar that owns the current branch, PLUS files on the PR-only list (with explicit user permission). The PR-only list lives in `docs/COLLABORATION.md`.
- A note: if you find yourself on `main`, do NOT make changes — switch to the appropriate role branch first or ask the user which branch they want.

**Change 3: PRESERVE these sections completely unchanged.** Do NOT touch them in any way:

- `## Stack` (the FastAPI / React / Tailwind stack note)
- `## Running tests` (the docker-network test pattern with the `docker run` invocation)
- `## Alembic conventions` (the slug revision IDs + version_num widening + downgrade enum cleanup note)
- `## CSV import cadence` (the "every 11 weeks, not yearly" note)
- `## Planning harness` (the GSD harness intro note — leave it as is; if this section says Phases 0-7 are code-complete and Phase 8 is deferred, that's still accurate; the v1.2-prod product pivot mention is also still relevant)
- `## Teaching style` (Andy's "one concept per turn" preference)

Use Edit tool calls that target specific old_string → new_string replacements. Do NOT rewrite the whole file with Write — that risks losing the preserved sections.

After the edits, verify by reading the file once and confirming all four preserved section headings still appear exactly: `## Running tests`, `## Alembic conventions`, `## CSV import cadence`, `## Teaching style`.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=CLAUDE.md; ! grep -q "Sole developer" $F; grep -q "Andy" $F; grep -q "Hung" $F; grep -q "git branch --show-current" $F; grep -q "feature/v1.2-participant" $F; grep -q "feature/v1.2-admin" $F; grep -q "feature/v1.2-organizer" $F; grep -q "## Running tests" $F; grep -q "## Alembic conventions" $F; grep -q "## CSV import cadence" $F; grep -q "## Teaching style" $F; grep -q "uni-volunteer-scheduler_default" $F; grep -q "every 11 weeks" $F; grep -q "one concept per turn" $F; echo "CLAUDE_MD_OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "Sole developer" CLAUDE.md` — the old "Sole developer: Andy" framing is GONE
    - `grep -q "Andy" CLAUDE.md && grep -q "Hung" CLAUDE.md` — both devs named
    - `grep -q "git branch --show-current" CLAUDE.md` — the branch-awareness instruction (D-18 #3)
    - `grep -q "feature/v1.2-participant" CLAUDE.md` — branch name appears
    - `grep -q "feature/v1.2-admin" CLAUDE.md` — branch name appears
    - `grep -q "feature/v1.2-organizer" CLAUDE.md` — branch name appears
    - `grep -i -q "participant.*pillar\|pillar.*participant" CLAUDE.md` — pillar name appears
    - `grep -i -q "admin.*pillar\|pillar.*admin" CLAUDE.md` — pillar name appears
    - `grep -i -q "organizer.*pillar\|pillar.*organizer" CLAUDE.md` — pillar name appears
    - `grep -q "## Running tests" CLAUDE.md` — preserved section heading still present
    - `grep -q "## Alembic conventions" CLAUDE.md` — preserved section heading still present
    - `grep -q "## CSV import cadence" CLAUDE.md` — preserved section heading still present
    - `grep -q "## Teaching style" CLAUDE.md` — preserved section heading still present
    - `grep -q "uni-volunteer-scheduler_default" CLAUDE.md` — docker-network test command still present (full preserved-section content check)
    - `grep -q "every 11 weeks" CLAUDE.md` — CSV cadence still present
    - `grep -q "one concept per turn" CLAUDE.md` — teaching style still present
    - `grep -q "VARCHAR(128)" CLAUDE.md` — Alembic version_num widening note still present
    - `grep -q "COLLABORATION.md" CLAUDE.md` — cross-reference to the full contract
  </acceptance_criteria>
  <done>CLAUDE.md acknowledges two devs, lists role pillars and owners, contains the branch-awareness instruction, and all four preserved sections (Running tests / Alembic / CSV cadence / Teaching style) are intact and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add .continue-here.md to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (the file being edited — confirm `.continue-here.md` is not already present and pick a sensible section to add it under)
  </read_first>
  <action>
Edit `.gitignore` (use the `Edit` tool) to add `.continue-here.md` as an ignored path. The file already has a `# --- AI scaffolding (never commit) ---` section near the bottom that contains `.claude/` and `.gsd/` — that is the natural place. Add `.continue-here.md` as a new line in that section.

Concretely: find the line `.gsd/` in the AI scaffolding section and add a new line immediately after it: `.continue-here.md`. Do not reorder, delete, or modify any other line.

Rationale (do not put this in the file, this is just for the executor): `/gsd-pause-work` writes a `.continue-here.md` file at repo root with the current session's context so `/gsd-resume-work` can pick up cleanly. With two devs each pausing on their own role branch, this artifact would otherwise leak into commits and create cross-branch noise. Ignoring it keeps the per-dev pause/resume cycle local to each clone.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "^.continue-here.md$" .gitignore; grep -q "^.claude/$" .gitignore; grep -q "^.gsd/$" .gitignore; echo "GITIGNORE_OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^.continue-here.md$" .gitignore` returns 0 (the new entry exists as its own line)
    - `grep -q "^.claude/$" .gitignore` and `grep -q "^.gsd/$" .gitignore` still return 0 (the existing AI scaffolding entries are untouched)
    - The total number of lines in `.gitignore` increased by exactly 1 from before the edit
    - No other entries were modified or removed
  </acceptance_criteria>
  <done>`.gitignore` includes a `.continue-here.md` entry under the AI scaffolding section. All other entries are unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- CLAUDE.md acknowledges two devs and contains the branch-awareness instruction
- All four preserved sections in CLAUDE.md are intact
- `.gitignore` includes `.continue-here.md`
- Both files are committed via execute-phase commit cadence
</verification>

<success_criteria>
- COLLAB-06 satisfied: future Claude sessions reading CLAUDE.md will know which branch they should be on for any given pillar
- The docker-network test pattern, Alembic conventions, CSV cadence note, and teaching style note are PRESERVED unchanged in CLAUDE.md
- `/gsd-pause-work` artifacts from either dev's clone won't accidentally commit
</success_criteria>

<output>
After completion, create `.planning/phases/14-collaboration-setup/14-02-SUMMARY.md` recording the exact CLAUDE.md changes made (which sections were preserved, what was added) and the `.gitignore` line that was added.
</output>
