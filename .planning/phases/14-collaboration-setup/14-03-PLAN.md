---
phase: 14-collaboration-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified: []
autonomous: false
requirements:
  - COLLAB-05
must_haves:
  truths:
    - "Three long-lived role branches exist locally on Andy's clone: feature/v1.2-participant, feature/v1.2-admin, feature/v1.2-organizer"
    - "All three role branches exist on origin (pushed with -u so they track origin)"
    - "All three role branches point at the same commit as main at the moment of creation (i.e. they were branched from a main that already contains plans 01 and 02)"
    - "main contains the wholesale-rewritten docs/COLLABORATION.md from Plan 01 and the updated CLAUDE.md + .gitignore from Plan 02 BEFORE the role branches are cut (otherwise the role branches inherit a stale baseline)"
    - "The docker stack still boots cleanly from each role branch — backend reachable at http://localhost:8000/api/v1/health, frontend reachable at http://localhost:5173 (Success Criterion #2 from ROADMAP)"
    - "Hung can fetch the three branches from origin and check them out on his own clone (verified by Hung after this plan ships, not by Andy)"
  artifacts:
    - path: ".git/refs/heads/feature/v1.2-participant"
      provides: "local ref for the participant role branch"
    - path: ".git/refs/heads/feature/v1.2-admin"
      provides: "local ref for the admin role branch"
    - path: ".git/refs/heads/feature/v1.2-organizer"
      provides: "local ref for the organizer role branch"
    - path: ".git/refs/remotes/origin/feature/v1.2-participant"
      provides: "remote tracking ref for the participant role branch"
    - path: ".git/refs/remotes/origin/feature/v1.2-admin"
      provides: "remote tracking ref for the admin role branch"
    - path: ".git/refs/remotes/origin/feature/v1.2-organizer"
      provides: "remote tracking ref for the organizer role branch"
  key_links:
    - from: "docs/COLLABORATION.md"
      to: "feature/v1.2-participant, feature/v1.2-admin, feature/v1.2-organizer"
      via: "branch names referenced verbatim in the rewritten contract"
      pattern: "feature/v1\\.2-(participant|admin|organizer)"
    - from: "CLAUDE.md"
      to: "feature/v1.2-participant, feature/v1.2-admin, feature/v1.2-organizer"
      via: "branch-awareness section maps each branch to its pillar"
      pattern: "feature/v1\\.2-(participant|admin|organizer)"
---

<objective>
Create the three long-lived role branches that v1.2-prod parallel work runs on (`feature/v1.2-participant`, `feature/v1.2-admin`, `feature/v1.2-organizer`), branched from the post-Plan-01/02 `main`, and pushed to origin so Hung can fetch them on his own clone. Verify the docker stack still boots from each role branch.

Purpose: This plan operationalizes COLLAB-05 ("Set up a worktree for each role pillar against the current `main` and verify both Andy and Hung can run their stack independently") and ROADMAP Phase 14 Success Criterion #2 ("Andy and Hung each have a working worktree on a long-lived role branch ... and can each boot the docker stack + run tests independently"). The branches MUST start from a `main` that already includes the rewritten `docs/COLLABORATION.md` (Plan 01) and the updated `CLAUDE.md` + `.gitignore` (Plan 02) — otherwise the role branches inherit a stale baseline and will need re-cutting later.

Output: Three new long-lived branches existing both locally and on origin, each pointing at the same commit as `main`. No working-tree file changes (this plan only runs git plumbing). Optionally, docker stack verified to boot from each branch (gated as a checkpoint task because it touches local docker state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v1.2-prod.md
@.planning/phases/14-collaboration-setup/14-CONTEXT.md
@.planning/phases/14-collaboration-setup/14-01-PLAN.md
@.planning/phases/14-collaboration-setup/14-02-PLAN.md
@docs/COLLABORATION.md
@CLAUDE.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — confirm main is up to date with Plans 01+02</name>
  <files></files>
  <read_first>
    - .planning/phases/14-collaboration-setup/14-01-PLAN.md (confirms what Plan 01 wrote — the wholesale-rewritten docs/COLLABORATION.md)
    - .planning/phases/14-collaboration-setup/14-02-PLAN.md (confirms what Plan 02 wrote — updated CLAUDE.md and .gitignore)
    - The current branch via `git branch --show-current` (working branch from Plans 01+02; likely `AndywClaude` or whatever the in-progress branch is)
    - The current state of `main` via `git log main -5` and `git show main:docs/COLLABORATION.md | head -20` (confirms the rewritten file is on main)
  </read_first>
  <action>
This is a non-modifying verification step. It catches the case where Plans 01 and 02 have been written but NOT yet merged to `main` — which would mean cutting the role branches now would inherit a stale baseline and require re-cutting later. Do NOT skip this step.

Run the following checks in order. If ANY check fails, STOP and surface the failure to the user before proceeding to Task 2.

1. **Working tree is clean.** Run `git status --porcelain` — expected output: nothing (empty). If there are uncommitted changes, surface them and ask the user how to proceed (commit, stash, or abort the plan).

2. **Currently checked out branch identified.** Run `git branch --show-current`. Record the result. This is the branch you'll return to at the end of Task 5 if the optional docker verification is run.

3. **`main` exists and is reachable.** Run `git rev-parse --verify main` and `git rev-parse --verify origin/main`. Both must succeed. If `origin/main` is ahead of local `main`, run `git fetch origin main` first.

4. **`main` contains Plan 01's wholesale rewrite of docs/COLLABORATION.md.** Run:
   - `git show main:docs/COLLABORATION.md > /tmp/main-collab.md && wc -l /tmp/main-collab.md` — expected: between 80 and 600 lines (matches Plan 01's acceptance criteria)
   - `git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-participant"` — expected: exit 0
   - `git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-admin"` — expected: exit 0
   - `git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-organizer"` — expected: exit 0
   - `git show main:docs/COLLABORATION.md | grep -q "REQUIREMENTS-v1.2-prod.md"` — expected: exit 0
   - `git show main:docs/COLLABORATION.md | grep -q "casting vote"` — expected: exit 0
   - `git show main:docs/COLLABORATION.md | grep -v "^$" | grep -q "Hetzner" && echo "STALE_HETZNER_FOUND" || echo "OK"` — expected: `OK` (no Hetzner reference on main)
   If ANY of these fail, the rewrite from Plan 01 is NOT yet on main. STOP and tell the user: "Plan 01's rewritten docs/COLLABORATION.md is not on main yet. Merge Plan 01's branch to main before running Plan 03, otherwise the role branches will inherit a stale baseline."

5. **`main` contains Plan 02's updated CLAUDE.md.** Run:
   - `git show main:CLAUDE.md | grep -q "git branch --show-current"` — expected: exit 0
   - `git show main:CLAUDE.md | grep -q "feature/v1.2-participant"` — expected: exit 0
   - `git show main:CLAUDE.md | grep -q "feature/v1.2-admin"` — expected: exit 0
   - `git show main:CLAUDE.md | grep -q "feature/v1.2-organizer"` — expected: exit 0
   - `git show main:CLAUDE.md | grep -q "Sole developer" && echo "STALE_SOLE_DEV_FOUND" || echo "OK"` — expected: `OK`
   If any fail, STOP with: "Plan 02's CLAUDE.md update is not on main yet. Merge Plan 02's branch first."

6. **`main` contains Plan 02's .gitignore update.** Run:
   - `git show main:.gitignore | grep -q "^.continue-here.md$"` — expected: exit 0
   If it fails, STOP with: "Plan 02's .gitignore update is not on main yet."

7. **No existing role branches.** Run `git branch --list "feature/v1.2-*"` and `git ls-remote --heads origin "feature/v1.2-*"`. Both should return empty (no existing branches with these names). If a role branch already exists, STOP and ask the user — overwriting an existing branch would lose history.

8. **Print a summary** of the pre-flight check results so the user can confirm before proceeding to branch creation:
   - Current branch: result of step 2
   - main HEAD: output of git rev-parse main
   - main has rewritten COLLABORATION.md: yes
   - main has updated CLAUDE.md: yes
   - main has .gitignore entry: yes
   - No existing role branches: confirmed
  </action>
  <verify>
    <automated>bash -c 'set -e; test -z "$(git status --porcelain)"; git rev-parse --verify main >/dev/null; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-participant"; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-admin"; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-organizer"; git show main:docs/COLLABORATION.md | grep -q "REQUIREMENTS-v1.2-prod.md"; git show main:docs/COLLABORATION.md | grep -q "casting vote"; ! git show main:docs/COLLABORATION.md | grep -q "Hetzner"; git show main:CLAUDE.md | grep -q "git branch --show-current"; git show main:CLAUDE.md | grep -q "feature/v1.2-participant"; git show main:CLAUDE.md | grep -q "feature/v1.2-admin"; git show main:CLAUDE.md | grep -q "feature/v1.2-organizer"; ! git show main:CLAUDE.md | grep -q "Sole developer"; git show main:.gitignore | grep -q "^.continue-here.md$"; test -z "$(git branch --list feature/v1.2-participant feature/v1.2-admin feature/v1.2-organizer)"; echo "PREFLIGHT_OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` is empty (clean working tree)
    - `git rev-parse --verify main` succeeds
    - `git show main:docs/COLLABORATION.md` contains `feature/v1.2-participant`, `feature/v1.2-admin`, `feature/v1.2-organizer`, `REQUIREMENTS-v1.2-prod.md`, `casting vote`
    - `git show main:docs/COLLABORATION.md` does NOT contain `Hetzner` (catches "still on the stale file")
    - `git show main:CLAUDE.md` contains `git branch --show-current`, `feature/v1.2-participant`, `feature/v1.2-admin`, `feature/v1.2-organizer`
    - `git show main:CLAUDE.md` does NOT contain `Sole developer` (catches "Plan 02 not merged")
    - `git show main:.gitignore` contains `.continue-here.md` as a top-level entry
    - `git branch --list feature/v1.2-*` returns empty (no existing role branches)
    - `git ls-remote --heads origin feature/v1.2-*` returns empty (no existing role branches on origin)
    - Pre-flight summary printed to user before any branch-creation commands run
  </acceptance_criteria>
  <done>main contains the merged Plan 01 + Plan 02 work, working tree is clean, no role branches exist yet, and a pre-flight summary has been printed.</done>
</task>

<task type="auto">
  <name>Task 2: Create and push feature/v1.2-participant</name>
  <files></files>
  <read_first>
    - The output of Task 1's pre-flight check (must have passed)
    - `git rev-parse main` (the commit the new branch will start from)
  </read_first>
  <action>
Create the participant role branch from `main` and push it to origin with `-u` so it tracks the remote.

Run these commands in order:

1. `git checkout main` (switch to main first so the new branch starts from the right commit)
2. `git pull origin main` (make sure local main matches origin)
3. `git checkout -b feature/v1.2-participant` (create the branch from main; this also checks it out)
4. `git push -u origin feature/v1.2-participant` (push and set upstream tracking)

After pushing, verify:
- `git rev-parse feature/v1.2-participant` equals `git rev-parse main` (branch is at the same commit)
- `git rev-parse origin/feature/v1.2-participant` equals `git rev-parse feature/v1.2-participant` (remote matches local)
- `git config branch.feature/v1.2-participant.remote` returns `origin` (upstream tracking set)

Do NOT make any commits on this branch yet. It is a long-lived branch but Phase 14 only creates it; the first real commit on `feature/v1.2-participant` will come from Hung in Phase 15 (or from Plan 14-04's parallel test, whichever lands first).
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(git rev-parse feature/v1.2-participant)" = "$(git rev-parse main)"; test "$(git rev-parse origin/feature/v1.2-participant)" = "$(git rev-parse feature/v1.2-participant)"; test "$(git config branch.feature/v1.2-participant.remote)" = "origin"; echo "PARTICIPANT_BRANCH_OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --verify feature/v1.2-participant` succeeds
    - `git rev-parse --verify origin/feature/v1.2-participant` succeeds
    - `git rev-parse feature/v1.2-participant` equals `git rev-parse main` (branched from current main, no extra commits)
    - `git rev-parse origin/feature/v1.2-participant` equals `git rev-parse feature/v1.2-participant` (remote and local in sync)
    - `git config branch.feature/v1.2-participant.remote` returns `origin` (upstream tracking is set so future `git push` from this branch works without `-u`)
    - No commits have been added to this branch beyond `main`'s tip
  </acceptance_criteria>
  <done>`feature/v1.2-participant` exists locally and on origin, points at the same commit as main, and tracks origin/feature/v1.2-participant.</done>
</task>

<task type="auto">
  <name>Task 3: Create and push feature/v1.2-admin</name>
  <files></files>
  <read_first>
    - The output of Task 2 (participant branch should already exist locally + on origin, but currently checked out)
    - `git rev-parse main` (must still equal `git rev-parse feature/v1.2-participant`; the admin branch starts from the same commit)
  </read_first>
  <action>
Create the admin role branch from `main`. Same procedure as Task 2.

1. `git checkout main` (switch back to main; we want admin to start from main, NOT from participant — even though they're the same commit right now, branching from main makes the intent explicit and matches the Phase 14 contract)
2. `git checkout -b feature/v1.2-admin`
3. `git push -u origin feature/v1.2-admin`

Verify:
- `git rev-parse feature/v1.2-admin` equals `git rev-parse main`
- `git rev-parse feature/v1.2-admin` equals `git rev-parse feature/v1.2-participant` (all three role branches share the same starting commit)
- `git rev-parse origin/feature/v1.2-admin` equals `git rev-parse feature/v1.2-admin`
- `git config branch.feature/v1.2-admin.remote` returns `origin`

Same rule as Task 2: NO commits on this branch yet. Andy's first real commit on `feature/v1.2-admin` will come from Phase 16 work (or from Plan 14-04's parallel test).
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(git rev-parse feature/v1.2-admin)" = "$(git rev-parse main)"; test "$(git rev-parse feature/v1.2-admin)" = "$(git rev-parse feature/v1.2-participant)"; test "$(git rev-parse origin/feature/v1.2-admin)" = "$(git rev-parse feature/v1.2-admin)"; test "$(git config branch.feature/v1.2-admin.remote)" = "origin"; echo "ADMIN_BRANCH_OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --verify feature/v1.2-admin` succeeds
    - `git rev-parse --verify origin/feature/v1.2-admin` succeeds
    - `git rev-parse feature/v1.2-admin` equals `git rev-parse main`
    - `git rev-parse feature/v1.2-admin` equals `git rev-parse feature/v1.2-participant` (consistency check across the role branches)
    - `git rev-parse origin/feature/v1.2-admin` equals `git rev-parse feature/v1.2-admin`
    - `git config branch.feature/v1.2-admin.remote` returns `origin`
    - No commits have been added to this branch beyond `main`'s tip
  </acceptance_criteria>
  <done>`feature/v1.2-admin` exists locally and on origin, points at the same commit as main and feature/v1.2-participant, and tracks origin/feature/v1.2-admin.</done>
</task>

<task type="auto">
  <name>Task 4: Create and push feature/v1.2-organizer</name>
  <files></files>
  <read_first>
    - The output of Task 3 (admin branch exists)
    - `git rev-parse main` (must still equal both prior role branches)
  </read_first>
  <action>
Create the organizer role branch from `main`. Same procedure as Tasks 2 and 3.

1. `git checkout main`
2. `git checkout -b feature/v1.2-organizer`
3. `git push -u origin feature/v1.2-organizer`

Verify:
- `git rev-parse feature/v1.2-organizer` equals `git rev-parse main`
- All three role branches are at the same commit:
  - `git rev-parse feature/v1.2-participant` == `git rev-parse feature/v1.2-admin` == `git rev-parse feature/v1.2-organizer` == `git rev-parse main`
- `git rev-parse origin/feature/v1.2-organizer` equals `git rev-parse feature/v1.2-organizer`
- `git config branch.feature/v1.2-organizer.remote` returns `origin`

After this task, all three role branches exist. Confirm with `git for-each-ref --format='%(refname:short) %(objectname:short)' refs/heads/feature/v1.2-* refs/remotes/origin/feature/v1.2-*` — expected output: 6 lines (3 local + 3 remote), all pointing at the same short SHA.

Same rule as before: NO commits on this branch yet.
  </action>
  <verify>
    <automated>bash -c 'set -e; M=$(git rev-parse main); test "$(git rev-parse feature/v1.2-organizer)" = "$M"; test "$(git rev-parse feature/v1.2-participant)" = "$M"; test "$(git rev-parse feature/v1.2-admin)" = "$M"; test "$(git rev-parse origin/feature/v1.2-organizer)" = "$M"; test "$(git rev-parse origin/feature/v1.2-participant)" = "$M"; test "$(git rev-parse origin/feature/v1.2-admin)" = "$M"; test "$(git config branch.feature/v1.2-organizer.remote)" = "origin"; LOCAL_COUNT=$(git for-each-ref --format="%(refname:short)" refs/heads/feature/v1.2-* | wc -l | tr -d " "); REMOTE_COUNT=$(git for-each-ref --format="%(refname:short)" refs/remotes/origin/feature/v1.2-* | wc -l | tr -d " "); test "$LOCAL_COUNT" = "3"; test "$REMOTE_COUNT" = "3"; echo "ORGANIZER_BRANCH_OK ALL_THREE_PRESENT"'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --verify feature/v1.2-organizer` succeeds
    - `git rev-parse --verify origin/feature/v1.2-organizer` succeeds
    - `git rev-parse feature/v1.2-organizer` equals `git rev-parse main`
    - All three role branches are at the same commit as main (4-way equality)
    - All three remote tracking refs are at the same commit (origin in sync with local for all three)
    - `git config branch.feature/v1.2-organizer.remote` returns `origin`
    - `git for-each-ref refs/heads/feature/v1.2-*` lists exactly 3 branches
    - `git for-each-ref refs/remotes/origin/feature/v1.2-*` lists exactly 3 branches
  </acceptance_criteria>
  <done>All three long-lived role branches exist locally and on origin, all pointing at the same commit as main. COLLAB-05's "set up a worktree for each role pillar against the current main" requirement is structurally satisfied — Hung can now fetch all three branches.</done>
</task>

<task type="auto">
  <name>Task 5: Confirm GSD config override is documented in COLLABORATION.md (no config change)</name>
  <files></files>
  <read_first>
    - .planning/config.json (to confirm the existing `phase_branch_template` value)
    - docs/COLLABORATION.md on main (to confirm Plan 01 mentioned the override — it should describe that the role-branch model overrides the GSD harness convention so future Claude sessions don't auto-create `gsd/phase-15-...` branches)
  </read_first>
  <action>
This task implements the safe option for the GSD config reconciliation flagged in CONTEXT.md → Deferred Ideas ("Reconciling .planning/config.json phase_branch_template"). Per the missing_plans_spec, the default resolution is option (b): leave the config alone and confirm the override is documented in COLLABORATION.md. We're picking (b) because changing the GSD harness config could have downstream effects on harness behavior that aren't worth risking in a docs-only phase.

This is a verification-only task. No file edits. No git commands beyond `git show main:...`.

Run these checks:

1. **Confirm the GSD config still has the old phase-branch template** (this is just documenting current state, not a problem):
   - `cat .planning/config.json` — expected: contains `"phase_branch_template": "gsd/phase-{phase}-{slug}"` and `"branching_strategy": "none"`
   - This is the loose contract that Phase 14 explicitly documents as overridden by the role-branch model.

2. **Confirm COLLABORATION.md on main describes the role-branch model and reconciles it with the GSD harness convention.** The exact wording is at Plan 01's discretion (per CONTEXT.md → Claude's Discretion), so use a few flexible greps rather than a strict string match:
   - `git show main:docs/COLLABORATION.md | grep -E -i "feature/v1.2-(participant|admin|organizer)" | head -5` — expected: at least 3 matches (the three branch names)
   - `git show main:docs/COLLABORATION.md | grep -E -i "single checkout|branch switching"` — expected: at least 1 match (the implementation reconciliation from D-06)
   - `git show main:docs/COLLABORATION.md | grep -E -i "long.lived|long-lived"` — expected: at least 1 match (the long-lived role-branch concept)

3. **If ANY of the COLLABORATION.md checks fails**, that means Plan 01's rewrite did NOT include the reconciliation of the role-branch model with the GSD config / harness conventions. Do NOT fix `docs/COLLABORATION.md` from Plan 03 — it's a PR-only file (per D-12) and Plan 03 should not be modifying files that Plan 01 owns. Instead, surface the failure: "COLLABORATION.md on main is missing the role-branch / GSD-config reconciliation. Plan 01 needs a fix-up commit." Stop the plan.

4. **Print a one-line confirmation** to the user: "GSD config left at `phase_branch_template: gsd/phase-{phase}-{slug}` (deferred). COLLABORATION.md documents the role-branch override."

Note for the executor: Per CONTEXT.md → Deferred Ideas, the alternative resolution is option (a) — actually update `.planning/config.json` to set `branching_strategy: "feature"` and change `phase_branch_template`. The plan does NOT take that path because changing GSD harness config from a docs-only phase risks unexpected harness behavior changes. If the user wants to take option (a) instead, they can do so manually in a follow-up phase.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "phase_branch_template" .planning/config.json; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-participant"; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-admin"; git show main:docs/COLLABORATION.md | grep -q "feature/v1.2-organizer"; git show main:docs/COLLABORATION.md | grep -E -i "single checkout|branch switching" >/dev/null; git show main:docs/COLLABORATION.md | grep -E -i "long.lived|long-lived" >/dev/null; echo "GSD_OVERRIDE_DOCUMENTED"'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/config.json` is unchanged (still has the old `phase_branch_template`) — verified by NOT running any edit on it
    - `git show main:docs/COLLABORATION.md` contains all three role-branch names verbatim
    - `git show main:docs/COLLABORATION.md` contains a phrase like "single checkout" or "branch switching" (D-06 reconciliation)
    - `git show main:docs/COLLABORATION.md` contains a phrase like "long-lived" describing the branches
    - Confirmation line printed to the user explaining the deferred decision
  </acceptance_criteria>
  <done>The GSD config is intentionally left unchanged, and the role-branch override is confirmed to be documented in the merged COLLABORATION.md so future Claude sessions reading the file will know not to auto-create `gsd/phase-15-...` branches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Optional human-verified docker stack boot from each role branch</name>
  <files></files>
  <read_first>
    - The output of Tasks 2-4 (all three role branches must already exist locally and on origin)
    - `backend/app/main.py:80` — confirms the actual health endpoint is `/api/v1/health` (verified during planning)
    - `docker-compose.yml` — confirms container names and network: `uni-volunteer-scheduler_default`
    - `CLAUDE.md` § "Running tests" — the docker-network pytest pattern that should still work from each role branch
  </read_first>
  <action>
This is a checkpoint because it touches local docker container state and the user (Andy) may not want the executor bringing the stack down/up unattended. The user can either run the verification themselves (recommended) or grant permission for the executor to run it.

ROADMAP Phase 14 Success Criterion #2 says "Andy and Hung each have a working worktree on a long-lived role branch ... and can each boot the docker stack + run tests independently". This task verifies the "boot the docker stack" half on Andy's machine. The "run tests independently" half is covered by the docker-network test pattern in CLAUDE.md and is verified passively whenever Andy runs `pytest` from any role branch.

The executor's job: print the verification steps below to the user and pause for `approved` / `skip` / failure-description. Do NOT run the docker commands without explicit user permission.

**Steps to run on Andy's machine (run yourself or instruct the executor to run after granting permission):**

1. Ensure docker stack is up. From any branch, run `docker compose ps`. Expected: `db`, `redis`, `backend`, `celery_worker`, `celery_beat` all running. If not, `docker compose up -d` first.

2. Verify the backend health endpoint responds. Run `curl -fsS http://localhost:8000/api/v1/health`. Expected: HTTP 200 with a JSON body indicating the DB is reachable. (The actual endpoint is `/api/v1/health` — confirmed in `backend/app/main.py:80`.)

3. Verify the frontend is reachable (optional). If `npm run dev` is running in the `frontend/` directory, run `curl -fsS http://localhost:5173 | head -20`. Expected: HTML response (Vite dev server index). If `npm run dev` is not running, that's fine — the role branches don't need the frontend dev server running to be valid.

4. Switch to each role branch in turn and confirm the stack still works. For each of `feature/v1.2-participant`, `feature/v1.2-admin`, `feature/v1.2-organizer`:
   - `git checkout feature/v1.2-<pillar>`
   - `curl -fsS http://localhost:8000/api/v1/health`
   - Expected: same 200 response (because all three branches are at the same commit as `main`, the running backend container does not need to restart — it serves the same code).

5. Switch back to `main` (or your working branch) with `git checkout main`.

6. Optional bonus: run the docker-network pytest pattern from one role branch to confirm tests work. From the participant branch, run:
   `docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q --collect-only" 2>&1 | tail -10`
   Expected: pytest collects tests with no errors. Use `--collect-only` so this finishes fast and doesn't actually run a test suite.

If all six steps pass, the user types `approved` to resume.

If any step fails, the user shares the error. The most likely failures are:
- Backend container not running, fixed via `docker compose up -d backend`
- Health endpoint not at `/api/v1/health`, check `backend/app/main.py` for the actual route
- Different docker network name, run `docker network ls | grep uni-volunteer-scheduler` to find the actual name and update the pytest command

If the user wants to skip this checkpoint entirely (e.g. because they're confident the stack works since all three branches are at the same commit as main), they can type `skip` and the plan still passes — Plan 04's parallel test will exercise the workflow end-to-end and indirectly validate that the stack works from each branch.
  </action>
  <verify>
    <automated>echo "MANUAL_CHECKPOINT - user must type approved or skip"</automated>
  </verify>
  <resume-signal>Type "approved" if the docker stack boots cleanly from each role branch, "skip" if you want to defer this verification to Plan 04's parallel test, or describe any failures.</resume-signal>
  <acceptance_criteria>
    - User explicitly types `approved` or `skip` to resume the plan
    - If `approved`: docker stack confirmed booting from all three role branches with the backend health endpoint returning 200
    - If `skip`: the user accepts the residual risk that the stack boot from each role branch hasn't been verified end-to-end on Andy's machine (low risk because all three branches share the same commit)
    - On resume, the working tree is back on a sane branch (main or the user's preferred working branch); the executor confirms with `git branch --show-current`
  </acceptance_criteria>
  <done>The user has either approved (verifying the docker stack boots from each role branch) or explicitly skipped this checkpoint, deferring the boot-from-each-branch validation to Plan 04's parallel test.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- All three role branches exist locally and on origin, all pointing at the same commit as `main` at the time of branch creation
- `.planning/config.json` is intentionally unchanged; the override is documented in `docs/COLLABORATION.md` from Plan 01
- The docker stack boot has been verified (or explicitly skipped) on Andy's machine
- No application code or tracked files were modified by this plan

Run the combined verification:
`bash -c 'set -e; M=$(git rev-parse main); test "$(git rev-parse feature/v1.2-participant)" = "$M"; test "$(git rev-parse feature/v1.2-admin)" = "$M"; test "$(git rev-parse feature/v1.2-organizer)" = "$M"; test "$(git rev-parse origin/feature/v1.2-participant)" = "$M"; test "$(git rev-parse origin/feature/v1.2-admin)" = "$M"; test "$(git rev-parse origin/feature/v1.2-organizer)" = "$M"; echo "ALL_THREE_ROLE_BRANCHES_VERIFIED"'`
</verification>

<success_criteria>
- COLLAB-05 satisfied: each role pillar has a long-lived branch against the current `main` and Andy has verified (or accepted skip on) booting the docker stack from each one
- ROADMAP Phase 14 Success Criterion #2 satisfied for Andy's clone: working role branch exists, docker stack boots, tests can be run via the docker-network pattern
- The branches are pushed to origin so Hung can fetch them on his own clone (Hung's verification is implicit — the next time Hung runs `git fetch`, he'll see the three branches and can `git checkout` any of them)
- No application code touched, no working-tree files modified
- The role branches are ready to receive Phase 15 / 16 / 19 work without further setup
</success_criteria>

<output>
After completion, create `.planning/phases/14-collaboration-setup/14-03-SUMMARY.md` recording:
- The `main` commit SHA the three branches were cut from
- Confirmation that all three branches exist locally + on origin
- Whether Task 6 (docker stack boot verification) was approved, skipped, or had failures
- A note that Hung still needs to run `git fetch && git checkout feature/v1.2-participant` on his clone to complete the symmetric half of COLLAB-05 (his side is verified passively when he picks up Phase 15)
</output>
</content>
</invoke>