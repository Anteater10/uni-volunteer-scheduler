---
phase: 20-cross-role-integration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - README.md
  - CLAUDE.md
  - IDEAS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [INTEG-05, INTEG-06]
must_haves:
  truths:
    - "README.md is a real v1.2-prod writeup (not the one-line placeholder)"
    - "No live code or doc file contains stale 'yearly' (CSV), 'student account', or bare '/organize' references outside deliberate redirects"
    - "CLAUDE.md Planning harness section reflects v1.2-prod phase completion state"
    - "Every cross-role bug surfaced in 20-01 + 20-02 is either fixed, filed as v1.3 deferral, or logged with explicit rationale"
  artifacts:
    - path: "README.md"
      provides: "v1.2-prod project README: stack summary, boot instructions, test commands, role tour, links to ROADMAP + smoke checklist + CLAUDE.md"
      min_lines: 60
    - path: "CLAUDE.md"
      provides: "Updated Planning harness paragraph reflecting phases 0–20 complete"
    - path: "IDEAS.md"
      provides: "All 'yearly' references rewritten to 'quarterly' per CLAUDE.md cadence rule"
    - path: ".planning/phases/20-cross-role-integration/20-bugs-log.md"
      provides: "Triage record for every INTEG-05 cross-role bug: fixed-here / filed-for-v1.3 / dismissed-with-rationale"
  key_links:
    - from: "README.md"
      to: "docs/smoke-checklist.md + .planning/ROADMAP.md + CLAUDE.md"
      via: "markdown links in role tour + test sections"
      pattern: "smoke-checklist|ROADMAP|CLAUDE"
---

<objective>
Close Phase 20 (and v1.2-prod) by sweeping stale references from live
docs, writing a real README, updating CLAUDE.md's phase-complete
wording, and triaging the cross-role bugs surfaced in Plans 20-01 and
20-02. Covers INTEG-05 (bug triage) and INTEG-06 (doc sweep).

Purpose: this is the v1.2-prod milestone sign-off plan. After this lands,
every doc a future reader touches reflects the actual state of the
codebase, and every bug surfaced during integration is accounted for.

Output: README rewrite + targeted edits to CLAUDE.md + IDEAS.md,
STATE.md + ROADMAP.md updates marking Phase 20 complete, and a
bugs-triage log.

**PR-only file warning:** README.md, CLAUDE.md, .planning/STATE.md,
.planning/ROADMAP.md, and .github/workflows/* are PR-only per
docs/COLLABORATION.md. This plan's commits ship as one final PR — do
NOT push directly to `main`. Coordinate with Hung before merging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-cross-role-integration/20-RESEARCH.md
@.planning/phases/20-cross-role-integration/20-01-SUMMARY.md
@.planning/phases/20-cross-role-integration/20-02-SUMMARY.md
@CLAUDE.md
@docs/COLLABORATION.md
@docs/ADMIN-AUDIT.md
@docs/ORG-AUDIT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@IDEAS.md
@README.md

<interfaces>
<!-- Precise sweep targets verified in research. -->

IDEAS.md stale "yearly" references (research confirmed — grep clean elsewhere):
- line 142: "yearly ops win + real AI surface" → "quarterly ops win + real AI surface"
- line 196: "yearly upload" → "quarterly upload"
- line 218: "yearly event generation" → "quarterly event generation"
- line 266: "yearly ops win + real AI surface" → "quarterly ops win + real AI surface"
- line 278: "yearly event CSV" → "quarterly event CSV"

(Verify line numbers during execution — content may have shifted.)

CLAUDE.md closing Planning harness paragraph (lines 67–71 at research
time — verify live):
- Currently says "Phases 0–7 are code-complete" — update to reflect
  v1.2-prod phases 0–20 complete (or 0–19 complete if Phase 20 ships
  this PR), Phase 8 deployment still deferred, v1.2-prod milestone
  closed.

DO NOT sweep these (intentional):
- `docs/ADMIN-AUDIT.md` line 50 "not yearly — quarterly" (correctness note)
- `frontend/src/pages/UsersAdminPage.jsx` "students don't have accounts"
  (correct copy)
- `frontend/src/App.jsx` `RedirectOrganizeRoster` catch-all (deliberate
  Phase 19-01 redirect)
- Any occurrence inside `.planning/phases/00-*` through `19-*` (frozen
  historical artefacts per docs/ORG-AUDIT.md precedent)
- `.planning/PROJECT.md` line 137 "Autonomous run introduced student
  accounts" (historical decision-log entry)
- `backend/app/models.py` / `magic_link_service.py` "override" references
  (different concept — env overrides, not the retired Overrides tab)

README.md content to produce (currently a one-line placeholder):
- Project name + one-sentence description (UCSB SciTrek volunteer
  scheduling, account-less signup, quarterly CSV import)
- Stack summary (FastAPI / React 19 / Postgres 16 / Celery / Redis /
  Playwright / Tailwind v4)
- Quick boot — exact docker compose commands from CLAUDE.md
- Running tests — backend (docker-network pattern, link to CLAUDE.md),
  frontend (`cd frontend && npm run test -- --run`), Playwright
  (`npx playwright test`)
- Role tour — three URLs:
  - Participant: http://localhost:5173/events (account-less)
  - Admin: http://localhost:5173/login (admin@e2e.example.com)
  - Organizer: http://localhost:5173/login (organizer@e2e.example.com,
    lands on `/organizer`)
- Links: `.planning/ROADMAP.md`, `docs/smoke-checklist.md`,
  `docs/COLLABORATION.md`, `CLAUDE.md`
- Credit footer (UCSB SciTrek)

STATE.md + ROADMAP.md close-out:
- ROADMAP.md: check Phase 20 box, update Progress Table row to 3/3
  Complete with today's date, update Phases line in frontmatter totals.
- STATE.md: update `status`, `progress.completed_phases`, `percent`,
  Current Position, Current Status, Next Action, last_activity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for v1.2-prod + sweep IDEAS.md yearly→quarterly</name>
  <files>README.md, IDEAS.md</files>
  <action>
**README.md** — replace the one-line placeholder with a real v1.2-prod
README per the §README.md content spec in the Interfaces block above.
Structure:

```markdown
# uni-volunteer-scheduler

UCSB SciTrek volunteer scheduling app. Account-less participant signup,
quarterly CSV module-template import, three-role UX (participant,
admin, organizer).

## Stack

- Backend: FastAPI + SQLAlchemy + Alembic + Postgres 16 + Celery + Redis
- Frontend: React 19 + Vite 7 + Tailwind v4
- Testing: vitest (frontend unit), pytest (backend), Playwright (E2E)
- Orchestration: docker-compose at repo root

## Quick boot

[exact docker compose commands from CLAUDE.md — fresh stack,
migrate, bring up backend + celery, cd frontend && npm run dev]

## Running tests

- Backend (docker-network pattern — see [CLAUDE.md](CLAUDE.md)):
  [exact docker run command]
- Frontend: `cd frontend && npm run test -- --run`
- Playwright E2E: `EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test`

## Role tour

- Participant (account-less): http://localhost:5173/events
- Admin: http://localhost:5173/login → admin@e2e.example.com /
  Admin!2345 → `/admin`
- Organizer: http://localhost:5173/login → organizer@e2e.example.com /
  Organizer!2345 → `/organizer` (phone-first dashboard)

## Further reading

- [Roadmap (v1.2-prod milestone)](.planning/ROADMAP.md)
- [Manual smoke checklist](docs/smoke-checklist.md)
- [Collaboration contract](docs/COLLABORATION.md)
- [Project notes for Claude](CLAUDE.md)
```

Use the exact docker + curl commands verified in
`.github/workflows/ci.yml` and `CLAUDE.md` — no invented syntax.

**IDEAS.md sweep** — grep for `yearly` in the live file (line numbers
in research may drift). For each match outside a "this used to say X —
now it's Y" historical comment, replace `yearly` → `quarterly`. Expected
matches: the five listed in the Interfaces block above. Do not touch
any surrounding context; keep diffs minimal.

After the edit, run:
`grep -n "yearly" IDEAS.md` — expect zero results or only a deliberate
historical-context mention that doesn't claim the current cadence is
yearly.
  </action>
  <verify>
    <automated>test -s README.md &amp;&amp; [ "$(wc -l &lt; README.md)" -ge 40 ] &amp;&amp; grep -q "quarterly" README.md &amp;&amp; grep -q "smoke-checklist" README.md &amp;&amp; grep -q "ROADMAP" README.md &amp;&amp; ! grep -qi "yearly" IDEAS.md</automated>
  </verify>
  <done>README.md is a real v1.2-prod writeup (≥40 lines, covers stack/boot/tests/role tour/links); IDEAS.md has zero remaining bare "yearly" references to the CSV cadence.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md Planning harness paragraph + ROADMAP.md + STATE.md close-out</name>
  <files>CLAUDE.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**CLAUDE.md** — find the Planning harness section (around lines 67–71
at research time; locate by the "## Planning harness" header). Current
text says "Phases 0–7 are code-complete. Phase 8 (deployment) is
deferred. A product pivot ... is coming in the next milestone." — that
wording pre-dates v1.1 and v1.2-prod.

Replace with language that accurately reflects post-Phase-20 state:
- v1.0 phases 0–7 shipped (deployment Phase 8 still deferred to a
  separate milestone)
- v1.1 phases 8–13 shipped (account-less realignment)
- v1.2-prod phases 14–20 shipped (production-ready by role)
- Cross-role Playwright suite + manual smoke checklist live
- Reference docs/smoke-checklist.md for manual verification

Do not rewrite the rest of CLAUDE.md — the branch-awareness table,
stack, test pattern, Alembic conventions, CSV cadence note, and
teaching-style bullet all stay. Keep the edit surgical.

**ROADMAP.md**:
- Check the Phase 20 box: `- [ ]` → `- [x]`
- Update the Progress Table row for Phase 20 from `0/?` / Not started
  to `3/3` / Complete / today's date (2026-04-17 or later per
  `currentDate`).
- If there is a milestone-level summary line or frontmatter progress
  count, update it to reflect all 7 v1.2-prod phases complete.

**STATE.md** frontmatter + body updates:
- `status`: `executing` → `complete` (or whatever GSD convention is —
  check existing phase-close summaries for precedent)
- `progress.completed_phases`: bump to total phases shipped including
  20
- `progress.completed_plans`: bump to include 20-01 + 20-02 + 20-03
- `progress.percent`: 100
- `last_updated` / `last_activity`: today's date
- Current Position: "Phase 20 complete — v1.2-prod milestone shipped"
- Current Status: append a bullet for Phase 20 delivery
- Next Action: point at the next milestone (deployment / v1.3
  organizer polish / whatever is next on Andy's radar); if uncertain,
  write "TBD — v1.2-prod milestone closed; next milestone decision
  pending" rather than inventing one.

Anti-goal: do NOT touch any frontmatter fields not listed above; do
NOT rewrite historical sections of STATE.md.
  </action>
  <verify>
    <automated>grep -q "v1.2-prod" CLAUDE.md &amp;&amp; grep -q "smoke-checklist" CLAUDE.md &amp;&amp; grep -q "\[x\] \*\*Phase 20" .planning/ROADMAP.md &amp;&amp; grep -q "percent: 100\|complete" .planning/STATE.md</automated>
  </verify>
  <done>CLAUDE.md Planning harness paragraph reflects v1.2-prod phases shipped and points at smoke-checklist.md; ROADMAP.md Phase 20 is checked + Progress Table shows 3/3 Complete; STATE.md shows milestone-complete status and today's date.</done>
</task>

<task type="auto">
  <name>Task 3: Triage INTEG-05 cross-role bugs into 20-bugs-log.md</name>
  <files>.planning/phases/20-cross-role-integration/20-bugs-log.md</files>
  <action>
Gather every cross-role bug surfaced during Plan 20-01 (Playwright
authoring) and Plan 20-02 (manual smoke dry run). Sources:
- `.planning/phases/20-cross-role-integration/20-01-SUMMARY.md`
- `.planning/phases/20-cross-role-integration/20-02-SUMMARY.md`
- `.planning/phases/20-cross-role-integration/20-bugs-notes.md` (if
  Plan 20-02 produced one)
- Any Playwright flake output observed during scenario authoring
- Any console.error hits captured in the new spec

Create `.planning/phases/20-cross-role-integration/20-bugs-log.md` with
a table:

| Bug ID | Surface (role / route) | Symptom | Root cause (best-known) | Disposition | Follow-up |
|---|---|---|---|---|---|
| B-20-01 | ... | ... | ... | fixed / v1.3-defer / dismissed | commit sha OR v1.3 issue ref OR "dismissed because ..." |

**Dispositions — use exactly these three values:**
- `fixed` — bug was resolved by a commit in this plan. Record the
  commit sha and the files touched. Only use for small, isolated fixes
  that don't expand plan scope per the phase constraint "prefer
  defer-and-document over scope creep unless it blocks a success
  criterion."
- `v1.3-defer` — bug is real but not blocking v1.2-prod sign-off.
  Record what an issue title would be so Andy can file it later.
  Mirror the `docs/ORG-AUDIT.md` v1.3 deferral pattern.
- `dismissed` — bug turned out to be misdiagnosed / not a bug / handled
  by existing infra. Record the rationale.

If during triage any bug IS blocking (breaks a success criterion for
this or an earlier phase) AND is small enough to fix without scope
creep: fix it in this same task, atomic commit per bug, mark `fixed`.

If zero cross-role bugs surfaced during Plans 20-01 and 20-02: still
create the log file with a single row documenting the clean run — the
INTEG-05 acceptance criterion is "bugs are fixed or filed as explicit
out-of-scope", and "no bugs found" is a valid explicit outcome that
deserves a record.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-cross-role-integration/20-bugs-log.md &amp;&amp; grep -qE "fixed|v1.3-defer|dismissed|no bugs|clean run" .planning/phases/20-cross-role-integration/20-bugs-log.md</automated>
  </verify>
  <done>20-bugs-log.md exists with an explicit disposition row for every cross-role bug surfaced in 20-01 + 20-02, or a clean-run record if none were surfaced. Each row has a disposition in {fixed, v1.3-defer, dismissed} with traceable follow-up (commit sha / v1.3 issue title / rationale). INTEG-05 acceptance satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc ↔ code reality | Stale docs lie to future readers; v1.2-prod sign-off requires doc truth matches code truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Repudiation | Unlogged cross-role bugs silently shipped as v1.2-prod | mitigate | Task 3 requires explicit disposition record for every bug surfaced — `fixed`, `v1.3-defer`, or `dismissed` with rationale |
| T-20-05 | Information Disclosure | Stale "yearly CSV" / "student account" copy misleads future maintainers about current data model | mitigate | Task 1 + 2 sweep explicit; CLAUDE.md cadence rule enforces quarterly going forward |
| T-20-06 | Tampering | PR-only files edited on a role branch without Hung review | accept | Phase 20 doc-sweep commits ship in one final PR per docs/COLLABORATION.md rules; this is a process control, not an automated gate |
</threat_model>

<verification>
- `grep -rn "yearly" IDEAS.md` returns zero CSV-cadence references
- `grep -rn "student account" frontend/src backend/app` returns only
  correct/historical mentions (none claim current behaviour)
- `grep -rn "/organize[^r]" frontend/src` returns only the deliberate
  `/App.jsx` redirect catch-all
- README.md is ≥40 lines and references ROADMAP, smoke checklist,
  COLLABORATION, and CLAUDE.md
- CLAUDE.md Planning harness paragraph mentions v1.2-prod and
  smoke-checklist
- ROADMAP.md Phase 20 row shows Complete with today's date
- STATE.md shows milestone-complete status
- `.planning/phases/20-cross-role-integration/20-bugs-log.md` exists
  with explicit dispositions (INTEG-05)
- Playwright suite still green after any INTEG-05 fix:
  `EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test`
</verification>

<success_criteria>
1. README.md is a production-grade v1.2-prod writeup replacing the
   one-line placeholder (INTEG-06).
2. IDEAS.md + CLAUDE.md sweep leaves zero stale "yearly" CSV references;
   historical / correct copy preserved (INTEG-06).
3. ROADMAP.md Phase 20 checked, Progress Table row Complete; STATE.md
   reflects milestone-closed state (INTEG-06 + milestone sign-off).
4. Every cross-role bug surfaced has a disposition in 20-bugs-log.md —
   fixed / v1.3-defer / dismissed with rationale (INTEG-05).
5. Phase 20 PR explicitly coordinates PR-only file edits with Hung per
   docs/COLLABORATION.md (process criterion).
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-role-integration/20-03-SUMMARY.md`
and a phase-level `.planning/phases/20-cross-role-integration/20-SUMMARY.md`
(milestone-closing summary) capturing:
- Files edited (README, CLAUDE.md, IDEAS.md, ROADMAP, STATE)
- Bugs surfaced + dispositions (link to 20-bugs-log.md)
- Full Playwright suite green gate (from 20-01) confirmed re-run green
  post-doc-sweep
- Manual smoke pass result (from 20-02) confirmed
- Pointer to the one PR that lands all PR-only file changes per
  COLLABORATION.md contract
</output>
