---
phase: 20-cross-role-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/cross-role.spec.js
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03]
must_haves:
  truths:
    - "A single Playwright spec runs admin → participant → organizer → admin-audit end-to-end against the docker stack"
    - "≥4 new cross-role scenarios are added on top of the v1.1 baseline"
    - "The full Playwright suite (existing + new) is green in CI"
  artifacts:
    - path: "e2e/cross-role.spec.js"
      provides: "Cross-role scenarios 1–5 composing existing spec patterns"
      min_lines: 150
  key_links:
    - from: "e2e/cross-role.spec.js"
      to: "e2e/fixtures.js"
      via: "import ADMIN, ORGANIZER, ephemeralEmail, getSeed"
      pattern: "from '\\./fixtures\\.js'"
    - from: "e2e/cross-role.spec.js"
      to: "backend /api/v1/public/signups + /admin/audit-logs"
      via: "Playwright page navigation + direct fetch"
      pattern: "public/signups|audit-logs"
---

<objective>
**Branch:** Execute on `v1.2-final` (current branch). Final Phase 20 PR (including
PR-only file edits from Plan 20-03) targets `main`.

Author cross-role Playwright scenarios proving admin + organizer + participant
roles compose correctly end-to-end. Covers INTEG-01 (canonical loop),
INTEG-02 (≥4 new scenarios), INTEG-03 (full suite green in CI).

Purpose: v1.2-prod acceptance gate — the three pillars must demonstrably work
together against the docker stack, not just in isolation.

Output: one new spec file `e2e/cross-role.spec.js` containing 4–5 scenarios
that reuse existing fixtures, seed infrastructure, and login patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-cross-role-integration/20-RESEARCH.md
@CLAUDE.md
@docs/COLLABORATION.md
@playwright.config.js
@e2e/fixtures.js
@e2e/global-setup.js
@e2e/public-signup.spec.js
@e2e/organizer-check-in.spec.js
@e2e/admin-smoke.spec.js
@backend/tests/fixtures/seed_e2e.py
@backend/app/routers/test_helpers.py
@frontend/src/pages/admin/AuditLogsPage.jsx
@frontend/src/pages/organizer/OrganizerDashboard.jsx
@frontend/src/pages/OrganizerRosterPage.jsx

<interfaces>
<!-- Key contracts executors need — extracted from codebase. No exploration required. -->

From e2e/fixtures.js:
```javascript
export const ADMIN = { email: 'admin@e2e.example.com', password: 'Admin!2345' };
export const ORGANIZER = { email: 'organizer@e2e.example.com', password: 'Organizer!2345' };
export function getSeed();           // reads process.env.E2E_SEED
export function ephemeralEmail(tag); // returns collision-free email
```

Seed JSON fields (set by globalSetup via seed_e2e.py):
- `event_id`, `period_slot_id`, `orientation_slot_id`
- `confirm_token`, `a11y_confirm_token`
- `attended_volunteer_email`, `portal_slug`

Login selectors (verified in admin-smoke.spec.js + organizer-check-in.spec.js):
- Email: `#login-email`
- Password: `#login-password`
- Submit: `getByRole('button', { name: /log.?in|sign.?in/i })`

Audit logs page keyword search input (verified in admin-smoke.spec.js line 40):
- `#al-q`

Roster row selector (verified in organizer-check-in.spec.js):
- Row: `page.locator('ul li button').filter({ hasText: /confirmed|pending/i })`
- Status chip: `page.locator('ul li button span').filter({ hasText: /^checked in$/i })`

Direct-API signup pattern (verified in organizer-check-in.spec.js):
```javascript
const apiBase = process.env.E2E_BACKEND_URL || 'http://localhost:8000';
const resp = await fetch(`${apiBase}/api/v1/public/signups`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ first_name, last_name, email, phone, slot_ids: [...] }),
});
// body: { signup_ids: [...], confirm_token: '...' }  // token only with EXPOSE_TOKENS_FOR_TESTING=1
```

Confirm endpoint: `POST /api/v1/public/signups/confirm?token=...`

Route table (App.jsx, verified):
- `/login`, `/events`, `/events/:eventId`, `/signup/confirm?token=...`, `/signup/manage?token=...`
- `/admin`, `/admin/audit-logs`, `/admin/users`, `/admin/templates`, `/admin/exports`, `/admin/imports`
- `/organizer`, `/organizer/events/:id/roster`
- RBAC: `/admin/users`, `/admin/audit-logs`, `/admin/exports` are admin-only;
  `/admin/templates`, `/admin/imports`, `/admin/events/:id` are admin+organizer

CI env (from .github/workflows/ci.yml line 202, 240):
- `EXPOSE_TOKENS_FOR_TESTING=1` is set → confirm_token returned, rate limits bypassed
- `E2E_BACKEND_URL=http://localhost:8000`, `E2E_BASE_URL=http://localhost:5173`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect unknowns and author Scenario 1 (canonical cross-role loop)</name>
  <files>e2e/cross-role.spec.js</files>
  <action>
Create `e2e/cross-role.spec.js`. Before writing asserts, inspect these
unknowns (flagged as Assumptions A2, A5, A6, A7 in 20-RESEARCH.md) and pick
the right pattern — do NOT guess:

1. Read `frontend/src/pages/admin/AuditLogsPage.jsx` to confirm:
   - `#al-q` is still the keyword search input id
   - Whether filter applies on `fill()` (debounce) or requires submit/Enter
   - The exact text/kind values rendered for `signup.created` and
     `signup.checked_in` (or whatever the audit kind literals are)
2. Grep `backend/app/services/audit_service.py` (or equivalent) + the
   signup / check-in routers to confirm whether audit writes are synchronous
   or Celery-async. If async, scenarios must use `expect.poll` with a
   10–15s window rather than an immediate assertion.
3. Read `frontend/src/components/admin/AdminTopBar.jsx` (or the admin
   shell header component) to find the logout control — the exact
   selector. If there is no UI logout, fall back to
   `await page.context().clearCookies()` + reload.

Write Scenario 1 in a `test.describe.serial` block because it threads a
single volunteer email + confirm_token across three tests:

- Test A — "public participant signs up via seeded event":
  - `getSeed()`; assert `event_id` + `period_slot_id` truthy.
  - `goto('/events/' + seed.event_id)`.
  - Reuse the `clickSlotByLabel` helper pattern from
    `public-signup.spec.js` (copy, don't import — the research doc's
    "Don't Hand-Roll" table says inline, don't extract).
  - Fill identity form with `ephemeralEmail('xrole1')` +
    `VOLUNTEER_IDENTITY`.
  - `Promise.all` with `waitForResponse` on POST `/public/signups`;
    capture `confirm_token` from response body.
  - Defensive: `expect(confirmToken, 'confirm_token missing — EXPOSE_TOKENS_FOR_TESTING=1 must be set').toBeTruthy()`.
  - `goto('/signup/confirm?token=' + token)`; assert confirmed banner
    visible.

- Test B — "organizer checks them in from roster":
  - Login as ORGANIZER using the inline 5-line pattern from
    `admin-smoke.spec.js` (do NOT extract a helper — scoped PR).
  - `goto('/organizer/events/' + seed.event_id + '/roster')`.
  - Locate the row for the participant by matching the ephemeral email's
    local part OR a last-name substring; click; assert status chip flips
    to "checked in" within 8s.

- Test C — "admin sees signup.created and signup.checked_in in audit log":
  - Logout (use the selector discovered in step 3).
  - Login as ADMIN.
  - `goto('/admin/audit-logs')`; `waitForLoadState('networkidle')`.
  - Fill `#al-q` with the participant's email; submit per the pattern
    discovered in step 1.
  - Assert both the create and check-in rows are visible within 10s.
    Use `expect.poll` if step 2 determined audit writes are async.

Add the per-test `pageerror` + `console.error` capture pattern from
`public-signup.spec.js` lines 40–65 — cross-role specs must not ship with
silent console errors. Use an empty `ALLOWED_CONSOLE_PATTERNS` allowlist.

Use a fileheader comment describing the scenario, the EXPOSE_TOKENS
dependency, and the serial-describe rationale — match the style of
`public-signup.spec.js` and `organizer-check-in.spec.js`.
  </action>
  <verify>
    <automated>EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test e2e/cross-role.spec.js --project=chromium -g "Scenario 1|canonical"</automated>
  </verify>
  <done>Scenario 1 runs green on chromium against the local docker stack; spec uses inline login + clickSlotByLabel patterns (no new helper modules); audit-log assertions are either synchronous or use expect.poll per the audit-write timing determined in inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Author Scenarios 2, 3, 4 (overview stats, roster polling/appearance, cancel→audit)</name>
  <files>e2e/cross-role.spec.js</files>
  <action>
Append three independent (non-serial, parallel-safe) scenarios to
`e2e/cross-role.spec.js`. Each uses a fresh `ephemeralEmail(tag)` so they
do not collide with Scenario 1 or each other.

**Scenario 2 — Overview stats update after participant signup:**
- Login as ADMIN; `goto('/admin')`.
- Read the current "Signups (all time)" or equivalent count from
  `OverviewSection.jsx` — inspect the file first to find the exact
  selector and label; if the counter renders as e.g. `{n} students have
  signed up`, match with a regex and parse the integer.
- Logout; run the public-signup flow exactly once with
  `ephemeralEmail('xrole2')` (direct API call to
  `POST /api/v1/public/signups` + confirm via token is acceptable —
  faster than the UI path and equally valid for this scenario).
- Login as ADMIN; `goto('/admin')`; assert the count incremented by
  exactly 1. Use `expect.poll` with a 10s window if the overview is
  cached or polled on the backend.

**Scenario 3 — Organizer sees fresh signup via roster poll or reload:**
- Inspect `frontend/src/pages/OrganizerRosterPage.jsx` to confirm the 5s
  polling behaviour still works post-Phase 19 rescope (Assumption A3 in
  research). If polling is live: use a `waitFor` with 6s timeout. If
  polling turns out to be removed or fragile, assert after a manual
  `page.reload()` with a comment documenting the downgrade.
- Login as ORGANIZER; `goto('/organizer/events/' + seed.event_id + '/roster')`.
- Direct-API signup with `ephemeralEmail('xrole3')` + confirm via token
  (use the exact fetch pattern from `organizer-check-in.spec.js` lines
  28–58).
- Assert the new row appears in the roster list within the 6s polling
  window (or after one reload in the downgrade path). Match the row by
  last-name substring or email local part.

**Scenario 4 — Cancel flow surfaces in audit log:**
- Public-signup flow with `ephemeralEmail('xrole4')` → confirm via token.
- `goto('/signup/manage?token=' + token)`.
- Click the per-row Cancel button for the participant's single signup.
  Inspect `frontend/src/pages/public/ManageSignupsPage.jsx` for the
  exact button selector — do not guess.
- Confirm any modal dialog if one appears.
- Login as ADMIN; `goto('/admin/audit-logs')`; filter by the volunteer's
  email; assert both `signup.created` and `signup.canceled` (single L,
  American spelling per Assumption A7 — verify against the audit service
  before asserting the exact string; match with a regex if the actual
  literal differs).

All three scenarios must include the per-test pageerror + console.error
capture from Task 1.
  </action>
  <verify>
    <automated>EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test e2e/cross-role.spec.js --project=chromium</automated>
  </verify>
  <done>Scenarios 2, 3, 4 each run green on chromium against the local docker stack. Spec file now contains 4 cross-role scenarios (Scenario 1 serial describe + 3 independent tests). No shared helpers extracted. Ephemeral emails prevent cross-scenario collision.</done>
</task>

<task type="auto">
  <name>Task 3: Author Scenario 5 (organizer RBAC) + full-matrix green gate</name>
  <files>e2e/cross-role.spec.js</files>
  <action>
Append Scenario 5 to `e2e/cross-role.spec.js` — tests the Phase 19 RBAC
work by asserting organizer redirects/denials for admin-only pages.

First inspect `frontend/src/App.jsx` to confirm:
- `ProtectedRoute roles={...}` behaviour: does an unauthorized org user
  get a redirect, a 403 page, or an empty state?
- Exact list of `admin-only` (Users, AuditLogs, Exports) vs
  `admin+organizer` (Templates, Imports, event detail) routes.

Write the scenario as a single non-serial test:

- Login as ORGANIZER.
- For each admin-only path `/admin/users`, `/admin/audit-logs`,
  `/admin/exports`:
  - `goto(path)`; assert the page is NOT accessible per the behaviour
    observed in App.jsx inspection (either `expect(page).toHaveURL(/not path/)`,
    or expect a 403 component, or expect the admin content to NOT be
    visible).
- For each shared path `/admin/templates`, `/admin/imports`,
  `/organizer`:
  - `goto(path)`; assert the page loads (positive check — a heading or
    known selector is visible).

Pitfall reminder from research doc §Pitfall 5: do NOT invert the list —
templates/imports MUST load for organizers.

**Full-matrix gate (final verify for this plan):**
Run the entire Playwright suite — existing 5 spec files + new
cross-role.spec.js — across all 6 projects defined in
`playwright.config.js`. Count of unique test names must be ≥ (existing
baseline) + 4. If CI fails on any pre-existing spec due to cross-scenario
side effects (e.g. seed-cleanup races), either fix the new spec to clean
up after itself via `/api/v1/test/seed-cleanup` or add a
`test.afterAll` that cancels the ephemeral volunteers' signups.

Anti-goal: do NOT refactor or "tidy up" any existing spec in this plan —
scope is additive only.
  </action>
  <verify>
    <automated>EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test</automated>
  </verify>
  <done>Full Playwright suite green across all 6 projects locally against the docker stack. e2e/cross-role.spec.js contains 5 scenarios (1 serial describe + 4 independent tests) covering INTEG-01 loop, overview stats, roster poll/reload, cancel→audit, and RBAC. No existing specs modified. Ready to push for CI to confirm INTEG-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend | Playwright tests drive the same untrusted-input surfaces a real user would |
| test-only router | `EXPOSE_TOKENS_FOR_TESTING=1` gate (already implemented) must not be set in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Elevation | organizer bypassing `ProtectedRoute` to admin pages | mitigate | Scenario 5 explicitly tests RBAC redirects for /admin/users, /admin/audit-logs, /admin/exports |
| T-20-02 | Information Disclosure | confirm_token leaking in CI artifacts (traces, videos) | accept | playwright.config.js captures trace only on-first-retry and video only retain-on-failure; tokens are ephemeral + test-only per EXPOSE_TOKENS_FOR_TESTING gate |
| T-20-03 | Tampering | race between organizer check-in and participant self-check-in (first-write-wins) | accept | Out-of-scope for Phase 20 (ORG-08 deferred to v1.3); existing single-transaction check-in endpoint holds the invariant |
</threat_model>

<verification>
- `EXPOSE_TOKENS_FOR_TESTING=1 npx playwright test` green locally across all 6 projects
- `grep -c "^test\\|^test\\.describe" e2e/cross-role.spec.js` returns ≥ 5
- No new files added outside `e2e/cross-role.spec.js`
- No edits to `e2e/fixtures.js`, `playwright.config.js`, or any existing spec file
- CI (after push) runs `npx playwright test` and stays green
</verification>

<success_criteria>
1. `e2e/cross-role.spec.js` exists with ≥4 new cross-role scenarios (INTEG-02).
2. Scenario 1 drives the canonical admin-create → participant-signup →
   organizer-check-in → admin-audit-log loop (INTEG-01).
3. Full Playwright suite (existing + new) is green locally on chromium
   and on CI after push (INTEG-03).
4. No shared helper modules extracted; no edits to existing specs or
   fixtures (scope discipline).
5. All scenarios use `ephemeralEmail()` to avoid cross-run collision and
   do not require manual DB cleanup between runs.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-role-integration/20-01-SUMMARY.md`
capturing: scenarios shipped, audit-write sync/async finding, logout selector
used, RBAC behaviour observed in App.jsx, any anomalies or follow-up items
discovered during scenario authoring that should feed INTEG-05 triage in
Plan 20-03.
</output>
