---
phase: "10"
plan: "04"
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - frontend/src/App.jsx
autonomous: false
requirements:
  - REQ-10-07
  - REQ-10-08

must_haves:
  truths:
    - "App.jsx /events route points to new EventsBrowsePage"
    - "App.jsx /events/:eventId route points to new public EventDetailPage"
    - "npm run build produces no errors or warnings"
    - "Full signup flow works visually: browse week -> open event -> select slots -> fill form -> orientation modal -> submit -> success card"
    - "No path in the public flow requires login"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Final route wiring for both new public pages"
      contains: "EventDetailPage"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "pages/public/EventsBrowsePage"
      via: "import + Route"
      pattern: "EventsBrowsePage"
    - from: "frontend/src/App.jsx"
      to: "pages/public/EventDetailPage"
      via: "import + Route"
      pattern: "EventDetailPage"
---

<objective>
Final route wiring in App.jsx (ensure both /events and /events/:eventId point to the new public pages) + build verification + visual smoke test of the full signup flow.

Purpose: Confirm the complete Phase 10 deliverable works end-to-end before writing the SUMMARY.
Output: Verified working build, visual confirmation of the signup flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-public-events-by-week-browse-signup-form/10-01-SUMMARY.md
@.planning/phases/10-public-events-by-week-browse-signup-form/10-02-SUMMARY.md
@.planning/phases/10-public-events-by-week-browse-signup-form/10-03-SUMMARY.md
@frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final route wiring + build verification</name>
  <files>frontend/src/App.jsx</files>
  <action>
Ensure App.jsx has the final correct imports and routes per D-05:

1. Update the /events route to import from `./pages/public/EventsBrowsePage` (Plan 02 may have done this; confirm it's correct)
2. Update the /events/:eventId route to import from `./pages/public/EventDetailPage` (Plan 03 created this page)
3. Keep old EventsPage.jsx and EventDetailPage.jsx imports REMOVED from App.jsx (the files stay on disk for Phase 12, just not imported)
4. Verify no ProtectedRoute wrapper on either public route per REQ-10-07
5. Run `npm run build` and confirm zero errors/warnings
6. Run the full vitest suite: `npm run test -- --run` and confirm all new tests pass alongside existing tests

If Plan 02 already wired /events but left /events/:eventId pointing to the old page with a comment, update it now to point to the new public EventDetailPage.
  </action>
  <verify>
    <automated>cd /Users/andysubramanian/uni-volunteer-scheduler/frontend && npm run build 2>&1 | tail -10 && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>Both /events and /events/:eventId point to new public pages. Build succeeds with no errors. All vitest tests pass (existing + new).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual smoke test of full public signup flow</name>
  <action>
Human verifies the complete Phase 10 UI on a mobile viewport. This confirms that the automated tests align with visual/functional reality.

What was built: Complete public browse + signup flow (Phase 10) -- week navigation on /events, event detail with slot checkboxes, identity form, orientation warning modal, success popup card, error handling for rate limits and capacity.

How to verify:
1. Start the dev server: `cd frontend && npm run dev`
2. Open http://localhost:5173/events in Chrome DevTools with device toolbar set to iPhone SE (375px width)
3. Verify the week navigation: prev/next arrows change the week, "This week" resets. URL updates with ?quarter=...&year=...&week=... params.
4. If no events exist for this week, verify the empty state shows "No events this week"
5. If events exist: click an event card to open /events/:eventId
6. On the detail page: verify slots are shown as cards with checkboxes, grouped by slot_type (Orientation / Period)
7. Check 1+ slot checkboxes. Verify the identity form appears (first_name, last_name, email, phone fields)
8. Fill in identity fields. Click "Sign up"
9. If you selected only a period slot: verify the orientation modal appears asking about prior attendance. Click "Yes" to proceed or "No" to return with orientation slots highlighted.
10. After successful submission: verify the success popup card shows "Check your email!", your name, and the slots you signed up for.
11. Click "Done" on the success card. Verify form resets to browse state.
12. Verify all touch targets are at least 44px (slot cards, buttons, form inputs).
13. Verify no bottom nav appears for logged-out users (expected -- Layout hides it).

NOTE: If the backend is not running or no seed data exists, the browse page will show loading then empty/error state. That is acceptable for visual smoke -- the important thing is that the UI renders correctly and handles states gracefully. The component tests cover the data-driven paths.
  </action>
  <verify>Human approval</verify>
  <done>Human confirms the full signup flow works visually on mobile viewport. Type "approved" or describe any visual/functional issues to fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this plan. Final wiring and verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | T (Tampering) | Route wiring | accept | Public routes are intentionally unauthenticated. No ProtectedRoute wrapper. This is by design per the account-less pivot. |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npx vitest run` all tests pass
- App.jsx imports EventsBrowsePage and EventDetailPage from pages/public/
- Old page files exist on disk but are not imported in App.jsx
- Visual smoke confirms the full flow works on mobile viewport
</verification>

<success_criteria>
- Build passes with no errors
- All vitest tests (new + existing) pass
- Visual smoke confirms: week nav, event cards, slot checkboxes, identity form, orientation modal, success card, error states
- No auth required for any public route
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-public-events-by-week-browse-signup-form/10-04-SUMMARY.md`
</output>
