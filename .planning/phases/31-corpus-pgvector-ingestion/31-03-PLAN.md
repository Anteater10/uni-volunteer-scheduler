---
phase: 31
plan: 03
type: execute
wave: 2
depends_on: [31-02]
files_modified:
  - backend/app/corpus/__init__.py
  - backend/app/corpus/chunker.py
  - backend/app/corpus/walker.py
  - backend/tests/test_corpus_chunker.py
  - backend/tests/test_corpus_walker.py
autonomous: true
requirements:
  - REQ-31-03
  - REQ-31-04
  - REQ-31-05
  - REQ-31-06
  - REQ-31-07
must_haves:
  truths:
    - "`chunk_text(t)` is deterministic — same bytes in, same chunks out, every time"
    - "Chunks preserve `char_start`/`char_end` such that `text[start:end] == chunk.content` for the trivial single-chunk case, and the concatenation of unique (non-overlap) segments equals the input"
    - "`walk_sources(root)` returns a deterministically-ordered list of `SourceDocument` records over the allow-list, excluding deny-list paths"
    - "The walker opens zero DB connections and reads zero JSON/CSV — verified by monkeypatching `app.database` to raise"
  artifacts:
    - path: "backend/app/corpus/chunker.py"
      provides: "deterministic recursive char splitter, no LangChain dep"
      contains: "CHUNKER_VERSION = \"v1-recursive-char-1024-128\""
      exports: ["chunk_text", "CHUNKER_VERSION", "CHUNK_SIZE", "CHUNK_OVERLAP", "Chunk"]
    - path: "backend/app/corpus/walker.py"
      provides: "allow-list source walker (markdown + python docstrings + alembic + frontend leading comments)"
      exports: ["walk_sources", "SOURCE_GLOBS_V1", "DENY_LIST", "SourceDocument"]
    - path: "backend/app/corpus/__init__.py"
      provides: "package marker; re-exports public chunker + walker symbols"
  key_links:
    - from: "chunker.chunk_text"
      to: "test_chunker_deterministic, test_chunker_offsets_consistent, test_chunker_version_constant"
      via: "imports + assertions on returned Chunk dataclass fields"
      pattern: "from app.corpus.chunker import chunk_text"
    - from: "walker.walk_sources"
      to: "test_walker_deterministic_order, test_walker_respects_deny_list, test_walker_opens_no_db_connection"
      via: "imports + iteration over yielded SourceDocument records"
      pattern: "from app.corpus.walker import walk_sources"
---

<objective>
Build the two pure-Python building blocks of the ingest pipeline: a deterministic recursive character chunker and an allow-list source walker. Both are DB-free, both are 100%-covered by the Wave 0 tests once we flip the xfail markers. Plan 04 composes them into the ingest CLI.

Purpose: chunker + walker are the two pieces of the corpus pipeline that have no external dependencies (no DB, no network), so they can be authored, tested, and locked in before we wire embeddings or the orchestrator. This is the smallest reversible step that produces a measurable behavior change.

Output:
- `backend/app/corpus/` package with `chunker.py` + `walker.py` + `__init__.py`.
- 6 chunker tests (3 from plan 01 + 3 new boundary tests) — all green.
- 3 walker tests (from plan 01) — all green; plus a fourth that exercises real repo markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/31-corpus-pgvector-ingestion/31-RESEARCH.md
@backend/tests/test_corpus_chunker.py
@backend/tests/test_corpus_walker.py
@backend/tests/conftest.py
@backend/app/copilot/prompts.py

<interfaces>
<!-- chunker.py public surface -->
```python
# backend/app/corpus/chunker.py
from dataclasses import dataclass

CHUNKER_VERSION = "v1-recursive-char-1024-128"
CHUNK_SIZE = 1024
CHUNK_OVERLAP = 128
SEPARATORS = ["\n\n", "\n", ". ", " ", ""]

@dataclass(frozen=True)
class Chunk:
    content: str
    char_start: int
    char_end: int

def chunk_text(
    text: str,
    *,
    chunk_size: int = CHUNK_SIZE,
    chunk_overlap: int = CHUNK_OVERLAP,
    separators: list[str] = SEPARATORS,
) -> list[Chunk]: ...
```

<!-- walker.py public surface -->
```python
# backend/app/corpus/walker.py
from dataclasses import dataclass
from pathlib import Path

@dataclass(frozen=True)
class SourceDocument:
    source_path: str   # repo-relative, POSIX
    source_kind: str   # 'markdown' | 'python_module' | 'python_function' | 'alembic_migration' | 'frontend_component'
    title: str | None
    content: str
    byte_size: int

SOURCE_GLOBS_V1: list[str] = [...]  # RESEARCH §Step 3
DENY_LIST: list[str] = [...]        # RESEARCH §Step 3

def walk_sources(*, root: Path) -> list[SourceDocument]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic recursive char chunker (chunker.py) + flip + extend chunker tests</name>
  <files>backend/app/corpus/__init__.py, backend/app/corpus/chunker.py, backend/tests/test_corpus_chunker.py</files>
  <read_first>
    - .planning/phases/31-corpus-pgvector-ingestion/31-RESEARCH.md (§Step 4 Chunker + Pitfall 4 + Risks R4)
    - backend/tests/test_corpus_chunker.py (the 3 xfail stubs from plan 01)
  </read_first>
  <behavior>
    - `test_chunker_deterministic` (flip): two calls on same input return equal lists.
    - `test_chunker_offsets_consistent` (flip): `chunk_text("a"*1024)` returns one chunk with `char_start=0, char_end=1024`.
    - `test_chunker_version_constant` (flip): `CHUNKER_VERSION == "v1-recursive-char-1024-128"`.
    - NEW `test_chunker_splits_on_double_newline_first`: input `"para1\n\npara2"` with `chunk_size=10` produces ≥ 2 chunks split at the `\n\n` boundary, not mid-word.
    - NEW `test_chunker_preserves_substring_invariant`: for every chunk, `text[chunk.char_start:chunk.char_end] == chunk.content`.
    - NEW `test_chunker_overlap_is_chunk_overlap_chars`: adjacent chunks for a long flat input (`"x" * 3000`, `chunk_size=1024, chunk_overlap=128`) overlap by exactly `CHUNK_OVERLAP` characters (chunk[i+1].char_start == chunk[i].char_end - CHUNK_OVERLAP).
  </behavior>
  <action>
    1. Create `backend/app/corpus/__init__.py` as an empty package marker (one-line docstring fine).

    2. Create `backend/app/corpus/chunker.py` implementing the interface in the `<interfaces>` block above. Algorithm (RESEARCH §Step 4):
       - Try separators in order; on each separator, split the input. If any resulting piece is still > `chunk_size`, recurse into it with the next separator.
       - Once every piece is ≤ `chunk_size`, glue adjacent pieces together greedily into windows of ≤ `chunk_size`, then introduce `chunk_overlap` characters of overlap by starting each subsequent window `chunk_size - chunk_overlap` chars after the previous window's start.
       - Track `char_start`/`char_end` as ABSOLUTE offsets into the original input (not the intermediate split positions). This is the load-bearing invariant for Phase 32 citations (RESEARCH §Step 7 hook #3).
       - No randomness, no `time.*`, no env reads, no I/O. Pure function.

    3. In `backend/tests/test_corpus_chunker.py`:
       - Remove all three `@pytest.mark.xfail(...)` decorators.
       - Add the three NEW tests listed in `<behavior>` above.

    4. Run:
       ```
       docker run --rm --network uni-volunteer-scheduler_default \
         -v $PWD/backend:/app -w /app \
         -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" \
         uni-volunteer-scheduler-backend \
         sh -c "pytest -q backend/tests/test_corpus_chunker.py --cov=app.corpus.chunker --cov-report=term-missing"
       ```
       Expected: 6 passed, coverage on `app.corpus.chunker` == 100%.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q backend/tests/test_corpus_chunker.py --cov=app.corpus.chunker --cov-fail-under=100"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/corpus/__init__.py` exists
    - `backend/app/corpus/chunker.py` contains `def chunk_text(`
    - `backend/app/corpus/chunker.py` contains `CHUNKER_VERSION = "v1-recursive-char-1024-128"`
    - `backend/app/corpus/chunker.py` contains `@dataclass` decorator (for Chunk)
    - `grep -c "@pytest.mark.xfail" backend/tests/test_corpus_chunker.py` == 0
    - `pytest -q backend/tests/test_corpus_chunker.py` exits 0 with 6 tests passed
    - Coverage on `app.corpus.chunker` == 100% (matches Phase 30 invariant for `app.copilot.*`)
    - `python -c "from app.corpus.chunker import chunk_text, Chunk, CHUNKER_VERSION, CHUNK_SIZE, CHUNK_OVERLAP"` succeeds
  </acceptance_criteria>
  <done>Chunker is deterministic, offset-preserving, version-pinned; 6 tests green; 100% coverage on `app.corpus.chunker`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement allow-list source walker (walker.py) + flip + extend walker tests</name>
  <files>backend/app/corpus/walker.py, backend/tests/test_corpus_walker.py</files>
  <read_first>
    - .planning/phases/31-corpus-pgvector-ingestion/31-RESEARCH.md (§Step 3 Source-set walker, Pitfalls 6 + 7)
    - backend/tests/test_corpus_walker.py (the 3 xfail stubs from plan 01)
    - backend/tests/conftest.py (`tiny_markdown_corpus` fixture)
  </read_first>
  <behavior>
    - `test_walker_deterministic_order` (flip): two `walk_sources` calls on the same root return identically-ordered `source_path` lists.
    - `test_walker_respects_deny_list` (flip): `node_modules/`, `test_*` files NOT in output.
    - `test_walker_opens_no_db_connection` (flip): monkeypatching `app.database.SessionLocal` to raise does NOT cause `walk_sources` to fail.
    - NEW `test_walker_extracts_python_docstrings_not_bodies`: given a fixture `foo.py` containing a function with a docstring and code, the walker yields one `SourceDocument(source_kind='python_function', content=<the docstring>)` and the body is NOT in `content`.
    - NEW `test_walker_lf_normalizes_line_endings`: a CRLF-encoded markdown file produces the same `byte_size` and same content hash as the LF-encoded version (the walker normalises before hashing — RESEARCH §Step 3 "Determinism").
  </behavior>
  <action>
    1. Create `backend/app/corpus/walker.py` implementing the interface in `<interfaces>`. Behavior:
       - Hardcode `SOURCE_GLOBS_V1` and `DENY_LIST` exactly per RESEARCH §Step 3.
       - For markdown (`*.md`): read file, normalize CRLF→LF + strip per-line trailing whitespace, yield one `SourceDocument(source_kind='markdown', title=<first H1 or None>, content=<normalised>, byte_size=len(content.encode()))`.
       - For alembic versions (`backend/alembic/versions/*.py`): yield one `SourceDocument(source_kind='alembic_migration', title=<revision string>, content=<the module docstring only>)`. Use `ast.parse(...).body[0]` and `ast.get_docstring`.
       - For backend python (`backend/app/**/*.py`): use `ast.parse` then walk `Module`, `ClassDef`, `FunctionDef`, `AsyncFunctionDef` and emit ONE `SourceDocument` per node that has a docstring (`ast.get_docstring(node)` non-None). `source_kind='python_module'` for module, `'python_function'` for the rest. Skip bodies.
       - For frontend (`frontend/src/**/*.{jsx,js,ts,tsx}`): read leading block comment only (lines until first non-comment/non-blank). Skip if absent. `source_kind='frontend_component'`.
       - For each yielded `SourceDocument`, set `source_path` to `path.relative_to(root).as_posix()`.
       - Sort the output list lexicographically by `source_path` before returning.
       - Apply `DENY_LIST` via `fnmatch.fnmatch` or pathlib `match`; reject any path where any deny pattern matches.
       - Open zero DB connections, read zero JSON/CSV. The function takes `root: Path` only — no session, no engine, no Settings.

    2. In `backend/tests/test_corpus_walker.py`:
       - Remove three `@pytest.mark.xfail` decorators.
       - Add NEW tests `test_walker_extracts_python_docstrings_not_bodies` (use a tmp_path with one tiny `.py` file) and `test_walker_lf_normalizes_line_endings` (write the same content twice, once CRLF once LF; assert equal `byte_size` and equal `hashlib.sha256(content.encode()).hexdigest()`).

    3. Run:
       ```
       docker run --rm --network uni-volunteer-scheduler_default \
         -v $PWD/backend:/app -w /app \
         -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" \
         uni-volunteer-scheduler-backend \
         sh -c "pytest -q backend/tests/test_corpus_walker.py --cov=app.corpus.walker --cov-report=term-missing"
       ```
       Expected: 5 passed, coverage on `app.corpus.walker` == 100%.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q backend/tests/test_corpus_walker.py --cov=app.corpus.walker --cov-fail-under=100"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/corpus/walker.py` exists; contains `def walk_sources(`
    - `backend/app/corpus/walker.py` contains `SOURCE_GLOBS_V1 = [` and `DENY_LIST = [`
    - `backend/app/corpus/walker.py` contains `import ast` (for Python docstring extraction)
    - `backend/app/corpus/walker.py` does NOT contain `from sqlalchemy`, `Session`, or `engine` (no DB access)
    - `grep -c "@pytest.mark.xfail" backend/tests/test_corpus_walker.py` == 0
    - `pytest -q backend/tests/test_corpus_walker.py` exits 0 with 5 tests passed
    - Coverage on `app.corpus.walker` == 100%
    - `python -c "from app.corpus.walker import walk_sources, SOURCE_GLOBS_V1, DENY_LIST, SourceDocument"` succeeds
  </acceptance_criteria>
  <done>Walker is deterministic, opens no DB, respects deny-list, extracts docstrings without bodies; 5 tests green; 100% coverage on `app.corpus.walker`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → in-memory corpus | The walker reads bytes from disk. The repo is trusted, but the walker is path-restricted by `SOURCE_GLOBS_V1` and `DENY_LIST` to prevent accidental ingestion of PII fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information disclosure | Walker over PII fixtures | mitigate | `DENY_LIST` excludes `test_*`, `__tests__/`, `*.test.*`, `.env*`; walker reads no JSON/CSV (only `.md`, `.py`, `.jsx/ts/tsx` leading comments). Covered by `test_walker_respects_deny_list`. |
| T-31-04 | Tampering | Non-determinism in chunker breaks paper reproducibility | mitigate | `test_chunker_deterministic` asserts byte-equality across two calls. No random seeds, no time-deps in chunker code (grep-verifiable). |
</threat_model>

<verification>
- `pytest -q backend/tests/test_corpus_chunker.py backend/tests/test_corpus_walker.py` exits 0
- Combined coverage on `app.corpus.chunker` + `app.corpus.walker` == 100%
- Existing tests (copilot, migration round-trip) still green
</verification>

<success_criteria>
Plan 04 can `from app.corpus.chunker import chunk_text` and `from app.corpus.walker import walk_sources` and compose them into the ingest orchestrator without further chunker/walker changes.
</success_criteria>

<output>
Create `.planning/phases/31-corpus-pgvector-ingestion/31-03-SUMMARY.md` documenting:
- Final coverage percentages on the two modules
- One representative chunk dump from a real `docs/learning/30-*/*.md` file
- Walker dry-run count (files yielded for the real repo root)
</output>
