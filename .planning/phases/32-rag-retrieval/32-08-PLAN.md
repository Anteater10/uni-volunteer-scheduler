---
phase: 32-rag-retrieval
plan: 08
type: execute
wave: 5
depends_on: [32-01, 32-02, 32-03, 32-04, 32-05, 32-06]
files_modified:
  - backend/.coveragerc
  - backend/pyproject.toml
  - backend/tests/test_coverage_gates.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-32-09]
must_haves:
  truths:
    - "Per-package coverage gate enforces ≥ 95% line + ≥ 95% branch on `app.copilot.*` AND `app.copilot.retrieval.*` AND `app.corpus.*`."
    - "`pytest --cov-fail-under` is invoked per-package in CI, not blended across the repo (Phase 31-followup pattern)."
    - "Coverage gate test asserts the gate file values (regression guard against accidental loosening)."
    - "The full pytest suite (backend + retrieval + copilot router + citations endpoint) is green."
  artifacts:
    - path: "backend/.coveragerc"
      provides: "Branch coverage on; per-package gates"
    - path: "backend/tests/test_coverage_gates.py"
      provides: "Regression test pinning the per-package thresholds"
  key_links:
    - from: "CI"
      to: "per-package coverage gates"
      via: "pytest --cov=app.copilot --cov=app.copilot.retrieval --cov=app.corpus --cov-branch --cov-fail-under=95 (per package)"
      pattern: "cov-fail-under"
---

<objective>
Lock in the Phase 31-followup coverage discipline for Phase 32's new modules. Update
`.coveragerc` and CI to run per-package gates including the new
`app.copilot.retrieval.*` namespace, and add a regression test pinning the threshold values
so a future loosening of the gate produces a failing test, not a silently-degraded gate.

Purpose: Honor the Phase 31 SUMMARY handoff hard rule (100% on `app.copilot.*` and `app.corpus.*`).
Output: .coveragerc update, CI workflow update, regression test, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-corpus-pgvector-ingestion/31-SUMMARY.md
@backend/.coveragerc
@backend/pyproject.toml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .coveragerc + CI workflow with retrieval namespace</name>
  <files>backend/.coveragerc, .github/workflows/ci.yml</files>
  <action>
    Update `.coveragerc` `[run]` section to include `app/copilot/retrieval/*` if it isn't already covered by the wildcard.

    Update `.github/workflows/ci.yml` to run THREE per-package coverage commands (mirroring the Phase 31-followup commit `75f1145` pattern — critical-path gate must read line ratio per package, not blended):
    ```yaml
    - name: Coverage gate — app.copilot
      run: pytest --cov=app.copilot --cov-branch --cov-fail-under=95 --cov-report=term-missing tests/
    - name: Coverage gate — app.copilot.retrieval
      run: pytest --cov=app.copilot.retrieval --cov-branch --cov-fail-under=95 --cov-report=term-missing tests/
    - name: Coverage gate — app.corpus
      run: pytest --cov=app.corpus --cov-branch --cov-fail-under=95 --cov-report=term-missing tests/
    ```
    (Use `--cov-fail-under=95` to match the Phase 31-followup line-ratio convention. The 100% aspirational target lives in the regression test below.)
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest --cov=app.copilot --cov=app.copilot.retrieval --cov=app.corpus --cov-branch --cov-fail-under=95"</automated>
  </verify>
  <done>All three coverage commands exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test pinning per-package gate thresholds</name>
  <files>backend/tests/test_coverage_gates.py</files>
  <behavior>
    - Parse `.github/workflows/ci.yml`; assert each of the three `--cov=app.copilot`, `--cov=app.copilot.retrieval`, `--cov=app.corpus` invocations carries `--cov-fail-under=95` (no lower).
    - Assert `.coveragerc` has `branch = True`.
    - Assert `pyproject.toml` `[tool.pytest.ini_options]` (if used) is consistent.
    - Assert all three namespaces have at least one test file present (`test_corpus_*.py`, `test_copilot_*.py`, `test_retrieval_*.py`).
  </behavior>
  <action>
    Parse YAML with `pyyaml` (already in requirements). The test is metadata-only — no DB, fast. Used as a self-check tripwire so a future PR that loosens the gate fails CI loudly.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_coverage_gates.py"</automated>
  </verify>
  <done>Test green. A future loosening to `--cov-fail-under=90` would fail this test.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression run</name>
  <files>(no new files — verification only)</files>
  <action>
    Run the full backend test suite + the frontend vitest suite. Address any incidental breakages introduced by Phase 32 wiring. If the Playwright matrix is in a separate CI job, ensure it is included.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q" && cd frontend && npm run test -- --run</automated>
  </verify>
  <done>Backend pytest + frontend vitest both green. CI workflow green on the feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI → main branch | Coverage gate is the contract that prevents quality regressions on merge. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-08-01 | Tampering | future PR silently lowers --cov-fail-under | mitigate | Regression test (Task 2) reads the YAML and asserts the threshold; loosening fails CI. |
</threat_model>

<verification>
- Three per-package coverage commands green.
- Regression test green.
- Full suite green.
</verification>

<success_criteria>
- All must_haves truths observable in CI logs.
- Per-package gates enforced (not blended).
</success_criteria>

<output>
After completion, create `.planning/phases/32-rag-retrieval/32-08-SUMMARY.md` with the coverage numbers per package and the CI run URL.
</output>
