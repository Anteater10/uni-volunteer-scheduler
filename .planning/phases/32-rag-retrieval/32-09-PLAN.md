---
phase: 32-rag-retrieval
plan: 09
type: execute
wave: 6
depends_on: [32-01, 32-02, 32-03, 32-04, 32-05, 32-06, 32-07, 32-08]
files_modified:
  - .planning/phases/32-rag-retrieval/32-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - docs/learning/32-rag-retrieval/00-phase-overview.md
  - docs/documentation/32-rag-retrieval/00-phase-overview.md
autonomous: false
requirements: [REQ-32-10, REQ-32-J]
must_haves:
  truths:
    - "Phase SUMMARY.md exists and references every plan's individual SUMMARY."
    - "STATE.md updated: Phase 32 moved from in-progress to shipped; next phase noted."
    - "ROADMAP.md Phase 32 row marked complete."
    - "Phase-overview lecture + publication writeup tie together the seven topic lectures."
    - "Smoke run against the live compose stack proves: meta event arrives before first token, chip click opens panel, rerank-lift figure is on disk."
  artifacts:
    - path: ".planning/phases/32-rag-retrieval/32-SUMMARY.md"
      provides: "Phase 32 shipping summary (definition of done + measurements)"
      min_lines: 100
    - path: "docs/learning/32-rag-retrieval/00-phase-overview.md"
      provides: "Phase index lecture linking the 7 topic lectures"
      min_lines: 80
    - path: "docs/documentation/32-rag-retrieval/00-phase-overview.md"
      provides: "Phase index publication writeup"
      min_lines: 80
  key_links:
    - from: ".planning/STATE.md"
      to: "Phase 32 status"
      via: "shipped marker + handoff notes for Phase 33"
      pattern: "32.*shipped"
---

<objective>
Close Phase 32. Produce the shipping SUMMARY with measurements (latencies, coverage, rerank-lift numbers), refresh STATE.md + ROADMAP.md, and write the two-folder phase-overview writeups that index the seven topic lectures from Plans 01-07.

Purpose: Documented, reproducible handoff to Phase 33 (tool calling / ReAct loop).
Output: Phase SUMMARY, STATE refresh, ROADMAP update, two phase-overview writeups, smoke transcript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-corpus-pgvector-ingestion/31-SUMMARY.md
@.planning/phases/32-rag-retrieval/32-01-PLAN.md
@.planning/phases/32-rag-retrieval/32-02-PLAN.md
@.planning/phases/32-rag-retrieval/32-03-PLAN.md
@.planning/phases/32-rag-retrieval/32-04-PLAN.md
@.planning/phases/32-rag-retrieval/32-05-PLAN.md
@.planning/phases/32-rag-retrieval/32-06-PLAN.md
@.planning/phases/32-rag-retrieval/32-07-PLAN.md
@.planning/phases/32-rag-retrieval/32-08-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-stack smoke (3-window pass, ~15 min)</name>
  <what-built>Phase 32 end-to-end pipeline.</what-built>
  <how-to-verify>
    1. Bring up the compose stack (`docker compose up`), make sure `corpus_chunks` has the `fts` column.
    2. Log in as admin in browser. Open the copilot drawer.
    3. Ask: "How does the magic-link login work?"
    4. Observe (curl -N or browser DevTools Network → EventStream): the `event: meta` arrives before the first `event: token`.
    5. Observe 5 chips appear under the assistant message.
    6. Click chip 1 → side panel opens, shows source_path, char range, full content. If `corpus_source_origin_url` is set, the "Open source" button works.
    7. Close panel; send a second question; observe chips replaced (not appended).
    8. Verify on-disk: `docs/documentation/32-rag-retrieval/rerank-lift.csv` exists and the PNG is non-empty.
    9. Run `pytest -q` in the backend container; full suite green. Coverage gates green per Plan 08.
  </how-to-verify>
  <resume-signal>Reply "smoke passed" with the measured latencies (retrieval_latency_ms, rerank_latency_ms from the meta event) OR describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Phase SUMMARY.md</name>
  <files>.planning/phases/32-rag-retrieval/32-SUMMARY.md</files>
  <action>
    Mirror the Phase 31 SUMMARY shape (sections: What shipped / Backend / Tests / Documentation / Locked decisions / End-to-end smoke / Definition of Done / Known limitations / Files changed / Handoff to Phase 33).

    Must include:
    - REQ-32-01 through REQ-32-10 + REQ-32-J ticked off with the artifact that closes each.
    - Measured numbers from the smoke (retrieval_latency_ms, rerank_latency_ms, p50 + p95).
    - Rerank-lift figures: mean ± stdev across the 3 RAGAS repeats for each metric.
    - Per-package coverage numbers from CI.
    - Handoff to Phase 33: what tool-calling can assume (citation contract, meta event, retrieval modules importable, per-provider invariant respected).
    - Known limitations: citations vs generation grounding (Pitfall 7); HNSW ef_search tuning still deferred to Phase 35.
    - File ≥ 100 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-rag-retrieval/32-SUMMARY.md && wc -l .planning/phases/32-rag-retrieval/32-SUMMARY.md | awk '$1 < 100 {exit 1}'</automated>
  </verify>
  <done>SUMMARY exists, ≥ 100 lines, all requirements ticked.</done>
</task>

<task type="auto">
  <name>Task 3: STATE.md + ROADMAP.md refresh</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    `STATE.md`: move Phase 32 to "shipped"; set next phase to 33 (tool calling). Record any decisions Phase 32 made that future phases must respect (locked decision list).

    `ROADMAP.md`: mark Phase 32 row complete; if there is a checkbox or status field, flip it. Do NOT alter Phase 33+ scope here.
  </action>
  <verify>
    <automated>grep -q "32.*shipped" .planning/STATE.md && grep -q "Phase 32" .planning/ROADMAP.md</automated>
  </verify>
  <done>Both files reflect Phase 32 as complete; next-phase pointer updated.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-overview lecture + publication writeup (index, two-folder)</name>
  <files>docs/learning/32-rag-retrieval/00-phase-overview.md, docs/documentation/32-rag-retrieval/00-phase-overview.md</files>
  <action>
    Both ≥ 80 lines.

    **Learning index** (`00-phase-overview.md` under learning/) — narrative tour of the seven topic lectures:
    1. FTS in Postgres (Plan 01)
    2. Hybrid retrieval + RRF (Plan 02)
    3. Cross-encoder rerank (Plan 03)
    4. SSE meta event (Plan 04)
    5. Citations endpoint (Plan 05)
    6. Citation chips frontend (Plan 06)
    7. RAGAS methodology (Plan 07)

    Each section is 2-3 paragraphs + a link to the topic lecture. Closes with "what Phase 33 builds on this".

    **Documentation index** (`00-phase-overview.md` under documentation/) — same shape, publication tone, with the citation-ready references collated into one bibliography at the bottom.
  </action>
  <verify>
    <automated>wc -l docs/learning/32-rag-retrieval/00-phase-overview.md docs/documentation/32-rag-retrieval/00-phase-overview.md | awk 'NR<=2 && $1<80 {exit 1}'</automated>
  </verify>
  <done>Both index files exist, ≥ 80 lines, paired, linking the 7 topic lectures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| (none new) | Phase closure is documentation-only; no new runtime surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-09-01 | Repudiation | undocumented locked decisions break Phase 33 | mitigate | SUMMARY + STATE explicitly list locked decisions (per-provider invariant, additive migrations, local-only rerank). |
</threat_model>

<verification>
- Live smoke passes (Task 1).
- SUMMARY ≥ 100 lines.
- STATE + ROADMAP reflect shipped.
- Two phase-overview writeups exist, paired, ≥ 80 lines.
- Full pytest + vitest + coverage gates green on the feature branch.
</verification>

<success_criteria>
- All must_haves truths observable.
- Phase 32 closed; Phase 33 can begin.
</success_criteria>

<output>
After completion, the feature branch is ready for PR to `main` (per CLAUDE.md collaboration contract — Andy owns admin/organizer/v1.4 work, no Hung overlap).
</output>
