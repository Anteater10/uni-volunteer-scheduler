---
phase: 32-rag-retrieval
plan: 04
type: execute
wave: 3
depends_on: [32-02, 32-03]
files_modified:
  - backend/app/copilot/router.py
  - backend/app/copilot/prompts.py
  - backend/app/copilot/schemas.py
  - backend/app/copilot/llm.py
  - backend/tests/test_copilot_router_with_retrieval.py
  - backend/tests/test_copilot_router_retrieval_errors.py
  - backend/tests/test_copilot_llm_blocking.py
  - backend/tests/fixtures/phase_30_system_prompt.txt
  - docs/learning/32-rag-retrieval/04-sse-meta-event.md
  - docs/documentation/32-rag-retrieval/04-sse-meta-event.md
autonomous: true
requirements: [REQ-32-05, REQ-32-06, REQ-32-09, REQ-32-10, REQ-32-J]
must_haves:
  truths:
    - "Copilot router calls `hybrid_search` → `rerank` → `chunks_to_citations` BEFORE the first LLM token."
    - "The Phase 30 placeholder context in the system prompt is replaced by the rerank-top-5 chunks."
    - "SSE emits exactly one `event: meta` carrying citations before the first `event: token`."
    - "Existing `event: token`, `event: done`, `event: error` shapes are unchanged (Phase 30 invariant)."
    - "Retrieval/rerank failures degrade gracefully: copilot still streams an answer without citations, an `event: error` is NOT emitted for retrieval miss."
    - "`retrieval_latency_ms` and `rerank_latency_ms` are populated in the meta payload."
    - "The full Phase 30 system prompt (persona / refusal / role-differentiation) is preserved verbatim as a substring of the new prompt."
    - "`app.copilot.llm.stream_completion_blocking(messages, system_prompt) -> str` exists and returns the accumulated text from the same OpenRouter code path as `stream_completion`."
  artifacts:
    - path: "backend/app/copilot/router.py"
      provides: "Retrieval-augmented streaming endpoint"
    - path: "backend/app/copilot/prompts.py"
      provides: "Prompt template with <retrieved_context> block appended to preserved Phase 30 prompt"
    - path: "backend/app/copilot/schemas.py"
      provides: "MetaEvent schema"
      exports: ["MetaEvent"]
    - path: "backend/app/copilot/llm.py"
      provides: "Streaming + non-streaming OpenRouter callers"
      exports: ["stream_completion", "stream_completion_blocking"]
    - path: "backend/tests/fixtures/phase_30_system_prompt.txt"
      provides: "Frozen Phase 30 prompt baseline (committed before edit) — used by preservation test"
  key_links:
    - from: "router stream"
      to: "meta event"
      via: "yield f'event: meta\\ndata: {payload}\\n\\n' before first token"
      pattern: "event:\\s*meta"
    - from: "system prompt"
      to: "retrieved chunks"
      via: "<retrieved_context> block appended to Phase 30 baseline"
      pattern: "retrieved_context"
    - from: "scripts/eval_rerank_lift.py"
      to: "app.copilot.llm.stream_completion_blocking"
      via: "non-streaming OpenRouter caller used by offline RAGAS harness"
      pattern: "stream_completion_blocking"
---

<objective>
Wire the retrieval layer into the copilot router. Append a `<retrieved_context>` block to
the EXISTING Phase 30 system prompt (do not modify the load-bearing persona / refusal /
role-differentiation scaffolding) and emit a single new `event: meta` carrying the
citation payload BEFORE the first token. Also add a non-streaming `stream_completion_blocking`
variant to `app/copilot/llm.py` so the Plan 07 RAGAS harness has a real function to import.
The Phase 30 SSE taxonomy (`token` / `done` / `error`) is preserved as an invariant — `meta`
is strictly additive.

Purpose: Make the chat actually grounded in retrieved docs; produce the wire-format Plan 06 will render; expose the blocking LLM caller Plan 07 depends on.
Output: Updated router, prompt template, MetaEvent schema, non-streaming LLM helper, three test files, Phase 30 prompt fixture, lecture/docs pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rag-retrieval/32-RESEARCH.md
@.planning/phases/32-rag-retrieval/32-02-PLAN.md
@.planning/phases/32-rag-retrieval/32-03-PLAN.md
@backend/app/copilot/router.py
@backend/app/copilot/prompts.py
@backend/app/copilot/schemas.py
@backend/app/copilot/llm.py
@.planning/phases/30-streaming-chat-mvp/30-SUMMARY.md

<interfaces>
From Phase 30 (`backend/app/copilot/router.py`):
- StreamingResponse with event-stream content type.
- `_require_admin_or_organizer` dependency guards the endpoint.
- `event: token` / `event: done` / `event: error` are the only events.
- A query-embedding step is NOT present yet — must be added here using the existing `EmbeddingProvider` from Phase 31.
- The system prompt in `prompts.py` is the load-bearing safety scaffolding (persona, refusal logic, admin-vs-organizer role differentiation). It MUST be preserved verbatim.

From Phase 30 (`backend/app/copilot/llm.py`):
- `stream_completion(messages, system_prompt) -> AsyncIterator[str]` — the OpenRouter caller used by the SSE endpoint.

From Plan 02:
```python
def hybrid_search(session, *, query_text, query_embedding, provider, top_n=20) -> list[HybridHit]
```
From Plan 03:
```python
def rerank(query, candidates: list[dict], top_k: int = 5) -> list[dict]
def chunks_to_citations(reranked, *, path_resolver) -> list[Citation]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MetaEvent schema + integration tests + Phase 30 prompt fixture (RED)</name>
  <files>backend/app/copilot/schemas.py, backend/tests/fixtures/phase_30_system_prompt.txt, backend/tests/test_copilot_router_with_retrieval.py, backend/tests/test_copilot_router_retrieval_errors.py</files>
  <behavior>
    Add to `schemas.py`:
    ```python
    class MetaEvent(BaseModel):
        citations: list[Citation]
        retrieval_latency_ms: int
        rerank_latency_ms: int
    ```

    **Capture the Phase 30 prompt baseline FIRST, before any edit to `prompts.py`:**
    Read the current Phase 30 system prompt (the exact string `prompts.py` currently
    produces / exports) and commit it verbatim as a fixture at
    `backend/tests/fixtures/phase_30_system_prompt.txt`. This file is the frozen
    baseline the preservation test asserts against.

    `test_copilot_router_with_retrieval.py`:
    - `test_sse_event_order` — POST a message via FastAPI TestClient (admin auth); parse the streamed events; assert order is `[meta, token, token, ..., done]` and `meta` appears exactly once (RESEARCH §Pitfall 5).
    - `test_meta_event_payload_shape` — `MetaEvent.model_validate_json(data_of_meta_event)` succeeds; `citations` has length ≤ 5; each has `id, source_path, char_start, char_end, quote, rrf_score, rerank_score`.
    - `test_retrieval_uses_active_provider` — `settings.corpus_embedding_primary="local"` → the hybrid_search call receives `provider="local-bge"`. Mock `hybrid_search` to assert the kwarg.
    - `test_system_prompt_contains_retrieved_context` — capture the prompt sent to `llm.stream_completion`; assert it contains `<retrieved_context>` and the content of the top-1 reranked chunk.
    - `test_system_prompt_preserves_phase_30_baseline` — read `backend/tests/fixtures/phase_30_system_prompt.txt`; build the new prompt (with `<retrieved_context>` block); assert the entire Phase 30 fixture string is a substring of the new prompt. This guards against accidental removal or modification of the persona / refusal / role-differentiation scaffolding.
    - `test_meta_event_emitted_even_when_zero_results` — empty hybrid results → `meta` event still emitted with `citations: []`; the LLM still streams a response (graceful degradation).
    - `test_existing_event_shapes_unchanged` — capture token + done events; assert their `data:` payloads match Phase 30 fixtures exactly (no extra keys, no shape change).

    `test_copilot_router_retrieval_errors.py`:
    - `test_hybrid_search_db_error_degrades_to_no_citations` — `hybrid_search` raises `OperationalError`; the endpoint still streams tokens; `meta` event has `citations: []` and `retrieval_latency_ms` reflects the elapsed time before the error; no `event: error` is emitted.
    - `test_rerank_exception_degrades_to_rrf_top5` — `rerank` raises; fall back to the top-5 RRF hits without rerank reordering (rerank_score=0.0); response still streams.
    - `test_embedding_provider_failure_degrades` — embedding fails (Jina 429 propagates as RateLimitError); fall back to FTS-only (no dense, no rerank); `meta` event still emitted with `citations` from FTS top-5.
  </behavior>
  <action>
    Use `fastapi.testclient.TestClient` + `EventSource`-style parsing (split on `\n\n`, parse `event:` / `data:` lines). Mock the LLM stream to return a deterministic 3-token sequence so tests don't hit OpenRouter. Mock `hybrid_search` and `rerank` per-test using `monkeypatch`. Reuse the Phase 30 auth fixtures.

    To capture the fixture: run the current `prompts.py` builder once with a representative input, write the resulting string to `backend/tests/fixtures/phase_30_system_prompt.txt`, and commit it in this same task BEFORE Task 2 edits `prompts.py`.

    Tests fail at this point — schema and router wiring don't exist yet.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_copilot_router_with_retrieval.py tests/test_copilot_router_retrieval_errors.py"</automated>
  </verify>
  <done>10 tests fail (RED) with assertions about missing `meta` event, missing schema fields, or the Phase 30 baseline not yet preserved. The fixture file `backend/tests/fixtures/phase_30_system_prompt.txt` exists and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire retrieval into router + append <retrieved_context> to preserved Phase 30 prompt (GREEN)</name>
  <files>backend/app/copilot/router.py, backend/app/copilot/prompts.py</files>
  <behavior>
    Router flow per request:
    1. Resolve `provider` from `settings.corpus_embedding_primary`.
    2. Embed the user's last message via the same provider used by Phase 31 ingest. Time it → `retrieval_latency_ms` accumulator.
    3. `hybrid_search(session, query_text=msg, query_embedding=emb, provider=provider, top_n=20)`. On exception, fall back to `[]` and continue.
    4. `rerank(msg, [asdict(h) for h in hits], top_k=5)`. On exception, fall back to first 5 hits with `rerank_score=0.0`. Time it → `rerank_latency_ms`.
    5. Resolve `document_id -> source_path` via a single `SELECT id, source_path FROM corpus_documents WHERE id = ANY(:ids)` query; build `path_resolver`.
    6. Build `MetaEvent(citations=chunks_to_citations(reranked, path_resolver=...), retrieval_latency_ms=..., rerank_latency_ms=...)`.
    7. As the first yielded chunk in the stream, emit `event: meta\ndata: {meta.model_dump_json()}\n\n`. Flush before the first `token`.
    8. Build the system prompt via Plan 04's updated `prompts.py` (see below) and stream the LLM as before.

    Set response header `X-Accel-Buffering: no` on the StreamingResponse (RESEARCH §Pitfall 5).
  </behavior>
  <action>
    **Update `prompts.py` by APPENDING a `<retrieved_context>` block to the EXISTING Phase 30
    system prompt. Do NOT remove or modify any existing persona / refusal /
    role-differentiation text. The existing prompt is the load-bearing safety scaffolding.**

    Concretely:
    - Keep the entire current Phase 30 prompt string untouched as the prefix.
    - Append (concatenate) the `<retrieved_context>` block after the existing prompt.
    - The block lists the rerank-top-5 chunks, one per line, with index, source path, char range, and quote.

    Suggested appended block (use f-string concat consistent with Phase 30's `prompts.py`
    style — do NOT introduce Jinja just for this):
    ```
    <retrieved_context>
    [1] source: {c.source_path} (chars {c.char_start}-{c.char_end})
    {c.quote}
    [2] source: ...
    ...
    </retrieved_context>
    ```

    The `test_system_prompt_preserves_phase_30_baseline` test asserts the captured Phase 30
    fixture string is a verbatim substring of the new prompt — if the test fails, the
    Phase 30 scaffolding has been mutated and MUST be restored before proceeding.

    Constraints:
    - Existing `event: token` / `event: done` / `event: error` shapes unchanged (Phase 30 invariant; test enforces).
    - All retrieval / rerank wrapped in try/except — failures degrade, don't crash the stream.
    - Embedding call reuses the Phase 31 provider; do not introduce a new embedding code path.
    - The Phase 30 system prompt is preserved verbatim (preservation test enforces).
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_copilot_router_with_retrieval.py tests/test_copilot_router_retrieval_errors.py --cov=app.copilot --cov-branch --cov-report=term-missing"</automated>
  </verify>
  <done>All 10 tests pass (including `test_system_prompt_preserves_phase_30_baseline`). `app.copilot.*` line + branch coverage ≥ 95%. Phase 30 latency SLO not regressed (sync rerank budget ≤ 350ms documented in SUMMARY).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Add stream_completion_blocking to app/copilot/llm.py (for Plan 07 RAGAS harness)</name>
  <files>backend/app/copilot/llm.py, backend/tests/test_copilot_llm_blocking.py</files>
  <behavior>
    Add a non-streaming variant to `app/copilot/llm.py`:
    ```python
    def stream_completion_blocking(messages, system_prompt) -> str:
        """Non-streaming variant of stream_completion.

        Calls the SAME OpenRouter code path as stream_completion, accumulates
        every token chunk into a single string, and returns the full text.
        Exists for the offline RAGAS harness (scripts/eval_rerank_lift.py)
        which needs a synchronous string-in / string-out call, not an async
        token iterator. NOT used by the SSE request path.
        """
    ```

    Implementation rule: do not duplicate the OpenRouter client / model / auth /
    error-handling logic. Reuse `stream_completion` internally (e.g. run the async
    iterator to completion via `asyncio.run` or an equivalent helper) and concatenate
    chunks. Same model, same env vars, same retry behaviour — only the return shape
    differs.

    Tests in `test_copilot_llm_blocking.py`:
    - `test_blocking_returns_accumulated_string` — mock the OpenRouter call to yield
      `["Hello", " ", "world"]`; assert `stream_completion_blocking(...) == "Hello world"`.
    - `test_blocking_propagates_errors` — mock the OpenRouter call to raise; assert the
      exception propagates (no silent swallow).
    - `test_blocking_uses_same_system_prompt_param` — assert the `system_prompt` kwarg is
      forwarded unchanged to the underlying caller.
  </behavior>
  <action>
    Place `stream_completion_blocking` in the same module so the Plan 07 import
    `from app.copilot.llm import stream_completion_blocking` resolves. Do not move it
    elsewhere. Keep the function public (no leading underscore).
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_copilot_llm_blocking.py"</automated>
  </verify>
  <done>All 3 blocking-variant tests pass. `from app.copilot.llm import stream_completion_blocking` resolves. Plan 07 Task 3 dependency satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Lecture + publication writeup — SSE meta event + retrieval-augmented stream</name>
  <files>docs/learning/32-rag-retrieval/04-sse-meta-event.md, docs/documentation/32-rag-retrieval/04-sse-meta-event.md</files>
  <action>
    Both ≥ 80 lines.

    **Learning**:
    1. Why `meta` is a separate SSE event (not inline markdown in tokens) — RESEARCH §Anti-patterns.
    2. The Phase 30 contract: token/done/error unchanged; meta is strictly additive.
    3. Why graceful degradation matters: if retrieval fails, the user still gets an answer.
    4. The `X-Accel-Buffering: no` header (Pitfall 5).
    5. Read through the new prompt template; explain `<retrieved_context>` block.
    6. Why the Phase 30 system prompt is preserved verbatim (load-bearing safety scaffolding) and how the substring assertion test enforces it.

    **Documentation**:
    - Methodology: SSE event taxonomy diff vs Phase 30.
    - Latency budget breakdown (embed + hybrid + rerank + LLM stream).
    - Graceful-degradation policy as an explicit design choice for the paper.
    - Cite RESEARCH §Pattern 5, §Pitfall 5, §Pitfall 7.
  </action>
  <verify>
    <automated>wc -l docs/learning/32-rag-retrieval/04-sse-meta-event.md docs/documentation/32-rag-retrieval/04-sse-meta-event.md | awk 'NR<=2 && $1<80 {exit 1}'</automated>
  </verify>
  <done>Both files exist, ≥ 80 lines, paired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user message → embedding+SQL | User query crosses into both the embedding model and FTS SQL. |
| LLM output → SSE | LLM response streamed to client; corpus content reaches the model. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-04-01 | Spoofing | `<retrieved_context>` injection in user prompt | mitigate | Retrieved chunks rendered inside a system-prompt block with explicit delimiters; user prompt rendered separately (RESEARCH §V5). |
| T-32-04-02 | Information disclosure | citation quote leaking PII | accept | Phase 31 walker excludes PII by construction; corpus is repo docs only. |
| T-32-04-03 | DoS | unbounded prompt size if too many chunks | mitigate | rerank `top_k=5`; chunks are 1024 chars max; total context bounded ≤ ~5KB. |
| T-32-04-04 | Repudiation | citations don't match generation | accept | Documented limitation (RESEARCH §Pitfall 7); UI copy says "Sources consulted". Phase 33 addresses with tool calling. |
| T-32-04-05 | Tampering | accidental removal of Phase 30 safety scaffolding | mitigate | `test_system_prompt_preserves_phase_30_baseline` asserts the frozen Phase 30 prompt fixture is a verbatim substring of every new prompt. |
</threat_model>

<verification>
- Both integration test files green (10 router tests + 3 llm-blocking tests).
- `grep -n 'event: meta' backend/app/copilot/router.py` returns the emission site.
- `grep -n 'X-Accel-Buffering' backend/app/copilot/router.py` returns the header.
- `grep -n 'stream_completion_blocking' backend/app/copilot/llm.py` returns the new function.
- `test -f backend/tests/fixtures/phase_30_system_prompt.txt` (the frozen baseline exists).
- `pytest --cov=app.copilot --cov-branch --cov-fail-under=95` passes (both `app.copilot.*` and `app.copilot.retrieval.*`).
- Manual: POST `/api/v1/copilot/messages`, observe meta event arrives before tokens via `curl -N`.
</verification>

<success_criteria>
- All must_haves truths observable via tests + manual curl.
- Graceful degradation proven by the 3 error-path tests.
- Phase 30 event shapes unchanged (test enforces).
- Phase 30 system prompt preserved verbatim (substring test enforces).
- `stream_completion_blocking` exported from `app.copilot.llm` and unit-tested.
- Two-folder docs paired and ≥ 80 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/32-rag-retrieval/32-04-SUMMARY.md` with sample SSE transcript (meta + first 3 tokens + done), measured retrieval/rerank latencies, the new system prompt template (showing the Phase 30 prefix + appended `<retrieved_context>` block), and the `stream_completion_blocking` signature.
</output>
