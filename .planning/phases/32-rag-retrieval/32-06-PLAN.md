---
phase: 32-rag-retrieval
plan: 06
type: execute
wave: 4
depends_on: [32-04, 32-05]
files_modified:
  - frontend/src/copilot/CitationChip.jsx
  - frontend/src/copilot/CitationPanel.jsx
  - frontend/src/copilot/useCopilotStream.js
  - frontend/src/copilot/CopilotDrawer.jsx
  - frontend/src/copilot/__tests__/CitationChip.test.jsx
  - frontend/src/copilot/__tests__/useCopilotStream.test.js
  - frontend/src/copilot/__tests__/CopilotDrawer.test.jsx
  - e2e/copilot-citations.spec.js
  - .github/workflows/ci.yml  # only edited if the existing Playwright invocation does NOT auto-pick up new e2e/*.spec.js files (see Task 3b)
  - docs/learning/32-rag-retrieval/06-citation-chips-frontend.md
  - docs/documentation/32-rag-retrieval/06-citation-chips-frontend.md
autonomous: true
requirements: [REQ-32-07, REQ-32-10, REQ-32-J]
must_haves:
  truths:
    - "`useCopilotStream` recognises `event: meta` and stores `citations` + latencies in stream state."
    - "`CitationChip` renders a clickable chip with `[N]` index, source filename, and tooltip quote."
    - "Clicking a chip opens `CitationPanel` (side-panel modal) fetching `/api/v1/copilot/citations/{id}`."
    - "Drawer renders up to 5 chips below the assistant message; scrolls horizontally on narrow viewports."
    - "When `document_url == ''`, the panel hides the external-link button (no broken/leaky link)."
    - "Existing Phase 30 token/done/error parsing in `useCopilotStream` is unchanged."
    - "`e2e/copilot-citations.spec.js` is covered by the CI Playwright matrix across all 6 browser projects — verified by grep against `.github/workflows/`."
  artifacts:
    - path: "frontend/src/copilot/CitationChip.jsx"
      provides: "Chip component (visual + click handler)"
    - path: "frontend/src/copilot/CitationPanel.jsx"
      provides: "Side-panel modal showing full quote + optional external link"
    - path: "e2e/copilot-citations.spec.js"
      provides: "Playwright cross-browser smoke (chip → panel → close)"
  key_links:
    - from: "SSE meta event"
      to: "citations state"
      via: "useCopilotStream's onmessage 'meta' branch"
      pattern: "event\\s*===\\s*['\\\"]meta['\\\"]"
    - from: "chip click"
      to: "GET /citations/{id}"
      via: "fetch in CitationPanel"
      pattern: "/api/v1/copilot/citations/"
    - from: "e2e/copilot-citations.spec.js"
      to: ".github/workflows/ci.yml"
      via: "Playwright spec glob in CI workflow picks up the new spec"
      pattern: "playwright test"
---

<objective>
Render citation chips in the chat drawer. Three new frontend components + a hook update +
one Playwright smoke. The hook handles the new `event: meta` branch additively (Phase 30
parsing untouched). RESEARCH §Pattern 5/6 + Open Question #2 (5 chips, scrollable).

Purpose: Close the user-visible loop — chips appear, click to inspect, external link optional.
Output: 3 frontend files, 1 hook update, 3 vitest specs, 1 Playwright spec, CI matrix coverage verified (and workflow edited if needed), lecture/docs pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rag-retrieval/32-RESEARCH.md
@.planning/phases/32-rag-retrieval/32-04-PLAN.md
@.planning/phases/32-rag-retrieval/32-05-PLAN.md
@frontend/src/copilot/CopilotDrawer.jsx
@frontend/src/copilot/useCopilotStream.js
@frontend/src/copilot/__tests__/CopilotDrawer.test.jsx
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCopilotStream — handle meta event additively (RED → GREEN)</name>
  <files>frontend/src/copilot/useCopilotStream.js, frontend/src/copilot/__tests__/useCopilotStream.test.js</files>
  <behavior>
    Tests:
    - `meta event populates citations state` — feed a mock EventSource stream with `event: meta\ndata: {"citations":[{id, source_path, char_start, char_end, quote, rrf_score, rerank_score}], "retrieval_latency_ms":50, "rerank_latency_ms":120}`; assert hook returns `{ citations: [...], latencies: { retrieval: 50, rerank: 120 } }`.
    - `meta event before token event` — assert citations available BEFORE first token is appended (RESEARCH §Pitfall 5 ordering).
    - `existing token/done/error branches unchanged` — Phase 30 fixtures still parse correctly.
    - `multiple messages keep latest citations only` — sending a new message resets citations to the new turn's payload.
    - `meta event with empty citations` — `citations: []` is rendered as zero chips (graceful empty state).
  </behavior>
  <action>
    Add a branch to the hook's event dispatcher:
    ```js
    } else if (event === 'meta') {
      const meta = JSON.parse(data);
      setCitations(meta.citations || []);
      setLatencies({ retrieval: meta.retrieval_latency_ms, rerank: meta.rerank_latency_ms });
    }
    ```
    Do not touch existing branches. Expose `citations` + `latencies` in the hook's return tuple/object so the drawer can render them.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run useCopilotStream</automated>
  </verify>
  <done>All useCopilotStream tests pass; Phase 30 fixtures still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CitationChip + CitationPanel components (RED → GREEN)</name>
  <files>frontend/src/copilot/CitationChip.jsx, frontend/src/copilot/CitationPanel.jsx, frontend/src/copilot/__tests__/CitationChip.test.jsx</files>
  <behavior>
    Tests (CitationChip):
    - `renders index, filename, and tooltip with quote`
    - `truncates long source_path to filename only` — `docs/learning/30-streaming-chat-mvp/01-sse.md` → display `01-sse.md`, full path on hover.
    - `clicking chip calls onClick with citation id`
    - `keyboard accessible` — Enter/Space activates the chip (role="button", tabIndex=0).

    Tests (CitationPanel — co-located in same vitest file or new one):
    - `renders quote + char range`
    - `external link button shown only when document_url is non-empty`
    - `clicking close calls onClose`

    Components:
    - `CitationChip` — small inline chip, `[N] filename`, tooltip on hover/focus shows `quote` truncated to ~200 chars.
    - `CitationPanel` — side-panel modal (NOT full nav-away; RESEARCH §Pattern 6 + Open Q #1). Fetches `/api/v1/copilot/citations/{id}` on open, shows source_path, char range, full content (scrollable), and an "Open source" link that appears only when `document_url` is non-empty.
  </behavior>
  <action>
    Use Tailwind v4 classes consistent with existing CopilotDrawer style. Title the panel "Source consulted" (RESEARCH §Pitfall 7 — honest copy, not "Source cited").
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run CitationChip</automated>
  </verify>
  <done>All CitationChip + CitationPanel tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire chips into CopilotDrawer + Playwright smoke</name>
  <files>frontend/src/copilot/CopilotDrawer.jsx, frontend/src/copilot/__tests__/CopilotDrawer.test.jsx, e2e/copilot-citations.spec.js</files>
  <behavior>
    Drawer tests:
    - `assistant message renders citation chips from hook state`
    - `chips show up to 5; horizontally scrollable on narrow viewport` (overflow-x-auto class asserted).
    - `clicking chip opens CitationPanel` (panel visible, then close hides it).
    - `no citations renders no chips section` (graceful empty state).

    Playwright (`e2e/copilot-citations.spec.js`):
    - Login as admin, open drawer, send a message that should produce citations, wait for the meta event to render chips, click chip 1, assert panel opens and shows content. Close panel. Send a second message; assert old chips are replaced.
    - Run across the same 6 browser projects the existing `cross-role.spec.js` uses (constraint: matches the v1.2 baseline).
  </behavior>
  <action>
    In `CopilotDrawer.jsx`, below each assistant message slot, render:
    ```jsx
    {citations.length > 0 && (
      <div className="flex gap-2 overflow-x-auto mt-2" role="list" aria-label="Sources consulted">
        {citations.slice(0,5).map((c, i) => (
          <CitationChip key={c.id} index={i+1} citation={c} onClick={() => setActiveCitation(c.id)} />
        ))}
      </div>
    )}
    {activeCitation && <CitationPanel chunkId={activeCitation} onClose={() => setActiveCitation(null)} />}
    ```
    Mock the SSE endpoint in vitest via the same approach Phase 30 used (no real backend hit).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run CopilotDrawer && cd .. && npx playwright test e2e/copilot-citations.spec.js --project=chromium</automated>
  </verify>
  <done>All vitest + the chromium Playwright project pass. Other 5 projects run on CI matrix (Task 3b confirms).</done>
</task>

<task type="auto">
  <name>Task 3b: Verify (and if needed extend) CI Playwright matrix coverage of the new spec</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    Goal: make CI matrix coverage of `e2e/copilot-citations.spec.js` across all 6
    browser projects EXPLICIT and VERIFIABLE — not assumed.

    Step 1 — Inspect:
    ```bash
    grep -nE 'playwright test|e2e/.*\.spec\.js|--project' .github/workflows/*.yml
    ```

    Step 2 — Decide based on what the grep reveals:

    Case A — implicit glob (CI runs `npx playwright test` with no spec list):
      The new spec is auto-picked up by Playwright's default `testMatch` (which globs
      `e2e/**/*.spec.js`). No workflow edit required. The verify step below proves it.

    Case B — explicit spec list (CI passes specific spec files to `npx playwright test`,
    e.g. `npx playwright test e2e/cross-role.spec.js e2e/foo.spec.js`):
      EDIT `.github/workflows/ci.yml` to add `e2e/copilot-citations.spec.js` to that
      list. Match the existing formatting (YAML list, line-continuation, etc.). Keep
      the matrix / project axis untouched — only the spec list changes.

    Case C — testMatch override in `playwright.config.js` that excludes the new spec:
      EDIT `playwright.config.js` (add to `files_modified` ad-hoc) so `testMatch`
      covers `e2e/copilot-citations.spec.js`. If no override exists, do nothing.

    Step 3 — Verification (REQUIRED, runs regardless of case):
    Confirm grep finds either (a) a non-spec-restricted `npx playwright test` invocation
    in the workflow, OR (b) an explicit reference to `copilot-citations.spec.js` in
    `.github/workflows/`. The automated verify command below encodes this check.
  </behavior>
  <action>
    Read `.github/workflows/ci.yml` and any sibling workflow files. Walk the three
    cases above. If Case A: do not edit the workflow, just record the finding in the
    plan summary. If Case B or C: make the minimal edit required and re-run grep to
    confirm. Do NOT change the matrix axis, browser project list, or any other
    unrelated workflow content — this task's scope is strictly "make sure the new
    spec is included in the existing 6-project matrix run".

    Document the chosen case (A / B / C) and the grep output in
    `32-06-SUMMARY.md` so the matrix-coverage claim is auditable.
  </action>
  <verify>
    <automated>grep -REn 'playwright test( |$)|copilot-citations\.spec\.js' .github/workflows/ | tee /tmp/pw-grep.out && { grep -qE 'npx playwright test\s*$|npx playwright test\s+--' /tmp/pw-grep.out || grep -q 'copilot-citations\.spec\.js' /tmp/pw-grep.out; }</automated>
  </verify>
  <done>Grep shows EITHER an unrestricted `npx playwright test` invocation in `.github/workflows/` (Case A — implicit glob covers the new spec) OR an explicit mention of `copilot-citations.spec.js` in `.github/workflows/` (Case B/C — workflow extended). The chosen case is recorded in `32-06-SUMMARY.md`. CI matrix coverage of the new spec across all 6 browser projects is now explicit and verifiable.</done>
</task>

<task type="auto">
  <name>Task 4: Lecture + publication writeup — Citation chips frontend</name>
  <files>docs/learning/32-rag-retrieval/06-citation-chips-frontend.md, docs/documentation/32-rag-retrieval/06-citation-chips-frontend.md</files>
  <action>
    Both ≥ 80 lines.

    **Learning**:
    1. The SSE meta event in React — single hook branch, state shape.
    2. Why a separate panel vs inline expansion (mobile UX, focus management).
    3. Honest copy: "Sources consulted" vs "Sources cited" (RESEARCH §Pitfall 7).
    4. Accessibility decisions (role, tabIndex, aria-label).

    **Documentation**:
    - Component diagram (hook → drawer → chip → panel → API).
    - UX rationale: 5 chips, horizontal scroll on narrow viewports (Open Q #2).
    - Cite RESEARCH §Pattern 5/6.
  </action>
  <verify>
    <automated>wc -l docs/learning/32-rag-retrieval/06-citation-chips-frontend.md docs/documentation/32-rag-retrieval/06-citation-chips-frontend.md | awk 'NR<=2 && $1<80 {exit 1}'</automated>
  </verify>
  <done>Both files exist, ≥ 80 lines, paired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| backend SSE → frontend | Meta payload trusted (server-rendered citations). |
| chip click → API | Authenticated fetch with session cookie. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-06-01 | Tampering (XSS via quote) | CitationChip tooltip + Panel content | mitigate | React's default escaping; never `dangerouslySetInnerHTML` for citation content. |
| T-32-06-02 | Information disclosure | `document_url` link leaks repo origin | accept | Server-controlled origin (Plan 05); empty by default. |
</threat_model>

<verification>
- All vitest specs green.
- At least chromium Playwright project green; full 6-project matrix runs on CI (Task 3b grep proves coverage).
- Manual: open drawer, send a known query, see 5 chips, click → panel → close.
</verification>

<success_criteria>
- All must_haves truths observable in the running UI.
- Phase 30 SSE parsing unchanged (test enforces).
- Honest copy in panel header.
- CI Playwright matrix coverage of `e2e/copilot-citations.spec.js` confirmed by grep (Task 3b).
- Two-folder docs paired and ≥ 80 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/32-rag-retrieval/32-06-SUMMARY.md` with screenshot of chips + panel, the Playwright matrix result, and the Task 3b grep output proving CI coverage (with the chosen case A/B/C recorded).
</output>
