---
phase: 32-rag-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0020_add_corpus_chunk_fts_column.py
  - backend/tests/test_corpus_fts_migration.py
  - docs/learning/32-rag-retrieval/01-fts-in-postgres.md
  - docs/documentation/32-rag-retrieval/01-fts-in-postgres.md
autonomous: true
requirements: [REQ-32-01, REQ-32-09, REQ-32-10, REQ-32-J]
must_haves:
  truths:
    - "Alembic 0020 upgrades cleanly on a fresh test DB."
    - "All 4,731 existing corpus_chunks rows have a non-null `fts` value after upgrade."
    - "GIN index `ix_corpus_chunks_fts` exists and is used by the planner for `fts @@ to_tsquery('english', ...)` queries."
    - "Migration round-trip (upgrade → downgrade → upgrade) is clean."
  artifacts:
    - path: "backend/alembic/versions/0020_add_corpus_chunk_fts_column.py"
      provides: "Additive migration: tsvector generated column + GIN index"
      contains: "GENERATED ALWAYS AS"
    - path: "backend/tests/test_corpus_fts_migration.py"
      provides: "Round-trip + GIN-index-used integration tests"
    - path: "docs/learning/32-rag-retrieval/01-fts-in-postgres.md"
      provides: "Lecture: Postgres tsvector + GIN + generated columns"
      min_lines: 80
    - path: "docs/documentation/32-rag-retrieval/01-fts-in-postgres.md"
      provides: "Publication writeup of the FTS layer (paper-ready, citations inline)"
      min_lines: 80
  key_links:
    - from: "corpus_chunks.fts"
      to: "ix_corpus_chunks_fts (GIN)"
      via: "GENERATED ALWAYS AS (to_tsvector('english', coalesce(content,''))) STORED"
      pattern: "USING GIN\\s*\\(fts\\)"
---

<objective>
Add the lexical retrieval substrate Phase 32 hybrid search depends on: a generated `tsvector`
column on `corpus_chunks` plus a GIN index, shipped as a strictly additive Alembic migration
(`0020`). No application backfill — the generated column auto-populates the 4,731 existing rows
at ALTER time. Per RESEARCH §FTS column strategy and locked decision D-01 (Postgres native FTS only).

Purpose: Unlock REQ-32-02 (hybrid retrieval) by giving Plan 02 a tsvector column to query.
Output: `0020_add_corpus_chunk_fts_column.py`, a passing round-trip test, and the lecture/docs pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rag-retrieval/32-RESEARCH.md
@.planning/phases/31-corpus-pgvector-ingestion/31-SUMMARY.md
@backend/alembic/versions/0019_enable_pgvector_corpus_tables.py
@backend/alembic/env.py
@backend/tests/test_corpus_migration_round_trip.py

<interfaces>
From backend/app/models.py (CorpusChunk — frozen, do not edit):
```python
class CorpusChunk(Base):
    __tablename__ = "corpus_chunks"
    id: Mapped[uuid.UUID]
    document_id: Mapped[uuid.UUID]
    content: Mapped[str]
    char_start: Mapped[int]
    char_end: Mapped[int]
    embedding: Mapped[Vector]  # vector(1024)
    embedding_provider: Mapped[str]
    embedding_model: Mapped[str]
    # NEW (added via migration 0020 — do not edit ORM here; Phase 32 reads via raw SQL/text)
    # fts: tsvector (generated, stored)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write round-trip + index-usage test for migration 0020 (RED)</name>
  <files>backend/tests/test_corpus_fts_migration.py</files>
  <behavior>
    - Test 1 `test_upgrade_adds_fts_column_and_index`: after upgrading head, `corpus_chunks` has a column named `fts` of type `tsvector`, and `pg_indexes` shows `ix_corpus_chunks_fts` of method `gin`.
    - Test 2 `test_existing_rows_populated`: seed 3 chunks before upgrade (or use the existing pre-0020 head), upgrade, assert every row has non-null `fts` and `fts @@ to_tsquery('english','volunteer')` matches a chunk containing the word "volunteers".
    - Test 3 `test_round_trip_clean`: upgrade → downgrade to `0019` → upgrade — no `DuplicateObject`, no leftover index, downgrade leaves zero `fts` columns.
    - Test 4 `test_gin_index_used_by_planner`: insert 200 chunks, `ANALYZE corpus_chunks`, run `EXPLAIN (FORMAT JSON) SELECT id FROM corpus_chunks WHERE fts @@ to_tsquery('english','volunteer')` and assert the plan contains `"Bitmap Index Scan"` on `ix_corpus_chunks_fts` (or `"Index Scan"`), NOT `"Seq Scan"`.
  </behavior>
  <action>
    Create `backend/tests/test_corpus_fts_migration.py`. Mirror the style of `backend/tests/test_corpus_migration_round_trip.py` (`alembic.command.upgrade/downgrade` against `TEST_DATABASE_URL`, a fresh `alembic.config.Config` per test). Use `sqlalchemy.text` for raw SQL. For Test 4, set `enable_seqscan = off` only as a fallback assertion — primary assertion is the plan shape with seqscan enabled. Tests MUST run inside the compose network (constraint C1).
    Tests will fail until Task 2 ships the migration — this is the RED step.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_corpus_fts_migration.py"</automated>
  </verify>
  <done>4 tests fail with a clear "no such column fts" or "relation ix_corpus_chunks_fts does not exist" — confirming RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement migration 0020 (GREEN)</name>
  <files>backend/alembic/versions/0020_add_corpus_chunk_fts_column.py</files>
  <action>
    Create the Alembic revision with slug ID `0020_add_corpus_chunk_fts_column` (constraint C2). `down_revision = "0019_enable_pgvector_corpus_tables"`.

    `upgrade()`:
    ```python
    op.execute(
        "ALTER TABLE corpus_chunks "
        "ADD COLUMN fts tsvector "
        "GENERATED ALWAYS AS (to_tsvector('english', coalesce(content, ''))) STORED"
    )
    op.execute("CREATE INDEX ix_corpus_chunks_fts ON corpus_chunks USING GIN (fts)")
    ```

    `downgrade()`:
    ```python
    op.execute("DROP INDEX IF EXISTS ix_corpus_chunks_fts")
    op.execute("ALTER TABLE corpus_chunks DROP COLUMN IF EXISTS fts")
    ```

    Constraints:
    - Strictly additive — no edits to existing columns or indexes (Phase 31 SUMMARY hard rule, C3).
    - Use `english` ts_config (locked, RESEARCH §Pattern 1). Do not use `simple`.
    - Use GIN, not GiST (RESEARCH §Pattern 1).
    - Do not edit `backend/app/models.py` — Plan 02 reads the column via `sqlalchemy.text`, not via the ORM. This keeps Phase 31's frozen ORM contract intact.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_corpus_fts_migration.py"</automated>
  </verify>
  <done>All 4 tests from Task 1 pass. Coverage on `app.corpus.*` unchanged (no app code touched).</done>
</task>

<task type="auto">
  <name>Task 3: Lecture + publication writeup — FTS in Postgres (two-folder rule)</name>
  <files>docs/learning/32-rag-retrieval/01-fts-in-postgres.md, docs/documentation/32-rag-retrieval/01-fts-in-postgres.md</files>
  <action>
    Both files ≥ 80 lines (C4 / REQ-32-J).

    **`docs/learning/32-rag-retrieval/01-fts-in-postgres.md`** — teaching tone for Andy. Cover:
    1. What `tsvector` actually is (a sorted multiset of lexemes with positions).
    2. Why `to_tsvector('english', ...)` — Snowball stemmer + stopwords (RESEARCH §Pattern 1).
    3. Generated columns vs triggers: why GENERATED is declarative & round-trip safe.
    4. GIN vs GiST: GIN is the read-heavy default; ours is a read-heavy corpus.
    5. The `plainto_tsquery` vs `to_tsquery` operator-injection gotcha (RESEARCH §Pitfall 1, §Security V5).
    6. Why the column populates existing rows at ALTER time (no backfill script).
    7. Inspecting via `EXPLAIN`: how to confirm the index is actually used.

    **`docs/documentation/32-rag-retrieval/01-fts-in-postgres.md`** — publication tone. Cover the schema change, cite the PG docs (`[CITED: postgresql.org/docs/current/textsearch-tables.html]`, `[CITED: postgresql.org/docs/current/ddl-generated-columns.html]`), state the design choices (english config, GIN, generated column) with the rejected alternatives table from RESEARCH §Standard Stack, and report the migration measurements (4,731 rows populated in N ms — fill in after smoke).
  </action>
  <verify>
    <automated>wc -l docs/learning/32-rag-retrieval/01-fts-in-postgres.md docs/documentation/32-rag-retrieval/01-fts-in-postgres.md | awk 'NR<=2 && $1<80 {exit 1}'</automated>
  </verify>
  <done>Both files exist, ≥ 80 lines each, link from the phase SUMMARY index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migration → DB | Schema change touches the corpus table. Migration is repo-internal, run by operator. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-01-01 | Tampering | `0020` migration | mitigate | Round-trip test (Task 1, Test 3) proves downgrade is clean — no orphan column / index after revert. |
| T-32-01-02 | Denial of Service | ALTER TABLE on a populated table | accept | Corpus has 4,731 rows; ALTER + GIN index build is bounded (RESEARCH A2). Operator runs migration during a maintenance window per ops policy. |
</threat_model>

<verification>
- `pytest -q tests/test_corpus_fts_migration.py` — all 4 tests pass.
- `alembic upgrade head` clean on a fresh DB.
- `\d corpus_chunks` in psql shows `fts | tsvector | generated always as (to_tsvector('english'::regconfig, COALESCE(content, ''::text))) stored`.
- `app.corpus.*` coverage gate unchanged (no app code touched; gate stays at 100%).
</verification>

<success_criteria>
- All 4 must_haves truths observable on a running test DB.
- Round-trip clean (Test 3 green).
- Learning + documentation writeups exist, ≥ 80 lines, paired.
</success_criteria>

<output>
After completion, create `.planning/phases/32-rag-retrieval/32-01-SUMMARY.md` recording row-count of populated `fts` values, EXPLAIN output snippet, and the migration timing.
</output>
