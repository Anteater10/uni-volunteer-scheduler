---
phase: 32-rag-retrieval
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - backend/app/copilot/retrieval/__init__.py
  - backend/app/copilot/retrieval/dense.py
  - backend/app/copilot/retrieval/fts.py
  - backend/app/copilot/retrieval/hybrid.py
  - backend/tests/conftest.py
  - backend/tests/test_retrieval_dense.py
  - backend/tests/test_retrieval_fts.py
  - backend/tests/test_retrieval_hybrid.py
  - docs/learning/32-rag-retrieval/02-hybrid-retrieval-rrf.md
  - docs/documentation/32-rag-retrieval/02-hybrid-retrieval-rrf.md
autonomous: true
requirements: [REQ-32-02, REQ-32-09, REQ-32-10, REQ-32-J]
must_haves:
  truths:
    - "`dense_search(query_embedding, provider, k)` returns chunks ranked by cosine, filtered to `embedding_provider = provider`."
    - "`fts_search(query_text, provider, k)` returns chunks ranked by `ts_rank_cd`, also filtered by `embedding_provider`."
    - "`hybrid_search(query_text, query_embedding, provider, k)` fuses both via RRF (k=60) in a single SQL CTE and returns top-N with `rrf_score`."
    - "Every cosine SQL string in `app.copilot.retrieval.*` contains `WHERE embedding_provider`."
    - "FTS SQL uses `plainto_tsquery('english', :q)` (NOT `to_tsquery(:q)` — operator-injection safe)."
  artifacts:
    - path: "backend/app/copilot/retrieval/dense.py"
      provides: "Cosine top-K with per-provider filter"
      exports: ["dense_search", "DenseHit"]
    - path: "backend/app/copilot/retrieval/fts.py"
      provides: "tsvector top-K with per-provider filter"
      exports: ["fts_search", "FtsHit"]
    - path: "backend/app/copilot/retrieval/hybrid.py"
      provides: "RRF fusion via single SQL CTE"
      exports: ["hybrid_search", "HybridHit"]
    - path: "backend/tests/conftest.py"
      provides: "`corpus_fixture` factory (10 chunks across 2 providers)"
  key_links:
    - from: "hybrid_search"
      to: "corpus_chunks"
      via: "WITH dense AS (...) , fts AS (...) SELECT ... rrf_score"
      pattern: "1\\.0\\s*/\\s*\\(60\\s*\\+"
    - from: "every dense SELECT"
      to: "embedding_provider"
      via: "WHERE embedding_provider = :provider"
      pattern: "embedding_provider\\s*="
---

<objective>
Implement the hybrid retrieval layer Phase 32's copilot router will call. Three modules under
`backend/app/copilot/retrieval/`: dense (cosine top-K), fts (tsvector top-K), and hybrid (single-SQL
RRF fusion at k=60). Every SQL string enforces the per-provider invariant from Phase 31 SUMMARY,
including on FTS (RESEARCH §Pattern 3 — pushed into SQL, not application-layer).

Purpose: Convert the FTS column from Plan 01 + the HNSW index from Phase 31 into a usable hybrid retriever.
Output: Three retrieval modules, three pytest files, a shared conftest fixture, and the lecture/docs pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rag-retrieval/32-RESEARCH.md
@.planning/phases/32-rag-retrieval/32-01-PLAN.md
@backend/app/models.py
@backend/app/config.py
@backend/app/db.py
@backend/tests/test_corpus_hnsw_index.py

<interfaces>
From backend/app/config.py:
```python
corpus_embedding_primary: Literal["jina", "local"]  # selects active provider
# Actual provider string in DB: "jina-v3-embeddings" or "local-bge" — see Phase 31 telemetry.
```

From backend/app/models.py:
```python
class CorpusChunk:
    id: uuid.UUID
    document_id: uuid.UUID
    content: str
    char_start: int
    char_end: int
    embedding: Vector  # 1024-dim
    embedding_provider: str   # filter on this in every dense query
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add corpus_fixture to conftest + write failing tests for dense/fts/hybrid (RED)</name>
  <files>backend/tests/conftest.py, backend/tests/test_retrieval_dense.py, backend/tests/test_retrieval_fts.py, backend/tests/test_retrieval_hybrid.py</files>
  <behavior>
    `conftest.py` adds `corpus_fixture(session)` — a factory inserting 10 chunks: 6 with `embedding_provider="local-bge"`, 4 with `embedding_provider="jina-v3-embeddings"`. Content includes overlapping topical keywords so FTS and dense should disagree on some rankings (the point of hybrid). Embeddings are deterministic numpy arrays (seeded).

    `test_retrieval_dense.py`:
    - `test_dense_search_filters_by_provider` — query with `provider="local-bge"`; ALL returned rows have that provider.
    - `test_dense_search_returns_top_k_ordered` — closest-by-cosine first.
    - `test_dense_search_empty_when_provider_unknown` — provider="nonexistent" returns [].
    - `test_dense_search_sql_contains_provider_filter` — captures the rendered SQL via SA event hook OR via inspecting the executable text; assertion `"embedding_provider"` substring present.

    `test_retrieval_fts.py`:
    - `test_fts_search_uses_plainto_tsquery` — passes `"volunteer & orientation"` (an FTS operator if `to_tsquery` were used); assert no exception, results consistent with treating it as a plain phrase.
    - `test_fts_search_respects_provider_filter` — same setup as dense; provider filter pushed into SQL.
    - `test_fts_search_uses_gin_index` — EXPLAIN proves `ix_corpus_chunks_fts` is used.
    - `test_fts_search_empty_query_returns_empty` — `""` and whitespace-only inputs return `[]` without an exception.

    `test_retrieval_hybrid.py`:
    - `test_hybrid_rrf_math` — given two synthetic ranked lists, hand-compute `1/(60+1) + 1/(60+3)` etc. and assert ordering.
    - `test_hybrid_uses_single_sql_roundtrip` — instrument the SA engine with `before_cursor_execute` event; assert exactly 1 query (not 2) fires during `hybrid_search`.
    - `test_hybrid_respects_provider_filter_on_both_sides` — chunks from the "wrong" provider never appear in results, even though they match the FTS query.
    - `test_hybrid_tiebreak_deterministic` — two near-identical chunks → ordering by `chunk_id` ascending (RESEARCH §Pitfall 6).
    - `test_hybrid_empty_query_returns_empty` — both empty query text and empty embedding return [].
  </behavior>
  <action>
    Use `sqlalchemy.text` and `Session.execute` directly in the modules; tests assert against the SA Result. Tests run inside the compose network (constraint C1). Tests fail at this stage — modules do not yet exist.
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_retrieval_dense.py tests/test_retrieval_fts.py tests/test_retrieval_hybrid.py"</automated>
  </verify>
  <done>All tests fail with ImportError on `app.copilot.retrieval.*` — RED confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dense.py + fts.py with per-provider filter (GREEN, part 1)</name>
  <files>backend/app/copilot/retrieval/__init__.py, backend/app/copilot/retrieval/dense.py, backend/app/copilot/retrieval/fts.py</files>
  <behavior>
    Both functions return `list[Hit]` where Hit is a frozen dataclass with `id`, `document_id`, `content`, `char_start`, `char_end`, and `rank` (1-based within source).
  </behavior>
  <action>
    `dense.py`:
    ```python
    DENSE_SQL = text("""
      SELECT id, document_id, content, char_start, char_end,
             row_number() OVER (ORDER BY embedding <=> :qvec) AS rank
      FROM corpus_chunks
      WHERE embedding_provider = :provider
      ORDER BY embedding <=> :qvec
      LIMIT :k
    """)
    def dense_search(session, *, query_embedding: list[float], provider: str, k: int = 20) -> list[DenseHit]: ...
    ```
    Bind `qvec` via the pgvector psycopg2 adapter (Phase 31 already imports it).

    `fts.py`:
    ```python
    FTS_SQL = text("""
      SELECT id, document_id, content, char_start, char_end,
             row_number() OVER (ORDER BY ts_rank_cd(fts, q) DESC) AS rank
      FROM corpus_chunks, plainto_tsquery('english', :q) AS q
      WHERE fts @@ q
        AND embedding_provider = :provider
      ORDER BY ts_rank_cd(fts, q) DESC
      LIMIT :k
    """)
    def fts_search(session, *, query_text: str, provider: str, k: int = 20) -> list[FtsHit]:
        if not query_text or not query_text.strip():
            return []
        ...
    ```
    Use `plainto_tsquery` exclusively (Pitfall 1 + ASVS V5). Never construct `to_tsquery` from user input.

    `__init__.py` re-exports `dense_search`, `fts_search`, `DenseHit`, `FtsHit`.

    Constraints:
    - SQL is `text()` not raw f-strings — bound parameters only (SQLi defense).
    - Every dense SELECT contains `WHERE embedding_provider = :provider`; even FTS does (RESEARCH §Pattern 3 — the invariant must be a SQL-level guarantee).
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_retrieval_dense.py tests/test_retrieval_fts.py --cov=app.copilot.retrieval --cov-branch"</automated>
  </verify>
  <done>All dense + fts tests pass; per-module branch coverage ≥ 95%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement hybrid.py — single-SQL RRF fusion at k=60 (GREEN, part 2)</name>
  <files>backend/app/copilot/retrieval/hybrid.py, backend/app/copilot/retrieval/__init__.py</files>
  <behavior>
    `hybrid_search` runs ONE round-trip SQL containing two CTEs (`dense`, `fts`) and a fusion SELECT (see RESEARCH §Code Examples). Returns top-N `HybridHit(id, document_id, content, char_start, char_end, rrf_score)` ordered by `rrf_score DESC, id ASC`.
  </behavior>
  <action>
    Implement the full CTE from RESEARCH §"Hybrid retrieval — single SQL CTE". Hard-code `k_rrf = 60` (locked, RESEARCH §Pattern 2 — D-03). Per-retriever LIMIT=20 (RESEARCH §Pattern 2). Final ORDER BY: `rrf_score DESC, id ASC` (tiebreak — RESEARCH §Pitfall 6).

    Signature:
    ```python
    def hybrid_search(
        session,
        *,
        query_text: str,
        query_embedding: list[float],
        provider: str,
        top_n: int = 20,
    ) -> list[HybridHit]:
        if not query_text or not query_text.strip():
            return []
        ...
    ```

    Re-export from `__init__.py`. Do NOT call `dense_search` + `fts_search` from Python and fuse in app code — fusion happens in SQL (single round-trip, RESEARCH "Hybrid retrieval — single SQL CTE", Pitfall not-stated-but-implied: app-layer fusion duplicates queries).
  </action>
  <verify>
    <automated>docker run --rm --network uni-volunteer-scheduler_default -v $PWD/backend:/app -w /app -e TEST_DATABASE_URL="postgresql+psycopg2://postgres:postgres@db:5432/test_uvs" uni-volunteer-scheduler-backend sh -c "pytest -q tests/test_retrieval_hybrid.py --cov=app.copilot.retrieval --cov-branch --cov-report=term-missing"</automated>
  </verify>
  <done>All hybrid tests pass; `app.copilot.retrieval.*` line coverage ≥ 95%, branch coverage ≥ 95%.</done>
</task>

<task type="auto">
  <name>Task 4: Lecture + publication writeup — Hybrid retrieval + RRF</name>
  <files>docs/learning/32-rag-retrieval/02-hybrid-retrieval-rrf.md, docs/documentation/32-rag-retrieval/02-hybrid-retrieval-rrf.md</files>
  <action>
    Both ≥ 80 lines.

    **Learning** (`docs/learning/32-rag-retrieval/02-hybrid-retrieval-rrf.md`):
    1. Why two retrievers: lexical catches exact-match queries dense misses; dense catches synonymy lexical misses (RESEARCH §State of the Art).
    2. The RRF intuition: `1/(k+rank)` — rank, not score; robust to ts_rank's unbounded log-scale.
    3. Why k=60 (Cormack/Clarke/Büttcher 2009) — stable across corpora.
    4. The per-provider invariant: why FTS needs the filter too (Pattern 3 + Pitfall 3).
    5. Walk through the single SQL CTE — what each piece does, how COALESCE handles UNION-side-missing.
    6. Why fusion in SQL beats fusion in Python.

    **Documentation** (`docs/documentation/32-rag-retrieval/02-hybrid-retrieval-rrf.md`):
    - Paper-grade methodology section. Cite RRF paper (Cormack 2009), Tiger Data hybrid post (`[CITED: tigerdata.com/...]`).
    - Include the exact SQL block.
    - Table: alternatives considered (weighted-sum, convex-combination) and rejection reasons.
  </action>
  <verify>
    <automated>wc -l docs/learning/32-rag-retrieval/02-hybrid-retrieval-rrf.md docs/documentation/32-rag-retrieval/02-hybrid-retrieval-rrf.md | awk 'NR<=2 && $1<80 {exit 1}'</automated>
  </verify>
  <done>Both files exist, ≥ 80 lines, paired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user query → SQL | User-provided text reaches `plainto_tsquery`; embedding floats reach `<=>` operator. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-02-01 | Tampering (SQLi) | fts_search query | mitigate | All inputs bound via SA `text()` parameters; `plainto_tsquery` strips FTS operators (RESEARCH §V5, Pitfall 1). |
| T-32-02-02 | Tampering (cross-provider leak) | dense/fts/hybrid SQL | mitigate | Per-provider filter pushed into every SQL string (Pattern 3); test asserts substring `embedding_provider` in rendered SQL. |
| T-32-02-03 | DoS | unbounded LIMIT | mitigate | `k` and `top_n` defaults are 20; caller-supplied values clamped to ≤ 100 inside each function. |
</threat_model>

<verification>
- `pytest -q tests/test_retrieval_*.py` all green.
- `pytest --cov=app.copilot.retrieval --cov-branch --cov-fail-under=95` passes.
- `grep -L embedding_provider backend/app/copilot/retrieval/*.py` returns empty (every module file mentions the filter column).
- `grep -n to_tsquery backend/app/copilot/retrieval/` returns NO hits (only `plainto_tsquery`).
</verification>

<success_criteria>
- All must_haves truths observable via tests.
- Per-provider invariant enforced in SQL, asserted in tests.
- RRF k=60 hard-coded; tiebreak deterministic.
- Two-folder docs paired and ≥ 80 lines each.
</success_criteria>

<output>
After completion, create `.planning/phases/32-rag-retrieval/32-02-SUMMARY.md` recording the rendered SQL for each retriever, the corpus_fixture shape, and the coverage delta.
</output>
