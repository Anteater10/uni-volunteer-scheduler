# v1.3 Verification — 2026-04-17

## Automated

- Backend: `pytest --no-cov` → **344 passed / 0 failed** (includes 4 new
  `tests/test_import_merge.py` cases covering the workflow bug fix).
- Frontend unit: `npm run test -- --run` → **206 passed / 33 files**.
- Playwright: not executed. `tests/playwright/v1.3-integration.spec.js` is
  `test.describe.skip`'d and there is still no `playwright.config.js` or
  `@playwright/test` devDep. Enabling it is its own wiring task — out of
  scope for tonight's run.

## Workflow bug — fixed

- `backend/app/services/import_service.py` now keys Events on
  `(family_slug, school, quarter, year)` and picks `slot_type` from
  `ModuleTemplate.type` (module → `PERIOD`, orientation → `ORIENTATION`).
- Orientation CSVs without a `school` column return 400 with a message
  telling the admin to add the column and retry.
- Naive ISO datetimes from the LLM are coerced to UTC tz-aware at
  normalisation, fixing `offset-naive vs offset-aware` merge comparisons.
- Commit returns `{created_count, merged_count, events[]}`.

## CRUD additions

- `POST /admin/module-templates/{slug}/clone` + TemplatesSection drawer button.
- `DELETE /admin/imports/{id}` + ImportsSection row action (guarded by confirm).

## Manual smoke — pending

`docs/smoke-checklist.md` (the three-window ~30-min pass) has NOT been
re-run tonight. Recommend Andy walks it once before tagging v1.3.
